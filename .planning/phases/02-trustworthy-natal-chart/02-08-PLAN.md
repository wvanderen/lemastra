---
phase: 02-trustworthy-natal-chart
plan: 08
type: execute
wave: 4
depends_on: ["02-06"]
files_modified:
  - src/components/birth/tricky-time-picker.tsx
  - src/components/birth/calculation-disclosure.tsx
  - src/components/birth/copy.ts
  - src/app/birth/confirm.tsx
  - src/app/birth.tsx
  - src/app/chart/result.tsx
  - src/app/_layout.tsx
  - src/data/provider-registry.json
  - src/schemas/registry.test.ts
  - src/__tests__/tricky-time-picker.test.tsx
  - src/__tests__/calculation-disclosure.test.tsx
  - src/__tests__/confirm-screen.test.tsx
autonomous: true
requirements: [BIRTH-02, BIRTH-03]
must_haves:
  truths:
    - "The confirm screen shows the resolved place label, coordinates, IANA zone, and the locally-resolved UTC offset for the birth date, with the drift note only when drift is true"
    - "Ambiguous times render two option cards (first/second pass with offsets) and nonexistent times render the single shifted-instant card; Calculate stays disabled until a choice is made"
    - "First run intercepts Calculate with the registry-driven disclosure; 'Got it — Calculate chart' acknowledges and never re-shows"
    - "Successful calculation navigates to /chart/result carrying the envelope; every CALC-04 failure renders the recovery banner"
    - "The provider registry marks exactly lemastra-calculation and google-geocoding-timezone active, with the Phase-1 consistency tests updated in lockstep and green"
  artifacts:
    - path: "src/app/birth/confirm.tsx"
      provides: "BIRTH-02 confirmation screen with D-08 picker, D-04 disclosure intercept, calculate mutation"
      contains: "Calculate chart"
    - path: "src/components/birth/tricky-time-picker.tsx"
      provides: "D-08 explicit resolution picker driven by server classification/options"
      contains: "nonexistent"
    - path: "src/components/birth/calculation-disclosure.tsx"
      provides: "Registry-driven one-time disclosure (zero own provider content)"
      contains: "provider-registry"
    - path: "src/app/chart/result.tsx"
      provides: "Minimal real result screen (title, identity line, validation status) deepened by 02-09"
      contains: "natal chart"
  key_links:
    - from: "src/components/birth/tricky-time-picker.tsx"
      to: "src/lib/api-schemas.ts"
      via: "options/consumed verbatim from the resolve payload — the UI never re-derives offsets"
      pattern: "ResolveTimeResponse"
    - from: "src/components/birth/calculation-disclosure.tsx"
      to: "src/data/provider-registry.json"
      via: "renders the two locked provider entries; contains no provider strings of its own (Phase-1 test-enforced invariant)"
      pattern: "lemastra-calculation"
    - from: "src/app/birth/confirm.tsx"
      to: "src/hooks/use-disclosure.ts"
      via: "acknowledged gate around the first Calculate tap"
      pattern: "useDisclosure"
---

<objective>
Deliver the trust hinge of the flow: the BIRTH-02 confirmation screen. Shows the resolved place/coordinates/zone and the D-06 historical UTC offset (with drift note), forces the D-08 explicit choice for ambiguous/nonexistent civil times before Calculate, intercepts the first calculation with the D-04 registry-driven disclosure, runs the calculate mutation with full CALC-04 recovery banners, and navigates to a minimal real result screen (deepened in 02-09). Closes with the governed registry activation flip (planned → active for the two locked provider ids) in lockstep with the Phase-1 consistency tests.

Purpose: ROADMAP success criteria 1 (confirm + explicit tricky-time resolution) and the governance rule that enabling remote flows requires the registry flip in the same change.
Output: /birth/confirm route + minimal /chart/result route + two components + registry flip with green suites.
</objective>

## Phase Goal (user story)

**As a** LemAstra user (curious beginner or serious enthusiast), **I want to** enter my birth details, confirm the resolved place and historical timezone, and explicitly resolve ambiguous or nonexistent civil times, **so that** I receive a validated, provenance-rich natal chart I can trust and inspect before any interpretation.

*(Derived from ROADMAP Phase 2 Goal + PROJECT.md audience definition.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-natal-chart/02-UI-SPEC.md
@.planning/phases/02-trustworthy-natal-chart/02-PATTERNS.md
@.planning/phases/02-trustworthy-natal-chart/02-02-SUMMARY.md
@.planning/phases/02-trustworthy-natal-chart/02-06-SUMMARY.md

- 02-UI-SPEC.md §"/birth/confirm — Confirmation" (layout order), §"Tricky-time picker" copy, §"One-time disclosure (D-04)" (registry-driven content rule + CTA), §"Calculating state", §"Copy Deck" confirm + disclosure strings
- 02-RESEARCH.md §"Resolve payload for the D-08 picker" (labels come from the server), §"Registry status flip is a governed act" (pitfall 9), §"Library wiring" (useMutation, no auto-retry on non-network errors)
- 02-PATTERNS.md §B (registry-driven disclosure excerpt from privacy.tsx), §D4 (the flip + the two lockstep tests with line references)
- src/data/provider-registry.json, src/schemas/registry.test.ts, src/app/privacy.tsx, src/__tests__/privacy-screen.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tricky-time picker + registry-driven calculation disclosure</name>
  <files>src/components/birth/tricky-time-picker.tsx, src/components/birth/calculation-disclosure.tsx, src/components/birth/copy.ts, src/__tests__/tricky-time-picker.test.tsx, src/__tests__/calculation-disclosure.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"Tricky-time picker" and §"One-time disclosure (D-04)" sections (exact headings/bodies/footnotes/CTA + the no-own-provider-content rule), §"Copy Deck" confirm-screen strings
  - 02-RESEARCH.md §"Resolve payload for the D-08 picker" (server option shapes with mode/label/utc)
  - src/app/privacy.tsx (registry-driven card pattern to replicate — data-category chips, When it sends, Retention, Purpose)
  - src/data/provider-registry.json (the two entries' field names)
  - src/components/ui/option-card.tsx, src/hooks/use-disclosure.ts, src/lib/api-schemas.ts (compose)
  </read_first>
  <behavior>
  - Picker with an ambiguous payload renders the heading "This time happened twice", both option cards with server labels, and the footnote "We never pick for you — ambiguous times change the chart."; selecting emits the option's mode; with no selection a helper states a choice is required
  - Picker with a nonexistent payload renders "This time didn't exist" and exactly one shifted option card; selection emits mode shifted
  - Picker with classification normal renders nothing
  - Disclosure renders entries for exactly lemastra-calculation and google-geocoding-timezone sourced from provider-registry.json with content matching the registry fields verbatim, the "Got it — Calculate chart" CTA, and the "Read full privacy details" link; the component source contains no provider name/retention/purpose strings of its own (test asserts registry-content parity)
  </behavior>
  <action>
  Extend copy.ts with the exact picker + disclosure strings from the UI-SPEC copy deck. Build tricky-time-picker.tsx consuming the ResolveTimeResponse resolved block (classification + options) — option labels rendered verbatim from the server payload, radio semantics via option-card, required-choice gating exposed through a selectedMode prop callback. Build calculation-disclosure.tsx replicating the privacy.tsx registry-driven card pattern for the two locked ids (filter provider-registry.json by id), with the onAcknowledge callback wired to the CTA; zero own provider content. Tests per the behavior rows (RNTL /pure), including a parity assertion that every rendered provider string traces to the registry data object (mirror the Phase-1 disclosures-consistency approach inside the component test).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/tricky-time-picker.test.tsx src/__tests__/calculation-disclosure.test.tsx</automated>
  </verify>
  <done>Both component test files green; option labels provably server-sourced; disclosure provably registry-sourced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /birth/confirm screen + minimal /chart/result route + cast cleanup in birth.tsx</name>
  <files>src/app/birth/confirm.tsx, src/app/birth.tsx, src/app/chart/result.tsx, src/app/_layout.tsx, src/components/birth/copy.ts, src/__tests__/confirm-screen.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"/birth/confirm — Confirmation" (layout order: heading → resolved-place card → birth summary line → tricky-time picker → confidence summary → CTA → back link; first-run disclosure intercept; calculating state), §"/chart/result" guard rule (redirect to /birth without a payload)
  - 02-06-SUMMARY.md + src/app/birth.tsx (the draft param contract and birthFormSchema export; the scoped `as never` cast + TODO(02-08) marker on the confirm push — remove both in this task)
  - src/lib/api.ts (postCalculate), src/lib/api-schemas.ts (CalculateResponse)
  - src/app/_layout.tsx (current registration state), src/app/privacy.tsx (screen structure)
  - 02-RESEARCH.md §"Common Pitfalls" #7 (typed-routes regen before tsc)
  </read_first>
  <behavior>
  - Confirm renders the resolved card fields Birthplace/Coordinates/Time zone/UTC offset at birth (coords + zone in mono), the birth summary line, and the zone-source note (Google server-side vs entered manually); the drift note renders only when drift is true
  - With a non-normal classification, Calculate is disabled until a picker selection exists (helper "Choose a time above to continue")
  - First run: tapping Calculate shows the disclosure; "Got it — Calculate chart" persists the flag and fires the mutation; second run fires directly (mutation via useMutation, no auto-retry on non-network errors, in-flight disables CTA with "Calculating chart…" + ActivityIndicator)
  - Calculate request carries the chosen time_resolution mode (and second-pass offset) from the picker selection
  - Success navigates to /chart/result with the envelope; CALC-04 codes render ErrorBanner (unsuitable house system offers the "Open Assumptions" action that returns to /birth with the advanced control expanded)
  - Arriving at /chart/result without a payload redirects to /birth; with a payload it renders the Display title "Your natal chart", the identity line, and the validation-status line
  - With /birth/confirm registered and typed routes regenerated, the 02-06 scoped cast in birth.tsx is removed and the confirm pathname typechecks uncast — tsc --noEmit is the proof
  </behavior>
  <action>
  Create src/app/birth/confirm.tsx per the layout order: parse the draft param (form state + resolve response) with zod before render; compose the resolved card (field labels Label/600, values Body, coords/zone in the mono code style), TrickyTimePicker (visible only when classification is not normal, selection stored in state and included in the calculate request), confidence summary, CalculationDisclosure gated by useDisclosure().acknowledged, the "Calculate chart" useMutation calling postCalculate, calculating state, ErrorBanner on ApiError with code, and the "Edit birth details" back link (router.back with the draft intact — params-carried state). Create src/app/chart/result.tsx as the minimal-but-real screen: guard-redirect when the envelope param is absent, title, identity line (Unknown omits the time slot), and the "Validated — passed chart schema {schema_version}" status line — placements/provenance/unavailable sections are 02-09 scope, do not stub them with fake data. Register both routes in _layout.tsx. Then remove the scoped `as never` cast and its TODO(02-08) marker from the confirm push in src/app/birth.tsx — with the route registered and typed routes regenerated, the pathname must typecheck without the cast; if tsc fails after removal, the registration is wrong, not the types. Regenerate typed routes (dev-server boot) before tsc. Tests per the behavior rows with the api module mocked.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/confirm-screen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Confirm tests green; result guard + identity render tested; tsc green after regen with the 02-06 cast removed and the pathname typechecking uncast; the whole birth → confirm → calculate → result path is walkable against a running local API.</done>
</task>

<task type="auto">
  <name>Task 3: governed registry activation flip + lockstep test updates</name>
  <files>src/data/provider-registry.json, src/schemas/registry.test.ts</files>
  <read_first>
  - 02-RESEARCH.md §"Common Pitfalls" #9 (the flip is a governed act — registry requires inventory + retention to already match; research verified the §1 strings align) and docs/governance/data-inventory.md + docs/governance/retention-deletion-policy.md (confirm the entries still match before flipping)
  - 02-PATTERNS.md §D4 (exact flip locations: lines 7 and 28 of provider-registry.json; the lockstep test at registry.test.ts lines 50–55)
  - src/data/provider-registry.json, src/schemas/registry.test.ts, src/__tests__/privacy-screen.test.tsx, src/__tests__/disclosures-consistency.test.ts (all consumers of status)
  </read_first>
  <action>
  Flip status from planned to active for exactly the two entries lemastra-calculation (line ~7) and google-geocoding-timezone (line ~28) in src/data/provider-registry.json — no other edits to the file. Update the lockstep test in src/schemas/registry.test.ts: replace the "marks every provider as planned" assertion with one asserting exactly these two ids are active and the remaining four are still planned (Phase-2 truth: calculation + geocoding live, everything else untouched). Check privacy-screen.test.tsx: its banner test already guards the any-active case via early return per the pattern map — verify it passes unmodified; modify only if the assertion genuinely contradicts. Run the full client suite; the Phase-1 governance tests (disclosures-consistency, governance-docs) must stay green — if any fails, the flip is NOT legitimate: stop and reconcile the governance docs first per the registry schema rule rather than weakening the test.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Full client suite green with exactly two active providers; disclosures-consistency and governance-docs tests green unmodified (except the one documented lockstep assertion); D-04's disclosure content now truthfully says Active for the live flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| birth data → calculate mutation → local API | The D-04 disclosure gates the first outbound payload |
| registry data → disclosure UI | Governance truth flows one direction: registry → screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Information Disclosure | first calculation without informed consent | mitigate | D-04 intercept: useDisclosure gates the mutation behind the registry-driven notice; the flag key is versioned (@lemastra:disclosure.calculation.v1) so future notice changes can re-prompt |
| T-02-30 | Tampering | disclosure content drift | mitigate | component renders only registry fields — test-enforced parity (Phase-1 invariant extended); flipping status without matching inventory/retention fails the Phase-1 governance tests |
| T-02-31 | Tampering | tricky-time choice | mitigate | server-supplied options consumed verbatim; UI never re-derives offsets; server revalidates the chosen mode (02-03 schema) |
| T-02-32 | Abuse | calculate mutation retries | mitigate | user-initiated useMutation with no auto-retry on non-network errors (silent re-calculation is forbidden by the UI-SPEC loading/failure contract) |
</threat_model>

<verification>
- `npx vitest run && npx tsc --noEmit` — everything green
- `grep -c '"status": "active"' src/data/provider-registry.json` returns exactly 2
- End-to-end smoke with the local API running: birth → confirm shows offset → (DST case) picker blocks Calculate → disclosure on first run → result identity screen
</verification>

<success_criteria>
- BIRTH-02 client half complete (resolved card + offset + zone source + drift) and BIRTH-03 client half complete (explicit picker, never silent)
- D-04 one-time disclosure shipped registry-driven; governed activation flip done with lockstep tests
- The user story's happy path is now fully walkable end-to-end (entry → confirm → calculate → result)
</success_criteria>

## Artifacts this phase produces

*Plan 02-08 symbols later plans consume (exclude from drift verification):*
- `src/components/birth/tricky-time-picker.tsx`: TrickyTimePicker ({ resolved, value, onChange })
- `src/components/birth/calculation-disclosure.tsx`: CalculationDisclosure ({ onAcknowledge })
- `src/app/birth/confirm.tsx`: ConfirmScreen (draft param contract: form state + resolve response)
- `src/app/chart/result.tsx`: ResultScreen minimal (envelope param contract consumed further by 02-09)
- provider-registry.json: lemastra-calculation + google-geocoding-timezone status active
- copy.ts additions: confirm/picker/disclosure strings

<output>
Create `.planning/phases/02-trustworthy-natal-chart/02-08-SUMMARY.md` when done
</output>
