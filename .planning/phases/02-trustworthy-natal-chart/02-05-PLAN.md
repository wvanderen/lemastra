---
phase: 02-trustworthy-natal-chart
plan: 05
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/constants/theme.ts
  - src/components/themed-text.tsx
  - src/components/ui/option-card.tsx
  - src/components/ui/error-banner.tsx
  - src/components/ui/copy.ts
  - src/components/birth/confidence-control.tsx
  - src/components/birth/assumptions-control.tsx
  - src/components/birth/copy.ts
  - src/__tests__/option-card.test.tsx
  - src/__tests__/error-banner.test.tsx
  - src/__tests__/confidence-control.test.tsx
  - src/__tests__/assumptions-control.test.tsx
autonomous: true
requirements: [BIRTH-04, CALC-02, CALC-04]
must_haves:
  truths:
    - "The theme defines accent and error tokens for light and dark, and linkPrimary uses the accent token instead of a hard-coded hex"
    - "Option cards expose radio semantics with selected state conveyed by fill + accent border + 600-weight label — never color alone — with ≥48dp hit areas"
    - "The error banner renders the copy-deck heading/body/hint and recovery action for each CALC-04 error_code"
    - "The confidence control offers exactly Timed, Approximate, Rectified, Unknown with the copy-deck helper text and Timed selected by default"
    - "The assumptions control lists exactly the ten calculator house systems verbatim with Whole Sign pre-selected, collapsed under Assumptions & advanced"
  artifacts:
    - path: "src/components/ui/option-card.tsx"
      provides: "Shared selectable card with radio accessibility semantics"
      contains: "accessibilityRole"
    - path: "src/components/ui/error-banner.tsx"
      provides: "Per-error_code recoverable error banner (CALC-04 client rendering)"
      contains: "error_code"
    - path: "src/components/birth/confidence-control.tsx"
      provides: "D-09 four-state inline confidence control"
      contains: "radiogroup"
    - path: "src/components/birth/assumptions-control.tsx"
      provides: "D-11 collapsible house-system selector"
      contains: "Whole Sign"
  key_links:
    - from: "src/components/ui/error-banner.tsx"
      to: "src/lib/api-schemas.ts"
      via: "consumes the typed error_code enum from the API error contract"
      pattern: "errorCodeSchema"
    - from: "src/components/birth/assumptions-control.tsx"
      to: "src/lib/api-schemas.ts"
      via: "house-system vocabulary imported from the shared schema enum (never a local literal list)"
      pattern: "houseSystemSchema"
---

<objective>
Build the client's shared UI layer: the two new semantic color tokens (accent, error) with the linkPrimary migration, the reusable option-card and error-banner primitives, and the two birth-entry controls — the D-09 four-state confidence segmented control and the D-11 collapsible house-system selector — with all copy centralized in copy modules so tests assert exact strings.

Purpose: These primitives are the vocabulary every screen plan (02-06, 02-08, 02-09) composes; building them against the approved 02-UI-SPEC.md keeps later screens mechanical.
Output: Two new tokens, two ui primitives, two birth controls, two copy modules, four green test files.
</objective>

## Phase Goal (user story)

**As a** LemAstra user (curious beginner or serious enthusiast), **I want to** enter my birth details, confirm the resolved place and historical timezone, and explicitly resolve ambiguous or nonexistent civil times, **so that** I receive a validated, provenance-rich natal chart I can trust and inspect before any interpretation.

*(Derived from ROADMAP Phase 2 Goal + PROJECT.md audience definition.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-natal-chart/02-UI-SPEC.md
@.planning/phases/02-trustworthy-natal-chart/02-PATTERNS.md

- 02-UI-SPEC.md §"Color" (token values, accent/error reserved-use lists, linkPrimary migration), §"Copy Deck" birth-form section (exact strings), §"Interaction Contract" (confidence + house-system behavior), §"Accessibility Contract", §"Component Inventory"
- 02-PATTERNS.md §C (themed-text typed-props pattern, confidence union mapping, privacy.tsx card/StyleSheet conventions), §"Closed enums" shared pattern
- src/constants/theme.ts, src/components/themed-text.tsx (files being modified)
- src/lib/api-schemas.ts (house-system + error-code enums from 02-02)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: accent/error theme tokens + option-card + error-banner primitives</name>
  <files>src/constants/theme.ts, src/components/themed-text.tsx, src/components/ui/option-card.tsx, src/components/ui/error-banner.tsx, src/components/ui/copy.ts, src/__tests__/option-card.test.tsx, src/__tests__/error-banner.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"Color" (exact hex pairs, contrast basis, reserved-use lists, validation-status neutrality) and §"Error banners" table (per-code heading/body/action copy)
  - src/constants/theme.ts and src/components/themed-text.tsx (current token shape and the linkPrimary style to migrate)
  - src/components/themed-view.tsx, src/hooks/use-theme.ts
  - src/__tests__/privacy-screen.test.tsx (RNTL /pure acquisition skeleton — copy it verbatim as the test file skeleton)
  </read_first>
  <behavior>
  - theme.light.accent is #2266CC, theme.dark.accent is #7AB0FF; theme.light.error is #B3261E, theme.dark.error is #F2B8B5; themed-text linkPrimary color resolves to theme.accent in both schemes
  - Option card unselected: backgroundElement fill, 1px hairline border; selected: backgroundSelected fill + 2px accent border + 600-weight label; accessibilityRole radio with accessibilityState checked; press target ≥48dp (hitSlop when visual size is smaller)
  - Error banner for CALC_UNSUITABLE_HOUSE_SYSTEM renders heading naming the house system, the switch-to-Whole-Sign body, and an "Open Assumptions" action; for CALC_TIMEOUT renders "Try again" action; each banner exposes its text via accessibilityLabel so meaning never rides on color
  - Network/unreachable fallback copy renders when the error carries no recognized code
  </behavior>
  <action>
  Add the accent and error color tokens to both schemes in src/constants/theme.ts (single source of truth — no hex literals in components). Migrate themed-text.tsx linkPrimary from the hard-coded #3c87f7 to theme.accent. Create src/components/ui/option-card.tsx per the UI-SPEC selected-state rules (fill + marker/border + weight — three channels, never color alone) with typed props (label, helper, selected, onPress, testID). Create src/components/ui/copy.ts holding the exact error-banner strings from the UI-SPEC copy-deck table keyed by error_code, plus the fallback network copy. Create src/components/ui/error-banner.tsx rendering card + 1px error border + heading (Label/600) + body + hint + optional action callback, mapping every code from the shared errorCodeSchema vocabulary in src/lib/api-schemas.ts. Tests: copy the privacy-screen RNTL /pure skeleton; assert token values, selected/unselected a11y states, and banner rendering for at least CALC_UNSUITABLE_HOUSE_SYSTEM, CALC_TIMEOUT, PLACE_ZERO_RESULTS, and the network fallback.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/option-card.test.tsx src/__tests__/error-banner.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Tokens present in both schemes; linkPrimary uses the token (grep for the old hex in themed-text.tsx returns nothing); both primitive test files green; tsc green (no routes touched).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: confidence control (D-09) + assumptions control (D-11) with copy modules</name>
  <files>src/components/birth/confidence-control.tsx, src/components/birth/assumptions-control.tsx, src/components/birth/copy.ts, src/__tests__/confidence-control.test.tsx, src/__tests__/assumptions-control.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"Copy Deck" birth-form section — confidence options + helper text strings and the Assumptions & advanced strings (exact copy), §"Interaction Contract" (radio semantics, Unknown side effect note, collapsed disclosure)
  - 02-PATTERNS.md §C (confidence typed union mapping to capitalized calculator labels Timed|Approximate|Unknown|Rectified)
  - src/components/ui/option-card.tsx (Task 1 output — compose it)
  - src/lib/api-schemas.ts (houseSystemSchema + confidenceSchema vocabularies)
  </read_first>
  <behavior>
  - Confidence control renders the heading "How well do you know your birth time?" and exactly four options with copy-deck helper text; Timed is selected by default; selecting an option calls back with the calculator label (Timed/Approximate/Rectified/Unknown); the group exposes accessibilityRole radiogroup with per-option radio + checked state
  - Assumptions control renders collapsed "Assumptions & advanced"; expanding reveals the house-system radio list with exactly the ten calculator labels in the schema vocabulary order, Whole Sign pre-selected, and the quadrant-failure helper text; selection calls back with the label
  - Both controls render correctly at 1.3× font scale (wrapping, not clipping)
  </behavior>
  <action>
  Create src/components/birth/copy.ts first with the exact UI-SPEC strings (confidence heading, four option labels + helpers, unknown helper swap "Not needed when the time is unknown.", assumptions header, house-system label + helper). Build confidence-control.tsx as an inline segmented radio group composed of option-card rows (no modal, no separate screen per D-09), value/onChange props typed against the confidenceSchema vocabulary, helper text swapping when Unknown is selected. Build assumptions-control.tsx as a collapsed disclosure that expands in place, radio list driven by the houseSystemSchema enum options (imported — never a local literal array), defaulting to Whole Sign. Tests per the behavior rows using the RNTL /pure pattern, including a 1.3×-scale render smoke (props mocked) and callback-value assertions.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/confidence-control.test.tsx src/__tests__/assumptions-control.test.tsx</automated>
  </verify>
  <done>Both control tests green; helper copy asserted exactly; house-system list sourced from the schema enum (no duplicated literal list in the component).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API error payloads → banner rendering | Untrusted-by-default error text is rendered into UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Tampering | error-banner copy | mitigate | banner headings/bodies come from the local copy deck keyed by validated error_code; only the server's field-level CALC_INVALID_INPUT message is passed through (verified user-facing calculator copy), never raw stderr/tracebacks |
| T-02-19 | Spoofing | accessibility semantics | mitigate | selected/error states conveyed by structure (roles, checked states, text) in addition to color — contract-tested |
| T-02-20 | Elevation | none | accept | pure presentational components, no privileges or data boundaries crossed |
</threat_model>

<verification>
- `npx vitest run` — full suite green (new tests + all Phase-1 tests)
- `npx tsc --noEmit` green
</verification>

<success_criteria>
- BIRTH-04 control exists with the D-09 four states and helper text (client half; form wiring lands in 02-06)
- CALC-04 client rendering vocabulary (error banner) complete for every code
- CALC-02 assumptions control (D-11) complete with the exact ten-system vocabulary
- UI-SPEC color/typography/spacing contracts honored (tokens only, no raw values)
</success_criteria>

## Artifacts this phase produces

*Plan 02-05 symbols later plans consume (exclude from drift verification):*
- theme.ts: Colors.accent, Colors.error (light+dark)
- `src/components/ui/option-card.tsx`: OptionCard (label/helper/selected/onPress)
- `src/components/ui/error-banner.tsx`: ErrorBanner ({ code, message?, onAction? }) + `src/components/ui/copy.ts` error copy map
- `src/components/birth/confidence-control.tsx`: ConfidenceControl ({ value, onChange })
- `src/components/birth/assumptions-control.tsx`: AssumptionsControl ({ value, onChange, defaultExpanded? })
- `src/components/birth/copy.ts`: confidence + assumptions copy constants

<output>
Create `.planning/phases/02-trustworthy-natal-chart/02-05-SUMMARY.md` when done
</output>
