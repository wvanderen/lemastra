---
phase: 02-trustworthy-natal-chart
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/api.ts
  - src/lib/api-schemas.ts
  - src/lib/api-schemas.test.ts
  - src/lib/query-client.tsx
  - src/hooks/use-disclosure.ts
  - src/__tests__/use-disclosure.test.ts
autonomous: false
requirements: [BIRTH-01]
must_haves:
  truths:
    - "The four STACK.md client libraries are installed through npx expo install after a human approves the flagged packages"
    - "Zod schemas parse the documented resolve/calculate success envelopes and reject malformed variants (mutation matrix)"
    - "The fetch client resolves its base URL from EXPO_PUBLIC_API_URL with platform defaults (10.0.2.2:8000 on Android, localhost:8000 elsewhere)"
    - "useDisclosure persists the one-time calculation-disclosure flag under the AsyncStorage key @lemastra:disclosure.calculation.v1"
  artifacts:
    - path: "src/lib/api-schemas.ts"
      provides: "Zod response contracts for places/search, places/resolve-time, charts/calculate, meta/zones, and the error body"
      contains: "error_code"
    - path: "src/lib/api.ts"
      provides: "Typed fetch client with platform base-URL resolution and parse-then-trust"
      contains: "EXPO_PUBLIC_API_URL"
    - path: "src/lib/query-client.tsx"
      provides: "TanStack Query provider with focusManager↔AppState wiring"
      contains: "focusManager"
    - path: "src/hooks/use-disclosure.ts"
      provides: "AsyncStorage-backed one-time disclosure flag (D-04)"
      contains: "disclosure.calculation.v1"
  key_links:
    - from: "src/lib/api.ts"
      to: "src/lib/api-schemas.ts"
      via: "every response parsed by the matching zod schema before returning"
      pattern: "api-schemas"
    - from: "src/lib/query-client.tsx"
      to: "src/app/_layout.tsx"
      via: "provider wraps the app in a later plan"
      pattern: "QueryClientProvider"
---

<objective>
Install the STACK.md client libraries (with the mandatory package-legitimacy human checkpoint for the [SUS]-flagged trio) and build the client's data layer: zod response contracts mirroring the API design, a typed parse-then-trust fetch client with simulator base-URL defaults, the TanStack Query provider, and the AsyncStorage-backed one-time disclosure hook (D-04).

Purpose: Wave 0 for the client — every screen plan (02-05..02-09) imports these contracts and clients; defining them now from the verified RESEARCH.md §3 endpoint contract lets client and API work proceed in parallel without drift.
Output: Four installed deps, three lib/hook modules with green tests.
</objective>

## Phase Goal (user story)

**As a** LemAstra user (curious beginner or serious enthusiast), **I want to** enter my birth details, confirm the resolved place and historical timezone, and explicitly resolve ambiguous or nonexistent civil times, **so that** I receive a validated, provenance-rich natal chart I can trust and inspect before any interpretation.

*(Derived from ROADMAP Phase 2 Goal + PROJECT.md audience definition.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-natal-chart/02-RESEARCH.md
@.planning/phases/02-trustworthy-natal-chart/02-PATTERNS.md

- 02-RESEARCH.md §"FastAPI Service Skeleton" (endpoint table + provenance block — the contract these schemas mirror), §"Expo Client Screens" (lib wiring), §"Package Legitimacy Audit"
- 02-PATTERNS.md §D1 (api-schemas.ts copies provider-registry.ts conventions: .describe() on every field, closed enums, z.infer type exports), §D2 (use-disclosure mirrors use-theme.ts shape), §D3 (api.ts/query-client greenfield guidance)
- src/schemas/provider-registry.ts + src/schemas/registry.test.ts (the exact conventions to replicate)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy approval + install client libraries</name>
  <files>package.json, package-lock.json</files>
  <what-built>Nothing yet — this gate approves the four npm packages and then installs them (three carry [SUS] too-new false-positive verdicts in the research audit; approval cannot be auto-advanced).</what-built>
  <how-to-verify>
    Review the research audit table (02-RESEARCH.md §"Package Legitimacy Audit") and spot-check on npmjs.com:
    1. https://www.npmjs.com/package/react-hook-form — canonical repo github.com/react-hook-form/react-hook-form, ~60M weekly downloads
    2. https://www.npmjs.com/package/@hookform/resolvers — canonical RHF resolvers package, ~51M weekly downloads
    3. https://www.npmjs.com/package/@tanstack/react-query — canonical TanStack query package, ~66M weekly downloads
    4. https://www.npmjs.com/package/@react-native-async-storage/async-storage — verdict OK, ~7M weekly downloads
    Confirm publisher/repo alignment and that none ship postinstall scripts (verified in research: npm view shows null).
  </how-to-verify>
  <action>
  After the human approves, run `npx expo install react-hook-form @hookform/resolvers @tanstack/react-query @react-native-async-storage/async-storage` (per the STACK.md form-library and query-wiring delegation). Do NOT hand-pin versions — expo install selects versions compatible with the existing ~57 tilde pins (Phase 01 T-01-SC); whatever it writes is authoritative. Then run `npx vitest run` to confirm the install broke nothing.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');for (const d of ['react-hook-form','@hookform/resolvers','@tanstack/react-query','@react-native-async-storage/async-storage']){if(!p.dependencies[d])throw new Error('missing '+d)};console.log('all four deps present')" && npx vitest run</automated>
  </verify>
  <done>All four packages present in dependencies with expo-selected versions after explicit human approval; existing expo pins untouched; vitest suite still green.</done>
  <resume-signal>Type "approved" to proceed with installation, or name a package to reject</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zod response contracts + typed fetch client (parse-then-trust)</name>
  <files>src/lib/api-schemas.ts, src/lib/api.ts, src/lib/api-schemas.test.ts</files>
  <read_first>
  - 02-RESEARCH.md §"FastAPI Service Skeleton" endpoint table (exact response shapes: candidates+provenance; iana_zone/zone_source/google/resolved{offset_seconds, offset_label, classification, options}/drift; chart envelope + provenance block + unavailable_factors + provisional_factors; error body error.code/message/recoverable/hint)
  - 02-RESEARCH.md §"Resolve payload for the D-08 picker" (option modes first_pass/second_pass/shifted)
  - 02-PATTERNS.md §A1 HOUSE_SYSTEMS verbatim list and §D1 conventions; 02-UI-SPEC.md §"Error banners" (full error_code vocabulary)
  - src/schemas/provider-registry.ts (closed-enum + .describe + z.infer export style)
  - src/schemas/registry.test.ts (parse-valid + mutation-reject test pattern)
  - .env.example (EXPO_PUBLIC_API_URL documented as plain-text inlined, non-secret)
  </read_first>
  <behavior>
  - placeSearchResponseSchema parses a payload with candidates (label, lat, lon, location_type, optional place_id) and provenance (provider id google-geocoding-timezone, lookup_timestamp)
  - resolveTimeResponseSchema parses normal (classification normal, empty options), ambiguous (two options with distinct mode and utc), and nonexistent (single shifted option) payloads; drift boolean present
  - calculateResponseSchema parses a full envelope: chart_data with placements array (body, sign, degree, absolute_degree, motion, optional house/dignity/notes), optional aspects/ascendant/midheaven/house_cusps/sect/lots, birth_time_confidence, plus provenance (skill_revision, swisseph_version, tzdata_version, schema_version, ephemeris_mode, house_system, zodiac_mode, orb_policy, input_revision, calculator_cmd) and optional unavailable_factors/provisional_factors arrays
  - errorSchema parses error.code from the closed enum with all 11 codes; mutating any enum field or removing required fields makes parse throw
  - apiClient base URL: EXPO_PUBLIC_API_URL when set; otherwise http://10.0.2.2:8000 on android, http://localhost:8000 otherwise
  </behavior>
  <action>
  Create `src/lib/api-schemas.ts` following provider-registry.ts conventions: `.describe()` on every field; closed enums via z.enum — house systems as the exact ten labels from the calculator (Whole Sign, Placidus, Regiomontanus, Koch, Equal, Campanus, Porphyrius, Morinus, Alcabitius, Topocentric); confidence as Timed, Approximate, Unknown, Rectified; classification as normal, ambiguous, nonexistent; time-resolution mode as first_pass, second_pass, shifted; error_code as the eleven CALC-04 codes (PLACE_ZERO_RESULTS, PLACE_PROVIDER_UNAVAILABLE, PLACE_INVALID_QUERY, TIMEZONE_NO_RESULTS, TIMEZONE_PROVIDER_UNAVAILABLE, TIMEZONE_INVALID_ZONE, CALC_INVALID_INPUT, CALC_ENGINE_ERROR, CALC_TIMEOUT, CALC_VALIDATION_FAILED, CALC_UNSUITABLE_HOUSE_SYSTEM). Export z.infer types for each response. `src/lib/api.ts`: resolve base URL as EXPO_PUBLIC_API_URL ?? platform default via react-native Platform.OS (android → 10.0.2.2 loopback alias); export typed functions postPlaceSearch, postResolveTime, postCalculate, fetchZones that call fetch and parse every response through the matching schema BEFORE returning (parse-then-trust); non-2xx responses parse through errorSchema and throw a typed ApiError carrying code/message/hint.
  `src/lib/api-schemas.test.ts`: copy the registry.test.ts discipline — parse each documented success envelope (one fixture object per endpoint, including a DST-ambiguous resolve payload and an unknown-time calculate payload lacking house keys), then a mutation table proving malformed variants throw (wrong enum value, missing provenance field, candidate without coordinates, option without mode). No RNTL needed — plain vitest imports.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api-schemas.test.ts</automated>
  </verify>
  <done>Schema tests green (parse + reject matrix); every endpoint from the D-03 two-step contract plus meta/zones is covered; house-system and error-code vocabularies match the calculator/errors.py exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Query provider + one-time disclosure hook</name>
  <files>src/lib/query-client.tsx, src/hooks/use-disclosure.ts, src/__tests__/use-disclosure.test.ts</files>
  <read_first>
  - 02-RESEARCH.md §"Library wiring" (focusManager↔AppState, AsyncStorage key, mutation-not-query for calculate)
  - 02-PATTERNS.md §D2 (hook shape mirrors use-theme.ts), §D3 (provider wraps app following _layout.tsx ThemeProvider shape)
  - src/hooks/use-theme.ts, src/app/_layout.tsx (provider-wrap pattern)
  - src/__tests__/privacy-screen.test.tsx lines 1–31 (RNTL /pure acquisition pattern for component tests)
  </read_first>
  <behavior>
  - useDisclosure starts with acknowledged false on a fresh store, and acknowledge() persists true so a second mount reports acknowledged true
  - The persisted key is exactly @lemastra:disclosure.calculation.v1
  - QueryClientProvider renders children and focusManager is wired to AppState
  </behavior>
  <action>
  `src/lib/query-client.tsx`: export a QueryClientProvider wrapper component (following the _layout.tsx provider-wrap shape) that creates the QueryClient once, subscribes focusManager to AppState visibility per TanStack Query React Native guidance, and will be mounted in _layout.tsx by plan 02-06. `src/hooks/use-disclosure.ts`: `useDisclosure()` returning `{ acknowledged, acknowledge }` backed by AsyncStorage under key `@lemastra:disclosure.calculation.v1` (D-04 one-time notice flag; safe-persist with a no-op catch so a unavailable storage never blocks calculation). Test in `src/__tests__/use-disclosure.test.ts` using the RNTL /pure renderHook acquisition pattern with AsyncStorage mocked (jest-preset-parity mock style used by the existing RN shim); assert the behavior block above.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-disclosure.test.ts</automated>
  </verify>
  <done>Hook test green; provider module typechecks; full `npx vitest run` still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party code enters the client build |
| API service → client | Untrusted-by-default HTTP responses consumed by render code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (react-hook-form, @hookform/resolvers, @tanstack/react-query, async-storage) | mitigate | slopcheck evidence recorded; blocking human checkpoint BEFORE install (Task 1); versions written by npx expo install, never hand-pinned |
| T-02-06 | Tampering | src/lib/api.ts response consumption | mitigate | zod parse of every response before render (parse-then-trust); malformed data never reaches components |
| T-02-07 | Information Disclosure | EXPO_PUBLIC_API_URL | accept | documented plain-text-inlined, non-secret by construction (root .env.example convention); contains only a local dev URL |
| T-02-08 | Spoofing | base-URL default | accept | local-dev-only phase (D-02); defaults point at loopback/emuator aliases; no credentials transit |
</threat_model>

<verification>
- `npx vitest run` — full suite green including new schema + hook tests
- `npx tsc --noEmit` green (no route changes in this plan, so no typed-routes regen needed)
- All four deps present via node require check (Task 1 automated gate)
</verification>

<success_criteria>
- Client Wave 0 items from 02-VALIDATION.md done: api-schemas.ts + tests, query provider, disclosure flag hook
- [SUS] package checkpoint satisfied and recorded
- Screen plans (02-05+) can import typed clients/contracts without touching package.json
</success_criteria>

## Artifacts this phase produces

*Plan 02-02 symbols later plans consume (exclude from drift verification):*
- `src/lib/api-schemas.ts`: schemas placeSearchResponseSchema, resolveTimeResponseSchema, calculateResponseSchema, zonesResponseSchema, errorSchema + z.infer types (PlaceSearchResponse, ResolveTimeResponse, CalculateResponse, ApiErrorBody) + enums houseSystemSchema, confidenceSchema, classificationSchema, timeResolutionModeSchema, errorCodeSchema
- `src/lib/api.ts`: apiClient base-URL resolver + postPlaceSearch, postResolveTime, postCalculate, fetchZones + class ApiError
- `src/lib/query-client.tsx`: QueryProvider component
- `src/hooks/use-disclosure.ts`: useDisclosure() → { acknowledged, acknowledge }
- AsyncStorage key constant `@lemastra:disclosure.calculation.v1`

<output>
Create `.planning/phases/02-trustworthy-natal-chart/02-02-SUMMARY.md` when done
</output>
