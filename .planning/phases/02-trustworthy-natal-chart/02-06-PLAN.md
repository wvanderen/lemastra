---
phase: 02-trustworthy-natal-chart
plan: 06
type: execute
wave: 3
depends_on: ["02-05"]
files_modified:
  - src/components/birth/place-search.tsx
  - src/components/birth/copy.ts
  - src/app/birth.tsx
  - src/app/index.tsx
  - src/app/_layout.tsx
  - src/__tests__/place-search.test.tsx
  - src/__tests__/birth-form.test.tsx
autonomous: true
requirements: [BIRTH-01, BIRTH-04]
must_haves:
  truths:
    - "The user can type at least 3 characters and see up to 5 debounced place candidates as cards; selecting one fills a resolved card with coordinates and a precision note when the match is approximate"
    - "Zero-results and unavailable search states render inline with an Enter coordinates manually action, and the manual branch validates latitude −90..90, longitude −180..180, and a required IANA zone from /api/v1/meta/zones"
    - "Selecting Unknown confidence disables and clears the birth-time field and swaps its helper text"
    - "A fully valid form triggers the resolve-time call and navigates to /birth/confirm carrying the JSON-stringified draft; resolve failures render the error banner on /birth"
    - "The home screen offers Calculate your first chart navigation to /birth"
  artifacts:
    - path: "src/app/birth.tsx"
      provides: "BIRTH-01/BIRTH-04 birth entry form (RHF + zod, discriminated place union)"
      contains: "react-hook-form"
    - path: "src/components/birth/place-search.tsx"
      provides: "D-05 debounced type-ahead + manual fallback branch"
      contains: "resolve-time"
    - path: "src/app/index.tsx"
      provides: "Home CTA into the birth flow"
      contains: "/birth"
  key_links:
    - from: "src/components/birth/place-search.tsx"
      to: "src/lib/api.ts"
      via: "debounced postPlaceSearch + fetchZones calls parsed through api-schemas"
      pattern: "postPlaceSearch"
    - from: "src/app/birth.tsx"
      to: "src/app/birth/confirm.tsx (created in 02-08)"
      via: "router.push with pathname /birth/confirm and params draft JSON — target route registration happens in 02-08; this plan registers only the birth route"
      pattern: "birth/confirm"
---

<objective>
Deliver the first walkable user slice: home CTA → birth entry form. The form composes the 02-05 controls with a debounced place type-ahead (D-05) including the always-available manual fallback, React Hook Form + zod validation with the discriminated place union and the unknown-time interdependency, and the resolve-then-navigate transition that hands the draft to the confirm screen (to be built in 02-08).

Purpose: BIRTH-01 and the client half of BIRTH-04 — after this plan a user can enter complete, validated birth data and reach the confirmation step's doorstep.
Output: /birth route wired into navigation, place-search component, home CTA, green form tests.
</objective>

## Phase Goal (user story)

**As a** LemAstra user (curious beginner or serious enthusiast), **I want to** enter my birth details, confirm the resolved place and historical timezone, and explicitly resolve ambiguous or nonexistent civil times, **so that** I receive a validated, provenance-rich natal chart I can trust and inspect before any interpretation.

*(Derived from ROADMAP Phase 2 Goal + PROJECT.md audience definition.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-natal-chart/02-UI-SPEC.md
@.planning/phases/02-trustworthy-natal-chart/02-PATTERNS.md
@.planning/phases/02-trustworthy-natal-chart/02-02-SUMMARY.md
@.planning/phases/02-trustworthy-natal-chart/02-05-SUMMARY.md

- 02-UI-SPEC.md §"Screen & Flow Contracts" (/birth layout order 1–7 and the /birth → /birth/confirm transition), §"Interaction Contract" (type-ahead debounce spec, manual branch), §"Copy Deck" birth-form strings
- 02-PATTERNS.md §B (privacy.tsx screen structure analog; _layout.tsx registration; typed-routes landmine), §F (RNTL /pure test pattern)
- src/app/privacy.tsx, src/app/_layout.tsx, src/app/index.tsx (files being modified/extended)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: place-search component — debounced type-ahead with manual fallback</name>
  <files>src/components/birth/place-search.tsx, src/components/birth/copy.ts, src/__tests__/place-search.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"Interaction Contract" type-ahead paragraph (debounce ≥300 ms, ≥3 chars, max 5 candidate cards, keyboard dismiss on selection, manual reachable from all states) and §"Copy Deck" place strings (empty state, zero results, unavailable, selected-candidate precision note, manual toggle labels, manual field errors)
  - 02-RESEARCH.md §"Manual fallback (D-05)" (tz picker sourced from /api/v1/meta/zones)
  - src/lib/api.ts + src/lib/api-schemas.ts (postPlaceSearch, fetchZones, typed candidates)
  - src/components/ui/error-banner.tsx, src/components/ui/option-card.tsx (compose)
  - src/__tests__/privacy-screen.test.tsx (test skeleton)
  </read_first>
  <behavior>
  - Typing "bro" (3 chars) triggers exactly one search call after the debounce; "br" (2 chars) triggers none
  - Candidates render as cards capped at 5 with label and coordinates; selecting one emits the selected candidate object and shows the coords line plus the approximate-match note when location_type is APPROXIMATE or partial_match is set
  - Zero-results and provider-unavailable states render the copy-deck headings/hints plus an "Enter coordinates manually" action; the persistent toggle swaps between search and manual branches without losing the other branch's entered state within the component lifetime
  - Manual branch: latitude 91 → "Enter a number between −90 and 90."; longitude 200 → the −180..180 error; zone picker populated from fetchZones with a searchable list; a complete manual entry emits a manual place object (label, lat, lon, iana_zone, zone_source manual)
  </behavior>
  <action>
  Extend src/components/birth/copy.ts with the exact place-related copy-deck strings. Build place-search.tsx: a controlled component emitting either a selected Google candidate or a complete manual entry via a single onChange prop typed as the discriminated place union; debounced querying via TanStack Query (debounce ≥300 ms with a ≥3-character guard — use a useDeferred timer state, not a loop); candidate cards using option-card styling conventions (secondary surface, radius 8, ≥48dp); inline zero/unavailable states from the error-banner vocabulary; manual branch with zod-validated numeric fields and a zone picker backed by fetchZones (mocked in tests via the api module). Tests per the behavior rows with the api module mocked (RNTL /pure pattern).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/place-search.test.tsx</automated>
  </verify>
  <done>Debounce, cap, states, and manual validation all asserted; component emits the discriminated union the birth form's zod schema expects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /birth route (RHF + zod form) + home CTA + route registration</name>
  <files>src/app/birth.tsx, src/app/index.tsx, src/app/_layout.tsx, src/__tests__/birth-form.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"/birth — Birth entry form" (layout order, field errors inline, zod-before-navigation, server revalidation note), §"Copy Deck" birth-form + home strings, §"Navigation/state" (draft travels as router params)
  - 02-RESEARCH.md §"Library wiring" (RHF+zodResolver; unknown disables time; discriminated place union) and §"Common Pitfalls" #7 (typed-routes regeneration — boot the dev server once after adding routes BEFORE tsc)
  - src/app/privacy.tsx (screen structure: ScrollView + StyleSheet + a11y roles), src/app/index.tsx, src/app/_layout.tsx (files being modified)
  - src/components/birth/confidence-control.tsx, assumptions-control.tsx, place-search.tsx (compose)
  - src/lib/api.ts (postResolveTime), src/lib/query-client.tsx (mount the provider in _layout now that a screen consumes queries)
  </read_first>
  <behavior>
  - Empty/invalid date or time submits show the copy-deck inline errors and do not navigate
  - Selecting Unknown disables and clears the time field; helper becomes "Not needed when the time is unknown."; Approximate/Rectified/Timed require a time
  - Review birth details with valid data calls postResolveTime and, on success, router.push to /birth/confirm with params draft = JSON.stringify of the form state plus the resolve response; on failure the error banner renders in place (PLACE_* codes offer the manual action)
  - Home renders heading "LemAstra", sub-line "Accurate charts. Transparent evidence.", CTA "Calculate your first chart" linking to /birth, and the privacy link
  - QueryProvider from 02-02 wraps the app in _layout
  </behavior>
  <action>
  Create src/app/birth.tsx following the privacy.tsx screen structure: react-hook-form with zodResolver over a schema where place is a discriminated union (google candidate vs manual entry) and time is conditionally required by confidence; fields for date (YYYY-MM-DD) and time (HH:MM 24-hour) as formatted text inputs per A-UI-6; compose ConfidenceControl, AssumptionsControl, PlaceSearch; primary CTA "Review birth details" runs client validation then postResolveTime (using the selected place + date/time, tz_override only on the manual branch) and navigates on success with the draft param; resolve failures render ErrorBanner with the code from the thrown ApiError. Edit src/app/index.tsx to the home contract above. Edit src/app/_layout.tsx: wrap children in the QueryProvider and add the Stack.Screen entry for the birth route only (confirm/result register in 02-08/02-09 — they do not exist yet, so do NOT reference them). After routes exist, regenerate typed routes by booting the dev server once (npx expo start, then stop) before running tsc — the Phase-1 CI ordering note. birth-form tests: mock the api module; assert the behavior rows including the unknown-time disable/clear and the no-navigation-on-invalid cases (RNTL /pure).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/birth-form.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Form tests green; tsc green after typed-route regeneration; navigating the running app: home CTA → /birth → valid submit reaches the resolve call (confirm screen 404s until 02-08 — acceptable mid-phase, tests carry the contract).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → form state → API request | Free-text birth data validated client-side before any network call |
| resolve response → navigation params | Server data serialized into router params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Tampering | birth form input | mitigate | zod schema gates every field (date/time patterns, lat/lon bounds, discriminated union) before navigation; server revalidates via pydantic + calculator (CALC_INVALID_INPUT path) |
| T-02-22 | DoS | place type-ahead | mitigate | ≥300 ms debounce + ≥3-char guard + 5-candidate cap — enforced by component tests |
| T-02-23 | Information Disclosure | router params draft | accept | birth draft stays in-memory navigation state this phase; no persistence (Phase 3 will own local-only storage per privacy posture) |
| T-02-24 | Tampering | resolve response → params | mitigate | postResolveTime parses through zod before the draft is built (parse-then-trust from 02-02) |
</threat_model>

<verification>
- `npx vitest run` — full suite green
- `npx tsc --noEmit` green (after dev-server typed-routes regen)
- Manual smoke (optional, local): `npx expo start` → home CTA opens /birth; typing a place with the API running returns candidates
</verification>

<success_criteria>
- BIRTH-01 fully walkable (entry + validation + resolve hand-off); BIRTH-04 client behavior complete (unknown disables time)
- D-05 type-ahead + manual fallback delivered with honest error states
- No wheel, no persistence, no interpretation — phase boundary respected
</success_criteria>

## Artifacts this phase produces

*Plan 02-06 symbols later plans consume (exclude from drift verification):*
- `src/components/birth/place-search.tsx`: PlaceSearch ({ value, onChange }) with discriminated place-union output
- `src/app/birth.tsx`: BirthForm screen + birthFormSchema (zod) exported for confirm-screen draft parsing
- `src/app/index.tsx`: home CTA contract
- _layout.tsx: QueryProvider wrapping + birth Stack.Screen
- copy.ts additions: place + home copy constants

<output>
Create `.planning/phases/02-trustworthy-natal-chart/02-06-SUMMARY.md` when done
</output>
