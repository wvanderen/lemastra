---
phase: 02-trustworthy-natal-chart
plan: 09
type: execute
wave: 5
depends_on: ["02-08"]
files_modified:
  - src/components/chart/placement-list.tsx
  - src/components/chart/assumptions-line.tsx
  - src/components/chart/provenance-details.tsx
  - src/components/chart/unavailable-factors.tsx
  - src/components/chart/copy.ts
  - src/app/chart/result.tsx
  - src/__tests__/placement-list.test.tsx
  - src/__tests__/provenance-details.test.tsx
  - src/__tests__/unavailable-factors.test.tsx
  - src/__tests__/result-screen.test.tsx
autonomous: true
requirements: [CALC-02, CALC-03, BIRTH-05]
must_haves:
  truths:
    - "Every placement renders as a row with body, sign, degree in D°MM′ format, house where present, capitalized motion, and dignity only where present — each row exposing the copy-deck accessibility sentence"
    - "The compact assumptions card shows house system, zodiac mode, ephemeris mode, and orb policy, with the Adjust & recalculate action returning to /birth with the advanced control open"
    - "Calculation details is collapsed by default and expands to mono key–value rows for skill revision, Swiss Ephemeris version, timezone database, schema, input revision, place resolution, and calculator command"
    - "Unknown mode renders the unavailable-factors section (one reason-bearing card per entry) and the provisional moon card; approximate mode adds the provisional angles/houses caveat to the assumptions card"
    - "No wheel, preview graphic, or interpretation appears anywhere on the result screen (D-13); the validation status renders as neutral text"
  artifacts:
    - path: "src/components/chart/placement-list.tsx"
      provides: "D-13 structured placement rows with per-row a11y sentences"
      contains: "accessibilityLabel"
    - path: "src/components/chart/assumptions-line.tsx"
      provides: "D-12 compact assumptions card"
      contains: "Assumptions"
    - path: "src/components/chart/provenance-details.tsx"
      provides: "D-12/CALC-03 expandable provenance section"
      contains: "input_revision"
    - path: "src/components/chart/unavailable-factors.tsx"
      provides: "D-10 unavailable + provisional factor cards"
      contains: "unavailable"
  key_links:
    - from: "src/app/chart/result.tsx"
      to: "src/lib/api-schemas.ts"
      via: "envelope param parsed with calculateResponseSchema before render (parse-then-trust)"
      pattern: "calculateResponseSchema"
    - from: "src/components/chart/assumptions-line.tsx"
      to: "src/app/birth.tsx"
      via: "Adjust & recalculate navigates back to /birth with the advanced control expanded (params flag)"
      pattern: "/birth"
---

<objective>
Complete the phase's user-visible trust surface: deepen the minimal result screen into the full D-13 structured view — placement list with degree formatting and accessibility sentences, the D-12 compact assumptions card plus expandable Calculation details carrying the complete CALC-03 provenance chain, and the D-10 unavailable/provisional factor cards for unknown and approximate times. Strictly no wheel and no interpretation.

Purpose: ROADMAP success criterion 3 (validated chart with visible assumptions and complete provenance) — the screen a user inspects before Phase 4 ever draws a wheel.
Output: Four chart components + full result screen + four green test files; the phase's last vertical slice.
</objective>

## Phase Goal (user story)

**As a** LemAstra user (curious beginner or serious enthusiast), **I want to** enter my birth details, confirm the resolved place and historical timezone, and explicitly resolve ambiguous or nonexistent civil times, **so that** I receive a validated, provenance-rich natal chart I can trust and inspect before any interpretation.

*(Derived from ROADMAP Phase 2 Goal + PROJECT.md audience definition.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-natal-chart/02-UI-SPEC.md
@.planning/phases/02-trustworthy-natal-chart/02-PATTERNS.md
@.planning/phases/02-trustworthy-natal-chart/02-08-SUMMARY.md

- 02-UI-SPEC.md §"/chart/result — Result" (layout order, degree format D°MM′ with Spacing.half micro-gaps, dignity-only-where-present, row a11y sentence format, provisional/approximate caveats), §"Trust-Boundary Display Rules" (unavailable ≠ missing; no invented values), §"Copy Deck" result strings
- 02-RESEARCH.md §"Output envelope" (placement fields incl. optional notes) and §"Unknown Birth Time" (provisional moon note; Approximate caveat)
- 02-PATTERNS.md §B (privacy.tsx list-render + card patterns, fieldLabel/fieldValue style pair), §C (themed-text types)
- src/app/chart/result.tsx (file being deepened), src/app/privacy.tsx (list/card analog)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: placement list + assumptions card with copy module</name>
  <files>src/components/chart/placement-list.tsx, src/components/chart/assumptions-line.tsx, src/components/chart/copy.ts, src/__tests__/placement-list.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"Copy Deck" result-screen strings (Placements heading, row layout Body/600 — sign degree — House n — Motion — Dignity, degree format D°MM′ e.g. Gemini 0°26′, motion capitalized, dignity examples, a11y sentence template incl. the spoken degrees form and the +dignities suffix), §"Typography" (the code/mono style for data), §"Spacing" (Spacing.half for degree glyphs)
  - src/app/privacy.tsx (role=list/listitem render pattern + card styles)
  - src/lib/api-schemas.ts (placement/calculateResponse types)
  - src/components/themed-text.tsx (type union incl. code)
  </read_first>
  <behavior>
  - A placement with degree 0.4375 renders "0°26′" (minutes rounded); 24.5496 renders "24°33′"
  - Row for Sun in Gemini, house 10, direct, no dignities renders body, "Gemini 0°26′", "House 10", "Direct" and NO dignity column or dash placeholder
  - accessibilityLabel matches the copy-deck sentence "{body} in {sign}, {degree spoken as X degrees Y minutes}, {house phrase}, {motion} motion" with the dignities suffix when present
  - Unknown-mode placements (no house key) render without a house slot and without crashing
  - Assumptions card renders "Whole Sign houses · tropical zodiac · Moshier (built-in) ephemeris · {orb_policy}" and the "Adjust & recalculate" action; when confidence is Approximate it appends the provisional angles/houses caveat string
  </behavior>
  <action>
  Create src/components/chart/copy.ts with the exact result-screen strings (placements heading, house phrase, motion labels, dignity rendering rule note, assumptions label/caveat, validation status template). Implement placement-list.tsx: map over placements rendering rows per the UI-SPEC column layout, degree formatting via an exported formatDegreeMinutes(degree) → D°MM′ helper (pure, unit-testable), dignity rendered only when the array is non-empty, motion capitalized (Direct/Retrograde/Stationary), rows wrapped in role=listitem with the sentence accessibilityLabel, containers role=list. Implement assumptions-line.tsx: compact card consuming the provenance block fields (house_system, zodiac_mode, ephemeris_mode, orb_policy) + confidence, with the adjust action invoking a navigation callback. Tests: formatDegreeMinutes table (incl. rounding edge 0.4375 → 26′ and 24.5496 → 33′), row rendering with and without house/dignity, a11y sentence assertions, assumptions card incl. the Approximate caveat (RNTL /pure).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/placement-list.test.tsx</automated>
  </verify>
  <done>Degree formatting table green; rows honor present-only fields; a11y sentences match the copy deck; assumptions card honors the approximate caveat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: provenance details + unavailable factors + full result screen</name>
  <files>src/components/chart/provenance-details.tsx, src/components/chart/unavailable-factors.tsx, src/app/chart/result.tsx, src/__tests__/provenance-details.test.tsx, src/__tests__/unavailable-factors.test.tsx, src/__tests__/result-screen.test.tsx</files>
  <read_first>
  - 02-UI-SPEC.md §"/chart/result" (expandable Calculation details field list: Skill revision, Swiss Ephemeris version, Timezone database, Schema, Input revision, Place resolution, Calculator command — Data-mono rows; unavailable section heading "Not available without a birth time" + card format "{Factor} — {reason}"; provisional cards; validation status as neutral textSecondary), §"Trust-Boundary Display Rules"
  - 02-RESEARCH.md §"Provenance block (CALC-03)" (exact field names) and §"Unknown Birth Time" (unavailable/provisional payload examples with reasons)
  - src/app/chart/result.tsx (minimal screen from 02-08 — deepen it)
  - src/app/privacy.tsx (fieldLabel/fieldValue card style pair for the key–value rows)
  </read_first>
  <behavior>
  - Calculation details is collapsed by default; expanding reveals all seven mono key–value rows including skill_revision, input_revision, and place resolution (zone source + provider)
  - Unavailable-factors section renders only for Unknown mode, one card per entry with factor + reason (Houses — requires a birth time, etc.), heading "Not available without a birth time"
  - Provisional factors render as labeled cards (moon ~13°/day reason); Approximate mode surfaces the angles/houses provisional entry
  - Full screen order with a Timed envelope: title → identity → Placements → assumptions card → Calculation details → validation status; with an Unknown envelope the identity line omits the time and the unavailable section appears after status
  - The screen renders zero interpretation strings and no wheel/graphic; validation status is neutral text "Validated — passed chart schema {schema_version}"
  </behavior>
  <action>
  Implement provenance-details.tsx as a collapsed-by-default disclosure of mono key–value rows built from the provenance block (labels from chart/copy.ts; place-resolution row composed from zone_source + provider fields). Implement unavailable-factors.tsx rendering the unavailable section and provisional cards from the envelope arrays — never blank rows or placeholder dashes (D-10). Deepen src/app/chart/result.tsx: parse the envelope param with calculateResponseSchema (parse-then-trust — reject-and-redirect on failure like the empty-payload guard), then compose PlacementList, AssumptionsLine, ProvenanceDetails, UnavailableFactors in the UI-SPEC order, keeping the existing title/identity/validation status and the redirect guard from 02-08. Tests per the behavior rows for both a Timed and an Unknown envelope fixture (RNTL /pure), including a negative assertion that no placeholder-dash row exists for absent houses and that collapsed details start hidden.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>All new tests green; full client suite green; tsc green; the phase's complete happy path (entry → confirm → result with full D-12/D-13/D-10 surface) renders against a running local API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calculate envelope → result render | Server output parsed and rendered as calculated facts only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-33 | Tampering | envelope → render | mitigate | calculateResponseSchema parse before render; malformed envelope redirects instead of partially rendering |
| T-02-34 | Tampering (trust boundary) | fact/interpretation separation | mitigate | result screen renders only calculated facts + assumptions + provenance; no interpretation strings exist in chart/copy.ts (project trust constraint); D-10 forbids invented values — unavailable factors render as labeled cards |
| T-02-35 | Information Disclosure | provenance display | accept | version/revision identifiers are non-secret by construction (GATE-06 posture: no secrets in provenance fields — verified field list contains only versions, hashes, and provider names) |
| T-02-36 | Spoofing | a11y surface | mitigate | every placement row and factor card carries a text sentence label — screen-reader users get the same facts without the visual layout |
</threat_model>

<verification>
- `npx vitest run && npx tsc --noEmit` — full client verification green
- `cd api && uv run pytest -q` — API suite still green (no API changes; sanity)
- Manual UAT candidate list (for /gsd-verify-work): full flow with a known time, an unknown time, and a DST-ambiguous time against the local service
</verification>

<success_criteria>
- CALC-02 client display complete (assumptions always visible, details one expansion away)
- CALC-03 client display complete (full version chain visible in mono rows)
- BIRTH-05 client display complete (explicit unavailable/provisional cards, no invented values, no wheel per D-13)
- Phase 2 vertical slices complete end-to-end: all ten requirement IDs verifiable
</success_criteria>

## Artifacts this phase produces

*Plan 02-09 symbols later plans consume (exclude from drift verification):*
- `src/components/chart/placement-list.tsx`: PlacementList + exported formatDegreeMinutes(degree)
- `src/components/chart/assumptions-line.tsx`: AssumptionsLine ({ provenance, confidence, onAdjust })
- `src/components/chart/provenance-details.tsx`: ProvenanceDetails ({ provenance, placeResolution })
- `src/components/chart/unavailable-factors.tsx`: UnavailableFactors ({ unavailable, provisional })
- `src/components/chart/copy.ts`: result-screen copy constants
- `src/app/chart/result.tsx`: full ResultScreen (envelope param contract shared with 02-08's confirm push)

<output>
Create `.planning/phases/02-trustworthy-natal-chart/02-09-SUMMARY.md` when done
</output>
