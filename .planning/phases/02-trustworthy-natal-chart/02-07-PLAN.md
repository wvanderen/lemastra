---
phase: 02-trustworthy-natal-chart
plan: 07
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - api/tests/fixtures/golden/README.md
  - api/tests/fixtures/golden/cases/natal-1990-brooklyn.json
  - api/tests/fixtures/golden/cases/natal-1992-kentucky.json
  - api/tests/fixtures/golden/cases/natal-southern-hemisphere.json
  - api/tests/fixtures/golden/cases/dst-ambiguous-ny-2024.json
  - api/tests/fixtures/golden/cases/dst-nonexistent-ny-2024.json
  - api/tests/fixtures/golden/cases/dst-half-hour-shift.json
  - api/tests/fixtures/golden/cases/unknown-time-1990-brooklyn.json
  - api/tests/fixtures/golden/cases/high-latitude-tromso-whole-sign.json
  - api/tests/fixtures/golden/cases/high-latitude-tromso-placidus.json
  - api/tests/test_golden.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [GATE-02, CALC-01]
must_haves:
  truths:
    - "Nine golden cases — normal natal ×3, DST ambiguous, DST nonexistent, half-hour DST shift, unknown time, and the high-latitude success/expected-failure pair — pass through POST /api/v1/charts/calculate"
    - "Digests compare selected stable fields, never whole documents, so deliberate dependency promotion is reviewable instead of silently breaking"
    - "The Placidus-at-Tromsø case asserts the CALC_UNSUITABLE_HOUSE_SYSTEM failure as its expectation"
    - "A CI api job runs the full pytest suite plus the vendored skill's own smoke tests on every push, with submodules checked out and uv sync --locked"
  artifacts:
    - path: "api/tests/test_golden.py"
      provides: "Parametrized golden suite driving the calculate endpoint"
      contains: "parametrize"
    - path: "api/tests/fixtures/golden/cases/"
      provides: "Nine committed case files (input + expectation digests)"
      contains: "digest"
    - path: ".github/workflows/ci.yml"
      provides: "api job: pytest + golden + upstream smoke tests"
      contains: "submodules"
  key_links:
    - from: "api/tests/test_golden.py"
      to: "api/tests/fixtures/golden/cases/"
      via: "parametrized discovery over case JSON files"
      pattern: "glob"
    - from: ".github/workflows/ci.yml"
      to: "vendor/astrology-skill"
      via: "actions/checkout with submodules true + uv sync --locked against the committed lockfile"
      pattern: "uv sync --locked"
---

<objective>
Close GATE-02: the in-repo golden fixture suite (D-14) pinned against the vendored skill revision, covering normal natal charts, DST ambiguity, DST nonexistence, a half-hour DST zone, unknown birth time, and representative high-latitude behavior — run through the FastAPI calculate endpoint (not the raw script) so the app/API regression-tests against the same fixtures — plus the CI wiring that makes the suite a permanent gate.

Purpose: CALC-01 ("placements… pass the product's reference fixtures") and ROADMAP success criterion 5. Values are deterministic under the pinned lockfile (Moshier ephemeris is built-in; tzdata exact-locked), so digests regenerate only by deliberate, reviewed dependency promotion.
Output: Nine case files + README, green test_golden.py, fourth CI job.
</objective>

## Phase Goal (user story)

**As a** LemAstra user (curious beginner or serious enthusiast), **I want to** enter my birth details, confirm the resolved place and historical timezone, and explicitly resolve ambiguous or nonexistent civil times, **so that** I receive a validated, provenance-rich natal chart I can trust and inspect before any interpretation.

*(Derived from ROADMAP Phase 2 Goal + PROJECT.md audience definition.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trustworthy-natal-chart/02-RESEARCH.md
@.planning/phases/02-trustworthy-natal-chart/02-PATTERNS.md
@.planning/phases/02-trustworthy-natal-chart/02-03-SUMMARY.md

- 02-RESEARCH.md §"GATE-02 Golden Fixture Suite (D-14)" (case list, case JSON schema, design rules — field digests not whole-document equality; through-the-API execution; skill smoke tests in CI), §"CI wiring", §"Common Pitfalls" #10 (uv lock reproducibility), #6 (high-latitude pair)
- 02-PATTERNS.md §A3 (pytest gate semantics) and §E (ci.yml job conventions — header-comment rationale, permissions contents: read)
- .github/workflows/ci.yml (file being modified — existing test/gitleaks/bundle-scan jobs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the nine golden case files</name>
  <files>api/tests/fixtures/golden/cases/natal-1990-brooklyn.json, api/tests/fixtures/golden/cases/natal-1992-kentucky.json, api/tests/fixtures/golden/cases/natal-southern-hemisphere.json, api/tests/fixtures/golden/cases/dst-ambiguous-ny-2024.json, api/tests/fixtures/golden/cases/dst-nonexistent-ny-2024.json, api/tests/fixtures/golden/cases/dst-half-hour-shift.json, api/tests/fixtures/golden/cases/unknown-time-1990-brooklyn.json, api/tests/fixtures/golden/cases/high-latitude-tromso-whole-sign.json, api/tests/fixtures/golden/cases/high-latitude-tromso-placidus.json, api/tests/fixtures/golden/README.md</files>
  <read_first>
  - 02-RESEARCH.md §"GATE-02 Golden Fixture Suite" (case schema with expect.digest / second_pass_digest; verified anchor values: NY 2024-11-03 01:30 first-pass ascendant 149.5557, second-pass 161.3879; unknown-time omission contract)
  - 02-RESEARCH.md §"Output envelope" (the fields available for digests: ascendant.absolute_degree, placements.BODY.absolute_degree, house_cusps count, aspects count, birth_time_confidence)
  - api/tests/conftest.py + api/lemastra_api/routes/charts.py (drive the endpoint to generate values)
  - src/data/provider-registry.json (versioned-JSON style with schemaVersion — mirror the convention)
  </read_first>
  <action>
  For each case, write the input block from the research case list (natal-1990-brooklyn: 1990-05-21 14:32, lat 40.7128, lon −74.006, America/New_York, Whole Sign, Timed; natal-1992-kentucky: a Kentucky location with the America/New_York zone; natal-southern-hemisphere: e.g. Sydney or São Paulo coordinates; dst-ambiguous-ny-2024: 2024-11-03 01:30; dst-nonexistent-ny-2024: 2024-03-10 02:30; dst-half-hour-shift: Australia/Lord_Howe fall-back wall time; unknown-time-1990-brooklyn: confidence Unknown; high-latitude-tromso-whole-sign and -placidus: 69.6496N 18.9553E mid-summer). GENERATE the expect digests by actually running each input through the running calculate endpoint (TestClient) and recording stable fields — never copy expected values by hand except the two verified research anchors, which the generated values MUST reproduce: dst-ambiguous first_pass ascendant.absolute_degree 149.5557 and second_pass_digest ascendant.absolute_degree 161.3879 (assert these during generation as a sanity gate; if they differ, stop and investigate rather than committing). Each case file: id, category, input (the calculate request body), expect (exit path: success with digest of ascendant/placement absolute degrees to 4 decimals, house_cusps count 12, aspect count range, birth_time_confidence; OR expected_error CALC_UNSUITABLE_HOUSE_SYSTEM for the Tromsø Placidus case), and for the DST cases the second-pass variant digest via the fixed-offset tz input. Add the unavailable-key expectations to the unknown case (house_system/ascendant/sect/lots absent). Write fixtures/golden/README.md documenting the deliberate regeneration protocol: digests regenerate ONLY on dependency promotion (tzdata/pyswisseph/skill rev), as a reviewed commit, never in CI.
  </action>
  <verify>
    <automated>cd api && uv run python -c "import json,glob; files=sorted(glob.glob('tests/fixtures/golden/cases/*.json')); assert len(files)==9, files; [json.load(open(f)) for f in files]; print('9 valid case files')"</automated>
  </verify>
  <done>Nine valid case files committed; both verified DST anchors reproduced by the generated digests; README records the regeneration protocol.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_golden.py + CI api job</name>
  <files>api/tests/test_golden.py, .github/workflows/ci.yml</files>
  <read_first>
  - 02-RESEARCH.md §"GATE-02" design rules and §"CI wiring" (job yaml with submodules: true, setup-uv, uv sync --locked, pytest, skill smoke tests; A1 — verify the current astral-sh/setup-uv action tag before committing rather than trusting the research's ASSUMED v5)
  - .github/workflows/ci.yml (existing job structure, header-comment convention, permissions block — extend, do not restructure)
  - api/tests/test_calculate.py (TestClient usage pattern from 02-03)
  - 02-PATTERNS.md §"Exit-code gates, fail hard / warn soft"
  </read_first>
  <behavior>
  - Parametrized over all nine case files: success cases POST to /api/v1/charts/calculate and assert every digest field (tolerance 0.0001 on degrees); the Tromsø Placidus case asserts HTTP error body code CALC_UNSUITABLE_HOUSE_SYSTEM; the DST ambiguous case additionally asserts the second-pass digest by submitting the fixed-offset tz variant
  - Unknown case asserts absent keys (no house_system/ascendant/sect/lots in chart_data — absent, not empty) and unavailable_factors non-empty
  - source_notes is never compared (version strings whitelisted out by digest-field design)
  - CI: the api job checks out submodules, syncs the locked env, runs the full pytest suite (golden included), then runs the vendored skill's own smoke tests (tools/smoke_test.py, tz_smoke_test.py, dignity_smoke_test.py, timing_smoke_test.py)
  </behavior>
  <action>
  Write api/tests/test_golden.py: discover cases via glob over tests/fixtures/golden/cases, parametrize with ids; drive POST /api/v1/charts/calculate through the TestClient fixture; compare digest fields with pytest.approx(abs=1e-4); ranges asserted inclusively. Add the api job to .github/workflows/ci.yml following the existing job conventions: actions/checkout@v4 with submodules: true; the astral-sh/setup-uv action (pin the tag that `gh api` / the marketplace shows as current — research flagged its v5 tag as ASSUMED); python-version 3.12 with the action's caching enabled; `uv sync --locked` then `uv run pytest -q` with working-directory api; then the four vendored skill smoke scripts via `uv run python vendor/astrology-skill/tools/<script>` from the repo root. Preserve the top-level permissions: contents: read and the header-comment rationale convention; no continue-on-error anywhere.
  </action>
  <verify>
    <automated>cd api && uv run pytest -q tests/test_golden.py && uv run pytest -q</automated>
  </verify>
  <done>Full API suite including golden green locally (~1 s per the verified 40 ms/case runtime); ci.yml passes a YAML parse (npx yaml or python -c yaml.safe_load) and the job mirrors the documented wiring; gitleaks/test jobs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI checkout → vendored skill | The pinned submodule is the code under regression test |
| dependency resolution → golden digests | Lockfile drift could silently change calculated truths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Tampering | golden digests | mitigate | committed case files + deliberate-regeneration README + CI running the suite on every push — a changed digest without a reviewed promotion commit fails the gate |
| T-02-26 | Tampering | uv dependency resolution | mitigate | committed uv.lock + uv sync --locked in CI (never bare uv sync); dependency promotion requires digest regeneration by protocol |
| T-02-27 | Supply chain | setup-uv action tag | mitigate | pin the verified current major tag at implementation time (research A1 flagged the ASSUMED v5); job cannot silently use a floating latest |
| T-02-28 | Information Disclosure | CI logs | accept | fixture payloads are synthetic public test data; no GOOGLE_API_KEY exists in CI (geocoder tests use recorded fixtures by construction) |
</threat_model>

<verification>
- `cd api && uv run pytest -q` — full suite green
- `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('ci.yml parses')"` or equivalent
- Git status shows only the intended files; the submodule pointer unchanged at 660d992
</verification>

<success_criteria>
- GATE-02 closed: fixture coverage maps to every required dimension (normal natal → CALC-01; DST ambiguous/nonexistent/half-hour → BIRTH-03; unknown time → BIRTH-05; high latitude incl. expected-failure → GATE-02/CALC-04 recovery)
- The suite runs through the calculation service per D-14, and CI enforces it permanently
</success_criteria>

## Artifacts this phase produces

*Plan 02-07 symbols later plans consume (exclude from drift verification):*
- api/tests/test_golden.py: parametrized golden suite (case discovery + digest comparison helpers)
- api/tests/fixtures/golden/cases/*.json: nine case contracts (ids: natal-1990-brooklyn, natal-1992-kentucky, natal-southern-hemisphere, dst-ambiguous-ny-2024, dst-nonexistent-ny-2024, dst-half-hour-shift, unknown-time-1990-brooklyn, high-latitude-tromso-whole-sign, high-latitude-tromso-placidus)
- ci.yml: api job (submodule checkout + locked uv sync + pytest + skill smoke tests)

<output>
Create `.planning/phases/02-trustworthy-natal-chart/02-07-SUMMARY.md` when done
</output>
