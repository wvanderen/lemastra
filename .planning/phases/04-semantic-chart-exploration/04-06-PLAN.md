---
phase: 04-semantic-chart-exploration
plan: 06
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - src/components/chart/explore/mode-toggle.tsx
  - src/components/chart/explore/glossary.tsx
  - src/components/chart/explore/copy.ts
  - src/components/chart/explore/evidence-lists.tsx
  - src/components/chart/explore/fact-panel.tsx
  - src/components/chart/explore/wheel-canvas.tsx
  - src/app/chart/explore.tsx
  - src/__tests__/explore-mode.test.tsx
autonomous: true
requirements: [EVID-02]
must_haves:
  truths:
    - "One global Simple ↔ Technical segmented toggle on the explore surface flips wheel labels, list rows, and fact panels together — vocabulary and factor depth change as one experience (D-05, EVID-02)"
    - "Simple mode hides lots, sect, orb columns, and applying/separating state; Technical shows every field the envelope carries at full precision (D°MM′, absolute degrees, orb values) — both modes render from the SAME envelope object, no second data path (D-06)"
    - "The mode preference persists per device under @lemastra:explore.mode.v1 and rehydrates — first-run default is Simple (D-07)"
    - "Simple mode carries tap-to-explain glossary affordances: terms (e.g. trine, orb, retrograde) reveal a short static definition inline; definitions are copy-deck content, never interpretation (D-08)"
    - "The toggle conveys state through accessibilityState + the three-channel selection treatment (fill + border + weight) — never color alone, mirroring the confidence-control pattern (D-05, A11Y-02)"
  artifacts:
    - path: "src/components/chart/explore/mode-toggle.tsx"
      provides: "Inline segmented radiogroup: Simple ↔ Technical with selected-state semantics"
      contains: "radiogroup"
    - path: "src/components/chart/explore/glossary.tsx"
      provides: "Per-term expandable disclosure rendering deck definitions inline (Simple mode)"
      contains: "accessibilityState"
    - path: "src/components/chart/explore/copy.ts"
      provides: "Mode label pairs, glossary term definitions, Simple-vocabulary sentence templates"
      contains: "GLOSSARY"
    - path: "src/app/chart/explore.tsx"
      provides: "useExploreMode wired; mode prop threaded to canvas, lists, and panel"
      contains: "useExploreMode"
  key_links:
    - from: "src/app/chart/explore.tsx"
      to: "src/hooks/use-explore-mode.ts"
      via: "useExploreMode() → mode prop threaded to WheelCanvas/EvidenceLists/FactPanel (D-06: one state, prop-passed — not context, not two trees)"
      pattern: "useExploreMode"
    - from: "src/components/chart/explore/evidence-lists.tsx"
      to: "src/components/chart/explore/copy.ts"
      via: "mode-keyed sentence templates: Simple vocabulary vs Technical full-precision strings from the same deck"
      pattern: "mode"
---

<objective>
Deliver the beginner ↔ technical dual view: the global mode toggle, mode-aware vocabulary and factor depth across wheel labels, list rows, and the fact panel, and the Simple-mode glossary affordance.

Purpose: EVID-02 completes — approachable summaries and technical detail derived from one evidence base, with the preference remembered per device. The same-envelope law (D-06) keeps trust intact: nothing is recomputed, only vocabulary and depth change.
Output: ModeToggle + Glossary components, mode-aware surfaces, expanded copy deck (label pairs + glossary definitions), green mode tests.
</objective>

## Phase Goal (user story — slice 6 of 7)

**As a** LemAstra beginner, **I want to** switch the whole exploration surface to plain language with tap-to-explain terms, **so that** I can understand my chart without prior astrology vocabulary — and switch back to full technical precision whenever I choose.

*(Derived from ROADMAP Phase 4 Goal + Success Criteria 3.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-semantic-chart-exploration/04-PATTERNS.md
@.planning/phases/04-semantic-chart-exploration/04-02-SUMMARY.md
@.planning/phases/04-semantic-chart-exploration/04-04-SUMMARY.md
@.planning/phases/04-semantic-chart-exploration/04-05-SUMMARY.md

- 04-CONTEXT.md §"Beginner/Technical Dual View" D-05–D-08 (locked)
- 04-PATTERNS.md §mode-toggle (confidence-control.tsx radiogroup analog + OptionCard three-channel selection) and §glossary (provenance-details.tsx expandable-disclosure analog)
- src/hooks/use-explore-mode.ts (04-02), src/components/birth/confidence-control.tsx + src/components/ui/option-card.tsx, src/components/chart/provenance-details.tsx
- src/components/chart/explore/{evidence-lists,fact-panel,wheel-canvas,copy}.tsx + src/app/chart/explore.tsx (surfaces being made mode-aware)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ModeToggle + Glossary components + mode/glossary copy</name>
  <files>src/components/chart/explore/mode-toggle.tsx, src/components/chart/explore/glossary.tsx, src/components/chart/explore/copy.ts, src/__tests__/explore-mode.test.tsx</files>
  <read_first>
  - src/components/birth/confidence-control.tsx (radiogroup + options-from-deck pattern D-05 mirrors) + src/components/ui/option-card.tsx (three-channel selected state)
  - src/components/chart/provenance-details.tsx (expandable disclosure: accessibilityState expanded, structural state — never glyph-alone)
  - src/components/chart/explore/copy.ts (deck law being extended), src/hooks/use-explore-mode.ts (ExploreMode type)
  </read_first>
  <behavior>
  - ModeToggle renders a compact horizontal segmented control: two options (Simple, Technical) from the deck, radiogroup semantics, accessibilityState selected/checked on the active option, selected via fill + 2px accent border + 600 weight
  - Glossary renders a term chip that expands inline to its deck definition; expanded state structural (accessibilityState); pressing again collapses; definitions are static strings from the deck
  - copy.ts gains: MODE_LABEL_SIMPLE/TECHNICAL + toggle heading, the glossary term inventory with definitions (at minimum: trine, sextile, square, opposition, conjunction, orb, retrograde, ascendant — planner-note: exact inventory finalized from the envelope vocabulary the surfaces actually render), and mode-keyed sentence template pairs (Simple plain-language vs Technical full-precision) for panel and rows
  - tests pin exact deck strings; glossary definitions asserted to be pure static content (no envelope interpolation — never interpretation, D-08/T-02-34)
  </behavior>
  <action>
Write tests first, then implement both components as controlled props (value/onChange; term/definition). ModeToggle mirrors the confidence-control radiogroup structure compacted to one row; Glossary mirrors provenance-details' disclosure per term. All strings to the deck (Pitfall 11).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/explore-mode.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - mode-toggle.tsx contains `accessibilityRole="radiogroup"` (View-level) and selected-state branches for both options
  - glossary.tsx contains `accessibilityState={{ expanded` per-term disclosure
  - explore copy deck contains MODE_LABEL constants and a glossary definitions map with at least the eight core terms
  - explore-mode.test.tsx exits 0 on the component/deck assertions in this task
  </acceptance_criteria>
  <done>Toggle and glossary components exact-asserted against the deck; ready to thread through the surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire mode through the explore surface — one flip changes everything together</name>
  <files>src/app/chart/explore.tsx, src/components/chart/explore/evidence-lists.tsx, src/components/chart/explore/fact-panel.tsx, src/components/chart/explore/wheel-canvas.tsx, src/__tests__/explore-mode.test.tsx</files>
  <read_first>
  - src/app/chart/explore.tsx + the three surface components (current contracts being extended with a mode: ExploreMode prop)
  - src/hooks/use-explore-mode.ts (04-02) and 04-CONTEXT D-06 (the exact Simple-hidden list: lots, sect, orb columns, applying/separating state)
  - src/components/chart/explore/scroll-target.ts (row keys stay stable across mode flips — selection survives a flip)
  </read_first>
  <action>
explore.tsx: const { mode, setMode } = useExploreMode(); render ModeToggle inline above the wheel hero; pass mode down as a plain prop to WheelCanvas, EvidenceLists, FactPanel (D-06: one state, prop-passed). EvidenceLists: Simple hides the lots + sect sections and the orb/applying/separating fields; rows compose Simple vs Technical sentence templates from the deck; Simple rows render Glossary chips for the covered terms. FactPanel: Simple sentences use plain-language templates (same facts, same degree split); Technical keeps full precision (D°MM′, absolute degrees, orb, flags). WheelCanvas: Simple = glyph labels only; Technical = glyphs + degree labels (composing with the zoom tier from 04-05 — mode filters the label set, tier governs density). Selection/scroll state is untouched by mode flips (same FactorRef space).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/explore-mode.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
  - explore.tsx contains `useExploreMode` and passes a `mode` prop to WheelCanvas, EvidenceLists, and FactPanel (source assertion — one flip, three consumers)
  - explore-mode.test.tsx exits 0 with: flipping to Technical reveals lots/sect sections and orb/applying fields; Simple hides exactly those and swaps panel/row vocabulary; both modes render from the same envelope fixture (same-data-path assertion); persistence round-trip (set technical → new render hydrates technical); first-run default Simple
  - Full npm test green (04-03/04-04 surface suites updated where prop contracts changed, not weakened)
  </acceptance_criteria>
  <done>EVID-02 complete: one toggle flips the whole experience; the preference persists; the glossary keeps Simple clean without hiding the technical path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → mode state | Two-valued enum preference; validated at the hook (04-02) — no data exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | mode-aware surfaces drifting from one evidence base | mitigate | Same-data-path test asserts both modes render identical underlying fixture values; no mode branch introduces new astrological content — only deck vocabulary and field visibility change |
| T-04-13 | Information Disclosure | glossary definitions becoming interpretation | mitigate | Deck-only static strings, test-asserted free of envelope interpolation (D-08; T-02-34 extension) |
</threat_model>

<verification>
- npx vitest run src/__tests__/explore-mode.test.tsx
- npm test (full regression — prior explore suites updated for the mode prop)
</verification>

<success_criteria>
- EVID-02 complete and test-proven: vocabulary + depth flip together, same envelope, persisted preference, glossary affordance
- D-05/D-06/D-07/D-08 honored end to end
</success_criteria>

## Artifacts this phase produces

*Plan 04-06 symbols later plans consume (exclude from drift verification):*
- `src/components/chart/explore/mode-toggle.tsx`: ModeToggle ({ mode, onChange })
- `src/components/chart/explore/glossary.tsx`: Glossary ({ term }) per-term disclosure
- `src/components/chart/explore/copy.ts`: mode label pairs, glossary definitions map, mode-keyed sentence templates
- `src/components/chart/explore/{evidence-lists,fact-panel,wheel-canvas}.tsx`: `mode: ExploreMode` prop contracts
- `src/app/chart/explore.tsx`: useExploreMode wiring + ModeToggle placement

<output>
Create `.planning/phases/04-semantic-chart-exploration/04-06-SUMMARY.md` when done
</output>
