---
phase: 04-semantic-chart-exploration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chart/evidence-vocabulary/kinds.ts
  - src/components/chart/evidence-vocabulary/tokens.ts
  - src/components/chart/evidence-vocabulary/phrases.ts
  - src/__tests__/evidence-vocabulary.test.ts
  - src/hooks/use-explore-mode.ts
  - src/__tests__/use-explore-mode.test.ts
autonomous: true
requirements: [EVID-01, EVID-02, A11Y-02]
must_haves:
  truths:
    - "The evidence-vocabulary module defines all four evidence kinds — calculated / judgment / interpretation / uncertainty — with copy and a11y phrasing for each, and the interpretation kind is defined but NOT renderable until Phase 6 (D-14, D-15; EVID-01)"
    - "Aspect-family styles and the provisional-factor treatment carry stroke pattern + weight (and text redundancy hooks), never hue as the only differentiator (A11Y-02, D-16)"
    - "Judgment and uncertainty kinds extend the existing sectional patterns — AssumptionsLine-style labeled sections and UnavailableFactors/provisional card phrasings — with no per-row source badges and no user-learned token legend (D-13)"
    - "useExploreMode returns the Simple ↔ Technical preference with first-run default 'simple', persisted under the versioned key @lemastra:explore.mode.v1, safe-persist writes that never block the UI, and best-effort reads that fall back to the default on failure (D-07, EVID-02)"
  artifacts:
    - path: "src/components/chart/evidence-vocabulary/kinds.ts"
      provides: "EvidenceKind union (four kinds) + renderableEvidenceKinds (three — interpretation excluded)"
      contains: "interpretation"
    - path: "src/components/chart/evidence-vocabulary/tokens.ts"
      provides: "Per-kind visual tokens: judgment section style, uncertainty card + dashed-outline marker, aspect stroke pattern + weight map"
      contains: "stroke"
    - path: "src/components/chart/evidence-vocabulary/phrases.ts"
      provides: "Copy-deck + a11y phrasing per kind (template-over-facts idiom), including defined-but-unrendered interpretation phrasing"
      contains: "phrasing"
    - path: "src/hooks/use-explore-mode.ts"
      provides: "useExploreMode() → { mode, setMode } with versioned-key AsyncStorage persistence"
      contains: "@lemastra:explore.mode.v1"
  key_links:
    - from: "src/components/chart/evidence-vocabulary/phrases.ts"
      to: "src/components/chart/copy.ts"
      via: "same copy-deck idiom (template-over-facts, no invented strings) — vocabulary phrases join the deck law, not a parallel system"
      pattern: "placementA11yLabel|filter"
    - from: "src/hooks/use-explore-mode.ts"
      to: "src/hooks/use-disclosure.ts"
      via: "mirrors the versioned-key safe-persist AsyncStorage pattern"
      pattern: "AsyncStorage"
---

<objective>
Build the shared evidence-vocabulary module (D-14/D-15) and the Simple ↔ Technical mode-preference hook (D-07) — the two cross-cutting modules every explore surface consumes.

Purpose: EVID-01 requires calculated fact / methodological judgment / interpretation / uncertainty to stay distinguishable everywhere; defining the vocabulary ONCE now means Phase 6's interpretation output joins an existing, tested system instead of inventing a parallel one. The mode hook grounds EVID-02's remembered preference. This is Wave-1 foundation (parallel with 04-01 — zero file overlap).
Output: kinds/tokens/phrases modules with green tests, useExploreMode hook with persistence + failure-path tests.
</objective>

## Phase Goal (user story — slice 2 of 7)

**As a** LemAstra user, **I want to** the app to speak one consistent evidence vocabulary and remember whether I prefer Simple or Technical detail, **so that** every surface distinguishes what-is-calculated from what-is-judged and my preference survives reopening the app.

*(Derived from ROADMAP Phase 4 Goal + Success Criteria 3/4; EVID-01/EVID-02 groundwork.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-semantic-chart-exploration/04-PATTERNS.md

- 04-CONTEXT.md §"Trust Labeling (EVID-01)" D-13–D-16 and §"Beginner/Technical Dual View" D-05–D-08
- 04-RESEARCH.md §Pattern 1 (mode preference) + §Pattern 2 (vocabulary module shape)
- src/components/chart/copy.ts (copy-deck law + placementA11yLabel template-over-facts idiom), src/components/chart/assumptions-line.tsx + unavailable-factors.tsx (sectional patterns D-13 extends), src/constants/theme.ts (as-const token idiom), src/hooks/use-disclosure.ts (versioned-key pattern)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evidence-vocabulary module — kinds, tokens, phrases (four kinds defined; interpretation unrenderable)</name>
  <files>src/components/chart/evidence-vocabulary/kinds.ts, src/components/chart/evidence-vocabulary/tokens.ts, src/components/chart/evidence-vocabulary/phrases.ts, src/__tests__/evidence-vocabulary.test.ts</files>
  <read_first>
  - 04-CONTEXT.md D-13/D-14/D-15/D-16 (locked: sectional + marker treatments; four kinds now; interpretation renders nothing this phase; on-wheel uncertainty treatment)
  - src/components/chart/copy.ts (deck law: header comment, SCREAMING_SNAKE constants, templated pure functions, `.filter((s): s is string => …)` idiom)
  - src/constants/theme.ts lines 10–39 (as-const light/dark token pattern + ThemeColor key discipline)
  - src/components/chart/assumptions-line.tsx + unavailable-factors.tsx (the sectional/card treatments whose phrasing the vocabulary extends)
  </read_first>
  <behavior>
  - kinds.ts exports `EvidenceKind` = 'calculated' | 'judgment' | 'interpretation' | 'uncertainty' and `renderableEvidenceKinds` = ['calculated', 'judgment', 'uncertainty'] — test asserts 'interpretation' is a member of the union AND absent from renderableEvidenceKinds (D-15 enforcement)
  - tokens.ts: per-kind token objects where every aspect-family style includes a `strokePattern` ('solid' | 'dashed' | 'dotted') AND `strokeWidth` number; the provisional-factor token is a dashed-outline marker; test asserts no style object in the aspect map omits either field (A11Y-02 — hue never the only differentiator)
  - tokens.ts aspect families are keyed by the lowercase envelope aspect names (conjunction, sextile, square, trine, opposition) and the (strokePattern, strokeWidth) pairs are pairwise distinct
  - phrases.ts: copy + a11y phrasing for all four kinds following the template-over-facts idiom — judgment: labeled-section heading phrasing (extends the AssumptionsLine vocabulary); uncertainty: unavailable-card + provisional-note phrasing (extends UnavailableFactors/provisional vocabulary); interpretation: phrasing DEFINED with an explicit not-rendered marker constant; calculated: plain render (no badge, no marker — D-13)
  - phrases tests assert exact strings and that a fact-bearing template never rewords server values ({} placeholders render verbatim)
  </behavior>
  <action>
Write the test file first (RED), then implement the three modules. kinds.ts is a typed union + renderable list (interpretation deliberately excluded — the Phase-6 seam). tokens.ts follows the theme.ts as-const idiom with documented semantic roles (comments state WCAG/contrast rationale and the D-16 dashed-outline law). phrases.ts joins the copy-deck law: pure functions composing sentence segments, undefined segments filtered, zero component imports. No React/RN imports anywhere in the module (plain-Node testable).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/evidence-vocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
  - src/components/chart/evidence-vocabulary/kinds.ts contains `interpretation` and exports `renderableEvidenceKinds`
  - src/components/chart/evidence-vocabulary/tokens.ts contains `strokePattern` entries for all five aspect families and a dashed provisional marker
  - src/__tests__/evidence-vocabulary.test.ts exits 0 with the interpretation-unrenderable assertion, the non-hue-only style assertions, and exact-string phrasing pins
  </acceptance_criteria>
  <done>Four evidence kinds defined once with copy, a11y phrasing, and non-hue-only tokens; interpretation kind provably unrenderable this phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useExploreMode hook — versioned-key Simple/Technical preference (D-07)</name>
  <files>src/hooks/use-explore-mode.ts, src/__tests__/use-explore-mode.test.ts</files>
  <read_first>
  - src/hooks/use-disclosure.ts (the exact pattern to mirror: exported versioned key constant, mounted-guarded best-effort read with catch → undefined, optimistic setState + safe-persist write with swallowed failure)
  - src/__tests__/use-disclosure.test.tsx (hook-test mechanics: RN shim dynamic-import law, AsyncStorage fake seam)
  - 04-CONTEXT.md D-06/D-07 (mode is ONE React state passed as a prop — not context, not two trees; first-run default Simple)
  </read_first>
  <behavior>
  - Initial render returns mode 'simple' (first-run default, D-07) before storage resolves
  - A stored 'technical' hydrates to 'technical' after mount
  - setMode flips state immediately and persists 'simple' | 'technical' under the key '@lemastra:explore.mode.v1'
  - A rejected persist write (AsyncStorage.setItem throws) leaves the in-state flip intact and never throws (safe-persist)
  - A rejected read (getItem throws) falls back to the 'simple' default silently
  </behavior>
  <action>
Write the test first mirroring use-disclosure.test.tsx mechanics (dynamic imports in beforeAll per the RN shim law; fake AsyncStorage module seam). Implement use-explore-mode.ts exporting EXPLORE_MODE_KEY = '@lemastra:explore.mode.v1', ExploreMode = 'simple' | 'technical', and useExploreMode(): { mode, setMode }. The hook owns no UI vocabulary — consumers pass mode down as a plain prop (D-06 same-data-path law).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/use-explore-mode.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
  - src/hooks/use-explore-mode.ts contains `@lemastra:explore.mode.v1` and exports `useExploreMode`
  - The test file covers: default simple, technical hydration, persist round-trip, write-failure no-crash, read-failure fallback — and exits 0
  - Full suite `npm test` stays green
  </acceptance_criteria>
  <done>Mode preference persists per device under a versioned key with best-effort semantics; ready for 04-06 to wire it through the explore surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → hook state | Local preference read/write only; no cross-boundary data (the stored value is a two-valued enum, not personal data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | corrupted preference value | mitigate | Value is parsed against the 'simple' \| 'technical' union; anything else falls back to the default — never rendered, never crashes |
| T-04-04 | Information Disclosure | vocabulary module | accept | Static copy + tokens only; contains no user data, no birth data, no payloads |
</threat_model>

<verification>
- npx vitest run src/__tests__/evidence-vocabulary.test.ts src/__tests__/use-explore-mode.test.ts
- npm test (full regression)
</verification>

<success_criteria>
- EVID-01's vocabulary foundation: four kinds, one module, Phase-6-ready seam with the interpretation kind testably unrenderable
- EVID-02's persistence foundation: versioned-key hook with safe-persist semantics
- A11Y-02 token law enforced by test (stroke pattern + weight, never hue alone)
</success_criteria>

## Artifacts this phase produces

*Plan 04-02 symbols later plans consume (exclude from drift verification):*
- `src/components/chart/evidence-vocabulary/kinds.ts`: EvidenceKind, renderableEvidenceKinds
- `src/components/chart/evidence-vocabulary/tokens.ts`: per-kind token objects, ASPECT_STYLE map (strokePattern + strokeWidth), provisional dashed-outline marker
- `src/components/chart/evidence-vocabulary/phrases.ts`: kind phrasing functions + not-rendered interpretation marker
- `src/hooks/use-explore-mode.ts`: useExploreMode, ExploreMode, EXPLORE_MODE_KEY

<output>
Create `.planning/phases/04-semantic-chart-exploration/04-02-SUMMARY.md` when done
</output>
