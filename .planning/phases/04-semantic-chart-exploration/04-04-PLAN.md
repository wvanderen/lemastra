---
phase: 04-semantic-chart-exploration
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - src/components/chart/explore/evidence-lists.tsx
  - src/components/chart/explore/scroll-target.ts
  - src/components/chart/explore/copy.ts
  - src/app/chart/explore.tsx
  - src/__tests__/evidence-lists.test.tsx
  - src/__tests__/explore-surface.test.tsx
autonomous: true
requirements: [WHEEL-04, EVID-01]
must_haves:
  truths:
    - "The explore surface renders structured evidence tables below the wheel — placements, houses, aspects (with orbs, applying/separating, exact), lots, and sect — every field present-only from the same envelope, with headings and sentence templates from the copy deck (WHEEL-04)"
    - "Tapping a list row selects that factor: the wheel highlights it and the fact panel shows its facts; tapping the wheel auto-scrolls the lists to the matching row — one shared selection, two-way sync (D-10)"
    - "Programmatic auto-scroll NEVER mutates selection — selection changes only on explicit user intent (tap/press), so no scroll→select→scroll feedback loop (Pitfall 9)"
    - "Unknown-time charts render no houses/lots/sect sections (absent envelope keys ⇒ absent sections), and the unavailable-factors cards carry the server reasons verbatim; methodological judgments stay in the labeled assumptions section (D-13, Phase-2 D-10 honesty)"
    - "Selected rows convey state through accessibilityState + accent border — never color alone (A11Y-02)"
    - "Auto-scroll targeting is a pure function unit-tested independently of RN scrolling mechanics (vitest seam per RESEARCH Open Question 3)"
  artifacts:
    - path: "src/components/chart/explore/evidence-lists.tsx"
      provides: "Five evidence sections (placements/houses/aspects/lots/sect) with pressable rows, selected state, and one-split a11y labels"
      contains: "accessibilityRole"
      min_lines: 100
    - path: "src/components/chart/explore/scroll-target.ts"
      provides: "Pure scrollTargetFor(selection, rowTopsRegistry) computation + loop-guard contract types"
      contains: "scrollTargetFor"
    - path: "src/app/chart/explore.tsx"
      provides: "Explore surface extended: lists mounted below panel in D-02/D-13 order, two-way selection wiring, auto-scroll with guard"
      contains: "EvidenceLists"
  key_links:
    - from: "src/components/chart/explore/evidence-lists.tsx"
      to: "src/components/chart/placement-list.tsx"
      via: "extends the PlacementList row/heading/a11y patterns (placementA11yLabel + splitDegreeMinutes) — pressable rows instead of a fork"
      pattern: "placementA11yLabel|splitDegreeMinutes"
    - from: "src/app/chart/explore.tsx"
      to: "src/components/chart/explore/evidence-lists.tsx"
      via: "shared selection state passed down; row press → setSelection (same FactorRef union the wheel emits)"
      pattern: "FactorRef"
    - from: "src/components/chart/assumptions-line.tsx"
      to: "src/app/chart/explore.tsx"
      via: "mounted as-is below the evidence sections — the D-13 judgment section (03-07 made its action optional)"
      pattern: "AssumptionsLine"
---

<objective>
Deliver the synchronized structured-evidence experience below the wheel: all five evidence tables, two-way selection sync with auto-scroll, and the sectional trust treatments (assumptions + unavailable factors) on the explore surface.

Purpose: WHEEL-04 completes (inspect placements, houses, aspects, orbs, motion in tables synchronized with the wheel) and EVID-01's render half lands (judgment in labeled sections, uncertainty in distinct cards — D-13). The lists are also the canonical non-visual path that 04-07's parity tests will lean on.
Output: EvidenceLists component + pure scroll-target module + extended explore surface with green sync tests.
</objective>

## Phase Goal (user story — slice 4 of 7)

**As a** LemAstra user, **I want to** read my chart's placements, houses, aspects, orbs, and motion in structured tables that stay in sync with the wheel, **so that** I can inspect dense evidence precisely and always see which item the wheel is highlighting.

*(Derived from ROADMAP Phase 4 Goal + Success Criteria 2/4.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-semantic-chart-exploration/04-PATTERNS.md
@.planning/phases/04-semantic-chart-exploration/04-03-SUMMARY.md

- 04-RESEARCH.md §Pattern 5 (one shared selection state + loop-guard contract), §Pitfall 9 (auto-scroll feedback loops), §"Open Questions" 3 (scroll mechanism must keep the vitest seam mockable)
- src/components/chart/placement-list.tsx + chart/copy.ts (row/heading/a11y/degree patterns to extend), src/components/chart/assumptions-line.tsx + unavailable-factors.tsx (mounted as-is)
- src/lib/api-schemas.ts aspectSchema/lotSchema/sectSchema (the exact fields each table renders), src/test/fixtures/frozen-natal-envelope.json + unknown-time-envelope.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EvidenceLists component — five sections, pressable rows, full envelope depth</name>
  <files>src/components/chart/explore/evidence-lists.tsx, src/components/chart/explore/copy.ts, src/__tests__/evidence-lists.test.tsx</files>
  <read_first>
  - 04-PATTERNS.md §evidence-lists (placement-list.tsx analog: section/heading/list/listitem structure, key convention, present-only slots) + §copy deck assignment
  - src/components/chart/placement-list.tsx + src/components/chart/copy.ts (placementA11yLabel, spokenDegrees, housePhrase — reuse, never a second formatter)
  - src/lib/api-schemas.ts lines 253–291 (aspect/lot/sect field contracts incl. applying/separating presence flags) + lines 340–364 (unavailable/provisional)
  - src/test/fixtures/frozen-natal-envelope.json (Timed: all sections present) + src/test/fixtures/unknown-time-envelope.json (absent sections)
  </read_first>
  <behavior>
  - placements section: rows pressable → onSelect({kind:'planet', body}); each row shows body, sign, D°MM′, house phrase when present, motion, dignities when present — a11y label via placementA11yLabel (unchanged sentence contract)
  - houses section (only when house_cusps present): rows pressable → onSelect({kind:'house', house}); row shows house number, cusp sign, D°MM′; new houseRowA11yLabel from the deck
  - aspects section (when aspects present): rows pressable → onSelect({kind:'aspect', index}); row shows body_a, aspect, body_b, orb value, applying/separating ONLY when the presence flag exists, exact state; aspectRowA11yLabel from the deck
  - lots section (when lots present) and sect card (when sect present): full envelope depth (formula, luminary, sect mates, notes) — these are the D-06 Technical-only sections, rendered here at full depth; 04-06 hides them in Simple mode
  - selected row: accessibilityState={{ selected: true }} + accent border + 600 label (three channels, never color alone)
  - unknown-time envelope: houses/lots/sect sections render nothing (not empty shells); placements + aspects still render
  </behavior>
  <action>
Write evidence-lists.test.tsx first (RNTL /pure dynamic-import law, fixtures drive expected rows, exact-string pins on deck sentences), then implement. EvidenceLists({ envelope, selection, onSelect }) is controlled; rows reuse the Phase-1 card treatment and ThemedText primitives; every new string (headings, aspect/house/lot/sect sentences) lands in explore/copy.ts — zero literals inside components (Pitfall 11).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/evidence-lists.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - src/components/chart/explore/evidence-lists.tsx contains `accessibilityState={{ selected` (or equivalent selected-state branch) and imports from placement-list/chart copy
  - explore copy deck contains houses/aspects/lots/sect headings + row sentence templates
  - evidence-lists.test.tsx exits 0: all five sections render exact envelope fields for the Timed fixture; unknown-time fixture renders no houses/lots/sect sections; row press emits the correct FactorRef
  </acceptance_criteria>
  <done>Every envelope fact is inspectable as a structured, pressable, a11y-labeled row — WHEEL-04's table half.</done>
</task>

<task type="auto">
  <name>Task 2: Mount on explore surface + two-way sync + loop-guarded auto-scroll + trust sections</name>
  <files>src/app/chart/explore.tsx, src/components/chart/explore/scroll-target.ts, src/__tests__/explore-surface.test.tsx</files>
  <read_first>
  - src/app/chart/explore.tsx (current surface being extended — selection state and section order)
  - 04-RESEARCH.md §Pattern 5 + §Pitfall 9 (selection only on explicit user intent; programmatic scrollTo under a guard)
  - src/components/chart/assumptions-line.tsx + unavailable-factors.tsx (mount as-is below sections; 03-07 made AssumptionsLine's action optional)
  - src/__tests__/placement-list.test.tsx (screen-test mechanics) + src/test/fixtures (both envelopes)
  </read_first>
  <action>
Create scroll-target.ts as a pure module: row registry type (Map from a row key derived from FactorRef → measured top), scrollTargetFor(selection, registry) → target y, and the loop-guard contract documented (programmatic scrolls set a guard so scroll events never call setSelection). Extend explore.tsx: mount EvidenceLists between the fact panel and AssumptionsLine/UnavailableFactors (final order: wheel hero → fact panel → placements → houses → aspects → lots → sect → assumptions → unavailable — D-02/D-13); row press and wheel tap both call the same setSelection (D-10); on selection change scroll the page ScrollView to scrollTargetFor(...) under the guard; mount AssumptionsLine (house system/zodiac/orb policy judgment section) and UnavailableFactors (server reasons verbatim) as-is. Explore-surface tests: two-way sync (row press → wheel selection prop + panel facts; simulated wheel onSelect → selected row state + scrollTo invoked with the expected target via a spied scroll seam), loop-freedom (programmatic scroll event does not invoke setSelection — asserted with a spy), unknown-time shape (no houses/lots/sect sections, unavailable cards render server reasons verbatim).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/explore-surface.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
  - src/components/chart/explore/scroll-target.ts contains `scrollTargetFor` with unit tests (pure — no RN imports)
  - src/app/chart/explore.tsx renders AssumptionsLine, UnavailableFactors, and EvidenceLists in the D-02/D-13 order
  - explore-surface.test.tsx exits 0: two-way selection sync, auto-scroll called with the computed target, programmatic scroll does not re-trigger selection, unknown-time section omissions
  - Full suite npm test stays green
  </acceptance_criteria>
  <done>Wheel ↔ lists read as one synchronized unit with loop-proof scrolling; judgments and uncertainty keep their sectional treatments (EVID-01 render half, D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| envelope → list rows | Rendered server-derived facts only; no client-side recomputation of aspects/houses/dignities anywhere in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | client-side aspect/house recomputation drift | mitigate | The stored envelope IS the evidence — EvidenceLists renders emitted fields only; tests pin rows to fixture values, no derived astrological math in components |
| T-04-09 | DoS | auto-scroll feedback loop janking the surface | mitigate | Pure scrollTargetFor + guard contract, unit- and component-tested loop-free (Pitfall 9) |
</threat_model>

<verification>
- npx vitest run src/__tests__/evidence-lists.test.tsx src/__tests__/explore-surface.test.tsx
- npm test (full regression)
</verification>

<success_criteria>
- WHEEL-04 complete: five synchronized tables with orbs, applying/separating, exact, and motion — two-way sync loop-free
- EVID-01's sectional treatments mounted on the explore surface (D-13); unavailable reasons render verbatim
- Unknown-time honesty preserved end to end
</success_criteria>

## Artifacts this phase produces

*Plan 04-04 symbols later plans consume (exclude from drift verification):*
- `src/components/chart/explore/evidence-lists.tsx`: EvidenceLists ({ envelope, selection, onSelect }) with five sections + row a11y contracts
- `src/components/chart/explore/scroll-target.ts`: scrollTargetFor, row-registry types, loop-guard contract
- `src/components/chart/explore/copy.ts`: houses/aspects/lots/sect headings + row sentence templates (houseRowA11yLabel, aspectRowA11yLabel)
- `src/app/chart/explore.tsx`: extended surface order (wheel → panel → lists → assumptions → unavailable)

<output>
Create `.planning/phases/04-semantic-chart-exploration/04-04-SUMMARY.md` when done
</output>
