---
phase: 04-semantic-chart-exploration
plan: 07
type: execute
wave: 6
depends_on: ["04-06"]
files_modified:
  - src/components/chart/explore/wheel-a11y-overlay.tsx
  - src/app/chart/explore.tsx
  - src/app/chart/result.tsx
  - src/components/chart/explore/wheel-canvas.tsx
  - src/__tests__/wheel-a11y-parity.test.tsx
  - src/__tests__/explore-web.test.tsx
autonomous: false
requirements: [WHEEL-05, EVID-02, A11Y-01, A11Y-02, A11Y-03]
must_haves:
  truths:
    - "Every wheel factor exists as an invisible accessible element positioned over its hit region — screen-reader users navigate and select wheel factors exactly like sighted users (D-12, WHEEL-05)"
    - "The raw canvas is hidden from screen readers (importantForAccessibility no-hide-descendants + accessibilityElementsHidden) while Pressables remain reachable (A11Y-01)"
    - "Overlay labels equal the fact-panel sentences — same formatters, one degree split — proven by list-parity tests across overlay ↔ lists ↔ panel (A-UI-4, A11Y-03)"
    - "The fact panel announces selection updates via a polite live region (A11Y-01)"
    - "No differentiation anywhere relies on color alone: aspect styles carry stroke pattern + weight, provisional factors dashed outline + text (A11Y-02, D-16)"
    - "Web /chart/result renders the full D-04 evidence experience from the in-memory envelope — ModeToggle + FactPanel + EvidenceLists with selection via pressable rows (D-10), zero canvas — so web users get dual views, structured lists, fact panels, and exact facts without the graphical wheel (D-04, EVID-02)"
    - "Web /chart/explore deep-links keep the WebUnsupported capability card with zero canvas — the repository is web-unavailable, so a direct web visit is genuinely data-less; all text surfaces preserve default font-scaling behavior (A11Y-03, D-04, Pitfall 8)"
    - "On-device verification passes on Android + iOS: wheel renders without glyph tofu (A1 resolved), pinch/pan/tap work, and a screen reader can navigate and select wheel factors"
  artifacts:
    - path: "src/components/chart/explore/wheel-a11y-overlay.tsx"
      provides: "Invisible Pressable per geometry hit region: role button, selected state, labels from the same vocabulary/degree split"
      contains: "importantForAccessibility"
      min_lines: 40
    - path: "src/__tests__/wheel-a11y-parity.test.tsx"
      provides: "Parity suite: one overlay element per wheel factor; overlay/list/panel label agreement; canvas hiding; selected-state conveyance"
      contains: "parity"
    - path: "src/app/chart/result.tsx"
      provides: "Web branch mounting the D-04 evidence experience: ModeToggle + FactPanel + EvidenceLists from the in-memory envelope (native layout unchanged)"
      contains: "EvidenceLists"
    - path: "src/__tests__/explore-web.test.tsx"
      provides: "Web conformance suite: D-04 delivery on /chart/result (evidence experience mounts, zero Canvas) + explore-route capability card"
      contains: "web"
  key_links:
    - from: "src/components/chart/explore/wheel-a11y-overlay.tsx"
      to: "src/lib/chart-wheel/geometry.ts"
      via: "hit regions' bounding rects position the overlay elements; labels from the same FactorRef space the panel renders"
      pattern: "hitRegions|FactorRef"
    - from: "src/app/chart/result.tsx"
      to: "src/components/chart/explore/evidence-lists.tsx"
      via: "web branch mounts ModeToggle + FactPanel + EvidenceLists from the in-memory envelope; row press → setSelection → panel facts (D-10 list-half — no wheel on web)"
      pattern: "EvidenceLists"
    - from: "src/app/chart/explore.tsx"
      to: "src/components/chart/explore/wheel-canvas.tsx"
      via: "canvas wrapper View carries importantForAccessibility no-hide-descendants + accessibilityElementsHidden — decorative to screen readers (Pattern 6)"
      pattern: "no-hide-descendants|accessibilityElementsHidden"
---

<objective>
Close non-visual access and deliver D-04 on web: the accessible overlay over the wheel, canvas hiding, label-parity guarantees, the full web evidence experience on /chart/result (no canvas — per the user decision resolving the D-04 plan-check blocker), and the on-device verification checkpoint that resolves the glyph assumption (A1) and validates gestures + screen-reader navigation.

Purpose: WHEEL-05, A11Y-01/02/03 complete and D-04 ships as written — the core chart view is navigable by screen reader, never color-dependent, scaling-safe, and fully usable without interpreting the graphical wheel; web users get dual views, lists, fact panels, and facts from the in-memory result envelope while the explore deep-link card stays honest about the web-unavailable repository.
Output: WheelA11yOverlay component mounted on the explore surface, the /chart/result web branch mounting ModeToggle + EvidenceLists + FactPanel, parity + web test suites, and an approved on-device human verification.
</objective>

## Phase Goal (user story — slice 7 of 7)

**As a** screen-reader user, **I want to** navigate and select every wheel factor from the same place sighted users tap, **so that** I get the identical exact facts without interpreting the graphical wheel — and every user gets the wheel verified on real devices.

*(Derived from ROADMAP Phase 4 Goal + Success Criteria 5.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-semantic-chart-exploration/04-PATTERNS.md
@.planning/phases/04-semantic-chart-exploration/04-06-SUMMARY.md

- 04-RESEARCH.md §Pattern 6 (overlay + canvas hiding, RN 0.86 a11y API — verified), §Assumption A1/A3 (Android glyph risk; overlay at base geometry acceptable), §"Validation Architecture" phase gate (real device for VoiceOver/TalkBack)
- src/components/ui/option-card.tsx (Pressable a11y state semantics), src/components/chart/copy.ts (placementA11yLabel sourcing law)
- src/app/chart/explore.tsx + wheel-canvas.tsx (surface being wrapped), src/lib/chart-wheel/geometry.ts (hit region rects), src/test/fixtures (both envelopes for parity)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accessible overlay + canvas hiding on the explore surface</name>
  <files>src/components/chart/explore/wheel-a11y-overlay.tsx, src/app/chart/explore.tsx, src/components/chart/explore/wheel-canvas.tsx, src/__tests__/wheel-a11y-parity.test.tsx</files>
  <read_first>
  - 04-RESEARCH.md §Pattern 6 code excerpt (importantForAccessibility="no-hide-descendants" + accessibilityElementsHidden + transparent Pressables with role/state/label) — verified against reactnative.dev/docs/accessibility 0.86
  - 04-PATTERNS.md §wheel-a11y-overlay (option-card selection semantics + label-sourcing law: placementA11yLabel-style sentences, never a second formatter)
  - src/app/chart/explore.tsx + src/components/chart/explore/wheel-canvas.tsx (where the wrapper and overlay mount), src/lib/chart-wheel/geometry.ts (hitRegions with bounding rects + FactorRef)
  </read_first>
  <behavior>
  - one transparent Pressable per geometry hit region, absolutely positioned from the base-coordinate bounding rect, accessibilityRole="button", accessibilityState={{ selected }}, accessibilityLabel = the same sentence the fact panel composes for that factor, onPress → setSelection(factor)
  - the canvas wrapper hides the canvas from screen readers on both platforms (no-hide-descendants + accessibilityElementsHidden); the overlay Pressables remain fully reachable
  - activating an overlay element produces the identical outcome as a sighted tap: selection updates, panel announces (live region from 04-03), lists highlight/scroll
  - parity tests (both fixtures): overlay element count == wheel factor count; for every factor, overlay accessibilityLabel === the panel/list sentence for that factor (string equality); selected state conveyed on the selected element; unknown-time fixture asserts no house/angle overlay elements
  </behavior>
  <action>
Write wheel-a11y-parity.test.tsx first, then implement WheelA11yOverlay({ regions, selection, onSelect, sentences }) mounted in explore.tsx over the canvas container (base/unzoomed positions per A3 — the lists remain the canonical non-visual path; the overlay provides the same-place experience). Labels derive from the existing deck sentence functions — zero new formatters. Add the wrapper hiding props around WheelCanvas.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/wheel-a11y-parity.test.tsx</automated>
  </verify>
  <acceptance_criteria>
  - wheel-a11y-overlay.tsx contains `accessibilityRole="button"` elements positioned from hit-region rects and `accessibilityState` selected branches
  - explore.tsx (or wheel-canvas wrapper) contains `importantForAccessibility="no-hide-descendants"` and `accessibilityElementsHidden`
  - wheel-a11y-parity.test.tsx exits 0: count parity, string-equal labels vs panel/list sentences, selected conveyance, unknown-time omissions
  </acceptance_criteria>
  <done>WHEEL-05 + A11Y-01 complete: the wheel is semantically navigable and selection-identical for screen-reader users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 web evidence experience on /chart/result + web/A11Y-02 conformance proofs</name>
  <files>src/app/chart/result.tsx, src/__tests__/explore-web.test.tsx, src/__tests__/wheel-a11y-parity.test.tsx</files>
  <read_first>
  - 04-RESEARCH.md §Validation Architecture rows A11Y-02/A11Y-03 + §Pitfall 8 (Skia text never the sole fact carrier; RN Text scales natively)
  - src/app/chart/result.tsx (the screen gaining the web branch — trust sections + SavePrompt wiring; 04-03 already made MiniWheelCard native-only, so the Platform seam exists)
  - src/components/chart/explore/{mode-toggle,evidence-lists,fact-panel}.tsx + src/hooks/use-explore-mode.ts (04-04/04-06 outputs being mounted — contracts unchanged, mode prop threaded)
  - src/components/workspace/web-unsupported.tsx + its copy (the approved capability card), src/app/chart/explore.tsx web branch (from 04-03)
  - src/components/chart/evidence-vocabulary/tokens.ts (stroke pattern + weight law), src/components/themed-text.tsx (default allowFontScaling posture)
  </read_first>
  <behavior>
  - web Platform mock rendering /chart/result: ModeToggle + FactPanel + EvidenceLists all mount from the in-memory envelope the screen already holds — and ZERO Canvas elements mount (facade/spy assertion)
  - pressing an evidence row populates the fact panel with that factor's exact facts (D-10 selection via pressable rows — the wheel half of the sync simply does not exist on web); the selected row conveys state through accessibilityState + accent treatment, never color alone
  - flipping the mode toggle on web switches vocabulary/depth exactly as on native — same envelope object, same deck templates (same-data-path; Simple hides lots/sect/orb columns web-side too)
  - web /chart/explore still renders the WebUnsupported capability card with zero Canvas — deep-link posture unchanged from 04-03
  - A11Y-02 conformance: every aspect style token includes strokePattern + strokeWidth (no hue-only pairs — re-asserted at component render level), the provisional treatment renders dashed + text redundancy, and no text surface disables font scaling (no allowFontScaling={false} in the explore family — Pitfall 8: facts live in RN Text surfaces)
  </behavior>
  <action>
  Extend explore-web.test.tsx FIRST with the /chart/result web assertions above (RED — result.tsx renders no evidence family on web today), then implement the web branch in result.tsx per D-04: on Platform.OS === 'web', render the evidence experience in place of the native wheel-first block — useExploreMode + ModeToggle inline, one shared useState&lt;FactorRef | null&gt; selection, FactPanel adjacent to the selection, EvidenceLists below with pressable rows driving the same selection (D-10 list-half; no canvas, no wheel auto-scroll). EvidenceLists' placements section supersedes the plain PlacementList on web (no duplicate placements table); AssumptionsLine/ProvenanceDetails/UnavailableFactors and the SavePrompt flow remain untouched; the MiniWheelCard stays native-only. Then extend the parity suite with the A11Y-02 conformance assertions (aspect style tokens non-hue-only at render level, provisional dashed + text, default font-scaling across the explore family).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/explore-web.test.tsx src/__tests__/wheel-a11y-parity.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
  - src/app/chart/result.tsx contains a Platform.OS === "web" branch mounting ModeToggle, EvidenceLists, and FactPanel from the in-memory envelope; the native layout is unchanged (MiniWheelCard still native-only, SavePrompt untouched)
  - explore-web.test.tsx exits 0: web /chart/result → evidence experience mounts with zero Canvas, row press populates the panel, mode flip changes vocabulary/depth; web /chart/explore → capability card, zero Canvas
  - Parity suite extended assertions exit 0: non-hue-only styles, dashed+text provisional, default font-scaling preserved across the explore family
  - Full npm test green (phase gate precondition)
  </acceptance_criteria>
  <done>D-04 shipped as written — web renders the full evidence experience (dual views, lists, fact panels, facts) without the graphical wheel on /chart/result, the explore deep-link card stays honest, and A11Y-02/A11Y-03 are proven in the suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device verification — glyphs (A1), gestures, screen reader, web card</name>
  <what-built>
The complete Phase-4 exploration experience: mini-wheel entry cards, the explore route with zoomable//selectable wheel, synchronized lists, Simple ↔ Technical toggle with glossary, and the accessible overlay. Automated suites cover geometry, selection, sync, mode, parity, and web degradation — what remains unverifiable in CI is on-device rendering (Android glyph coverage — assumption A1), gesture feel inside the scrolling page (Pitfall 6), and real screen-reader navigation (simulators cannot run VoiceOver/TalkBack).
  </what-built>
  <how-to-verify>
1. Start the dev server (`npx expo start`) and open the app in Expo Go on an Android device/emulator AND an iOS device/simulator.
2. Calculate or open a saved Timed chart: confirm the mini-wheel card renders on result + saved; tap through to /chart/explore; confirm the full wheel renders — CHECK FOR TOFU (□) on any planet/sign glyph (Node ☊☋, Chiron ⚳, Lilith ⚸ are the risk cases). If tofu appears, report which glyphs — the pre-built fallback abbreviations (glyphs.ts) or a bundled OFL font (legitimacy-gated) will be swapped in.
3. Pinch to zoom and pan the wheel: confirm zoom clamps ~1–4×, the wheel cannot be lost off-canvas, page scrolling still works outside the canvas, and TAPS SELECT THE CORRECT FACTOR WHILE ZOOMED.
4. Tap several factors (planet in a dense cluster, a sign, a house cusp, an aspect chord): confirm the fact panel shows exact facts and the matching list row highlights + scrolls into view; press list rows and confirm the wheel highlights.
5. Flip Simple ↔ Technical: confirm labels, rows, and panel change vocabulary/depth together; reload the app and confirm the preference persisted; open a glossary term in Simple mode.
6. Enable VoiceOver (iOS)/TalkBack (Android): navigate the wheel area — confirm overlay elements are announced with label + selected state, the canvas itself is not focusable, and activating an overlay element announces the fact panel update.
  7. On web (`npx expo start --web`): open a chart result — confirm the D-04 evidence experience mounts with no wheel (flip Simple ↔ Technical and confirm vocabulary/depth change together; press list rows and confirm exact facts appear in the panel); then deep-link /chart/explore directly — confirm it shows the capability card (repository is web-unavailable there).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass on both platforms, or list the failing items (e.g. "Android: Chiron glyph tofu; wheel pan scrolls page") for targeted fixes.</resume-signal>
  <files>.planning/phases/04-semantic-chart-exploration/04-07-SUMMARY.md</files>
  <action>Present this checkpoint with the seven verification steps and pause plan execution. On resume, record the decision verbatim (approval or the failing items per platform) in the plan SUMMARY; failing items become targeted fix-back tasks before the phase can pass /gsd-verify-work.</action>
  <verify>
    <automated>grep -q -i 'approved\|checkpoint' .planning/phases/04-semantic-chart-exploration/04-07-SUMMARY.md</automated>
  </verify>
  <done>On-device verification decision recorded in the SUMMARY; A1 (glyph coverage) resolved with evidence or a fallback task queued.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a11y overlay → screen reader | Labels are locally composed facts (same sentences as the panel); no untrusted content crosses — envelope strings are calculator-emitted and zod-validated upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Denial of Service (a11y) | overlay drift from wheel geometry making factors unreachable | mitigate | Parity tests assert one overlay element per geometry hit region from the SAME module (D-12) — single source prevents drift |
| T-04-15 | Information Disclosure | a11y labels diverging from visible facts | mitigate | String-equality parity assertions (overlay ↔ list ↔ panel) — A-UI-4 law enforced by test |
| T-04-16 | Tampering | web result branch drifting into a parallel evidence implementation | mitigate | The web branch mounts the SAME components (ModeToggle/EvidenceLists/FactPanel) and deck the native surface uses — no web-specific formatter or data path; tests assert identical fixture values render on web |
| T-04-SC | Tampering | (carried) package supply chain | mitigate | Closed in 04-01 via the blocking legitimacy checkpoint + Expo-pinned install; re-linked here for the phase record |
</threat_model>

<verification>
- npx vitest run src/__tests__/wheel-a11y-parity.test.tsx src/__tests__/explore-web.test.tsx
- npm test && npx tsc --noEmit (phase gate: full suite + typecheck green before /gsd-verify-work)
- On-device checkpoint approval (Task 3)
</verification>

<success_criteria>
- WHEEL-05, A11Y-01, A11Y-02, A11Y-03 complete and proven
- D-04 delivered as written (user decision): web /chart/result renders the full evidence experience — dual views, lists, fact panels, facts — with zero canvas; the capability card remains only on the genuinely data-less /chart/explore deep-link
- A1 (Android glyph coverage) resolved by on-device evidence with the pre-authorized fallback path ready
- All ten Phase-4 requirements green across the seven plans — phase ready for /gsd-verify-work
</success_criteria>

## Artifacts this phase produces

*Plan 04-07 symbols later plans consume (exclude from drift verification):*
- `src/components/chart/explore/wheel-a11y-overlay.tsx`: WheelA11yOverlay ({ regions, selection, onSelect, sentences })
- `src/app/chart/explore.tsx`: canvas-hiding wrapper props + overlay mount
- `src/app/chart/result.tsx`: web branch mounting ModeToggle + EvidenceLists + FactPanel from the in-memory envelope (the D-04 web delivery; WEB-01 v2 swaps in the canvas here)
- `src/__tests__/wheel-a11y-parity.test.tsx` + `src/__tests__/explore-web.test.tsx`: parity/web conformance suites (Phase 10 reuses the parity pattern)

<output>
Create `.planning/phases/04-semantic-chart-exploration/04-07-SUMMARY.md` when done
</output>
