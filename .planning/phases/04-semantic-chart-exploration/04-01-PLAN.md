---
phase: 04-semantic-chart-exploration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/vitest/skia-facade/index.ts
  - vitest.config.ts
  - src/app/_layout.tsx
  - src/test/fixtures/unknown-time-envelope.json
  - src/lib/chart-wheel/glyphs.ts
  - src/lib/chart-wheel/geometry.ts
  - src/lib/chart-wheel/collision.ts
  - src/lib/chart-wheel/geometry.test.ts
  - src/lib/chart-wheel/collision.test.ts
autonomous: false
requirements: [WHEEL-01, WHEEL-03]
must_haves:
  truths:
    - "package.json pins @shopify/react-native-skia at the Expo SDK 57 manifest version 2.6.2 (tilde pin via npx expo install — never npm-latest 2.11.1), installed only after the human legitimacy checkpoint approves (T-04-SC)"
    - "The full vitest suite passes with @shopify/react-native-skia aliased to a no-op facade — no test graph loads CanvasKit (facade law, 03-01/03-08 pattern)"
    - "buildWheelGeometry(frozen-natal-envelope.json) emits deterministic primitives: anchor at the 1st-house cusp absolute longitude, anchor angle at π (9 o'clock), 12 sign spokes at 30° spacing, ring radii 330/302/252/210/130 at base size 720 (WHEEL-01, vendor port)"
    - "An unknown-time envelope produces NO house lines, NO angle markers, and NO sect/lots geometry while placements and interplanetary aspects still position (Phase-2 D-10 honesty, D-16)"
    - "hitTest resolves every factor kind (planet/sign/house/angle/aspect) in base coordinates, and inverseTransform keeps hit-testing correct at zoom ≠ 1 with a pan offset (WHEEL-03 math)"
    - "The app root wraps content in GestureHandlerRootView — without it every Phase-4 gesture silently no-ops (Pitfall 2)"
  artifacts:
    - path: "src/lib/chart-wheel/geometry.ts"
      provides: "Pure envelope → wheel primitives transform: buildWheelGeometry, anchorLongitude, lonToAngle, polar, hitTest, inverseTransform"
      contains: "buildWheelGeometry"
      min_lines: 120
    - path: "src/lib/chart-wheel/collision.ts"
      provides: "Vendor declutter algorithm ported and parameterized (min angular distance, radius step, max level) + zoom tier mapping"
      contains: "declutter"
    - path: "src/lib/chart-wheel/glyphs.ts"
      provides: "SIGN_GLYPHS/PLANET_GLYPHS vocabularies keyed by envelope strings, fallback abbreviations, ASPECT_STYLES (stroke pattern + weight per family — never hue-only)"
      contains: "PLANET_GLYPHS"
    - path: "scripts/vitest/skia-facade/index.ts"
      provides: "No-op Skia surface for the vitest graph (Canvas, Group, shapes, Text, matchFont, Skia, DashPathEffect)"
      contains: "Canvas"
    - path: "src/test/fixtures/unknown-time-envelope.json"
      provides: "Frozen Unknown-confidence envelope shape (no house_cusps/ascendant/midheaven/sect/lots) for geometry golden tests"
      contains: "birth_time_confidence"
  key_links:
    - from: "src/lib/chart-wheel/geometry.test.ts"
      to: "src/test/fixtures/frozen-natal-envelope.json"
      via: "golden numeric fixtures asserted against the frozen Timed envelope"
      pattern: "frozen-natal-envelope"
    - from: "vitest.config.ts"
      to: "scripts/vitest/skia-facade/index.ts"
      via: "resolve alias for @shopify/react-native-skia beside the expo-device facades"
      pattern: "skia-facade"
---

<objective>
Install the phase's single new native dependency safely and port the authoritative wheel geometry from `vendor/astrology-skill/tools/chart_diagram.py` into a pure, renderer-agnostic TypeScript module with golden numeric fixtures.

Purpose: The geometry module is the phase's load-bearing seam — the Skia canvas (04-03), the mini-wheel preview (D-03), the a11y overlay (D-12), and the entire numeric test surface all consume the same deterministic primitives (STACK "Chart Wheel Strategy" split). Nothing user-visible mounts without it; this is the documented Wave-1 foundation deviation (same shape as Phases 2–3 Wave 1).
Output: Skia 2.6.2 installed behind a human legitimacy checkpoint, skia test facade + vitest alias, GestureHandlerRootView at the app root, and `src/lib/chart-wheel/` (geometry + collision + glyphs) with green golden-fixture tests.
</objective>

## Phase Goal (user story — slice 1 of 7)

**As a** LemAstra developer, **I want to** the chart wheel's geometry computed by one pure, deterministically tested module and the Skia dependency installed at the Expo-pinned version, **so that** every later surface (canvas, preview, overlay, tests) renders identical primitives without pulling CanvasKit into vitest.

*(Derived from ROADMAP Phase 4 Goal + WHEEL-01/WHEEL-03; foundation slice per the Phase 2/3 wave-shape precedent.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-semantic-chart-exploration/04-PATTERNS.md

- 04-RESEARCH.md §Standard Stack, §Package Legitimacy Audit, §Pattern 3 (geometry), §Pattern 4 (zoom math), §Code Examples, §Pitfalls 1–5, §"Wave 0 Gaps"
- vendor/astrology-skill/tools/chart_diagram.py — THE authoritative geometry to port (read lines 43–104 glyph vocabularies, 284–285 polar, 337–354 radii/anchor, 455–486 chords/collision)
- scripts/vitest/expo-device-facades/sharing.ts — the committed-facade convention to mirror
- vitest.config.ts (alias block ~lines 104–125), src/app/_layout.tsx (root layout), src/lib/api-schemas.ts lines 216–370 (envelope input types)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Dependency-legitimacy gate for @shopify/react-native-skia (T-04-SC)</name>
  <what-built>
Research verdict for the phase's only new package is [SUS] ("too-new" heuristic: latest publish 2026-08-23). Mitigating evidence already gathered: Expo officially lists it as an SDK 57 third-party library with `inExpoGo: true`; Shopify-maintained canonical repo; 1,365,750 weekly downloads; empty install scripts; and the version we will install (2.6.2) is selected by Expo's own bundledNativeModules.json manifest, not by us. House convention (03-09 precedent) requires a human to approve before the install task runs.
  </what-built>
  <how-to-verify>
1. Open https://www.npmjs.com/package/@shopify/react-native-skia — confirm: repository links to github.com/Shopify/react-native-skia, no deprecation or security-advisory banner, weekly downloads in the ~1.3M range, scripts section shows no postinstall/install hooks.
2. Open https://docs.expo.dev/versions/v57.0.0/sdk/skia — confirm it is documented as an Expo SDK 57 library and the install command selects 2.6.2.
3. Locally: `node -e "console.log(require('./node_modules/expo/bundledNativeModules.json')['@shopify/react-native-skia'])"` — expect `2.6.2`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the Expo-pinned install in Task 2, or describe concerns to stop planning-safe.</resume-signal>
  <files>.planning/phases/04-semantic-chart-exploration/04-01-SUMMARY.md</files>
  <action>Present this checkpoint to the user and pause plan execution. Surface the npm URL, the Expo docs URL, and the audit's SUS-with-mitigating-evidence analysis. After the resume signal, record the decision verbatim (approval or concerns) in the plan SUMMARY before starting Task 2.</action>
  <verify>
    <automated>grep -q -i 'approved\|checkpoint' .planning/phases/04-semantic-chart-exploration/04-01-SUMMARY.md</automated>
  </verify>
  <done>Human decision recorded in the SUMMARY; install proceeds only on approval (T-04-SC closed).</done>
</task>

<task type="auto">
  <name>Task 2: Expo-pinned Skia install + skia test facade + vitest alias + GestureHandlerRootView</name>
  <files>package.json, scripts/vitest/skia-facade/index.ts, vitest.config.ts, src/app/_layout.tsx</files>
  <read_first>
  - 04-RESEARCH.md §"Installation" + §Pitfall 2 (GestureHandlerRootView) + §Pitfall 3 (never npm-latest)
  - scripts/vitest/expo-device-facades/sharing.ts (facade header law: WHY comment + per-file vi.mock precedence note + surface = exactly what consumers import)
  - vitest.config.ts alias block (lines ~104–125 — add the skia alias beside expo-sharing)
  - src/app/_layout.tsx (current root layout being wrapped)
  </read_first>
  <action>
Run `npx expo install @shopify/react-native-skia` (NEVER `npm install` — Pitfall 3: npm-latest 2.11.1 breaks Expo Go). Confirm package.json records the tilde pin `~2.6.2`. Create scripts/vitest/skia-facade/index.ts as a no-op TS facade exporting exactly the surface Phase-4 components will import: Canvas, Group, Circle, Line, Path, Text, matchFont, Skia, DashPathEffect (benign defaults; per-file vi.mocks keep precedence per facade law). Add the vitest resolve alias `/^@shopify\/react-native-skia$/ → scripts/vitest/skia-facade/index.ts` beside the expo-sharing alias. In src/app/_layout.tsx import GestureHandlerRootView from react-native-gesture-handler and wrap the QueryProvider content (inside ThemeProvider) with `<GestureHandlerRootView style={{ flex: 1 }}>` — zero RNGH usage exists today, so existing screens are unaffected.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
  - package.json contains `"@shopify/react-native-skia": "~2.6.2"`
  - vitest.config.ts contains the alias `find: /^@shopify\/react-native-skia$/` with replacement pointing at `scripts/vitest/skia-facade/index.ts`
  - src/app/_layout.tsx contains `GestureHandlerRootView`
  - `npm test` exits 0 (full Phase 1–3 suite unaffected) and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Skia installed at the Expo pin behind an approved checkpoint; test graphs provably CanvasKit-free; gestures have their required root view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure geometry module — golden numeric fixtures first, then the vendor port</name>
  <files>src/test/fixtures/unknown-time-envelope.json, src/lib/chart-wheel/glyphs.ts, src/lib/chart-wheel/geometry.ts, src/lib/chart-wheel/collision.ts, src/lib/chart-wheel/geometry.test.ts, src/lib/chart-wheel/collision.test.ts</files>
  <read_first>
  - vendor/astrology-skill/tools/chart_diagram.py (THE port source: lines 43–104 glyph vocabularies, 284–285 _polar/lon_to_angle, 337–354 cx/cy/scale/ring radii/anchor, 455–457 aspect chord anchors, 464–486 collision algorithm)
  - 04-RESEARCH.md §Pattern 3 + §Pattern 4 + §"Code Examples" (lonToAngle/polar/anchorLongitude TypeScript shapes, hit-region taxonomy, aspectChordHit distance threshold)
  - src/lib/api-schemas.ts lines 216–370 (ChartData/Placement/Aspect/HouseCusp/Lot/Sect input types; optional-key semantics per unknown-time)
  - src/test/fixtures/frozen-natal-envelope.json (Timed golden source — anchor cusp 254.25° Sagittarius)
  - src/lib/workspace/revision-diff.ts (pure-module convention: zero RN/Skia imports, header doc-comment, trivially unit-testable)
  </read_first>
  <behavior>
  - anchorLongitude(frozen chart) === 254.25 (1st-house cusp absolute_degree); lonToAngle(anchor, anchor) === Math.PI (anchor at 9 o'clock); longitudes increase counter-clockwise
  - At size 720: outer rim r=330, sign band 302→252, planet ring 210, aspect-chord circle 130; all radii scale linearly with size
  - 12 sign spokes at 30° intervals from the anchor; sign glyph anchors at mid-sign (+15°); Asc/Dsc markers at asc/asc+180 and MC/IC at mc/mc+180 absolute longitudes
  - Aspect chords join the two bodies' positions projected onto the aspect circle via lonToAngle+polar, indexed to the envelope aspects array order
  - Unknown-time fixture (no house_cusps/ascendant/midheaven/sect/lots): anchorLongitude === 0, geometry emits NO house lines/angle markers/sect/lots primitives, placements + aspects still position
  - hitTest: a point at a planet anchor resolves that planet; a point in the sign band between two spokes resolves that sign; a point inside a house's annulus sector resolves that house (Timed fixture); a point within an angle marker's hit circle resolves that angle — all four region kinds plus chords, so every FactorRef kind is hit-testable; a point within the 12px chord threshold resolves the aspect; topmost z-order wins overlaps
  - inverseTransform round-trips: for scale 2.7 and offset {x:40,y:-60} with origin at wheel center, hitTest(inverseTransform(screenPoint)) resolves the same factor as base coordinates (Pitfall 5)
  - declutter: no two same-radius-level glyph anchors sit within min angular distance; min distance shrinks as the scale parameter grows (zoom parameterization); tierForScale maps 1.0→base, mid, high tiers monotonically
  - ASPECT_STYLES: each aspect family carries stroke pattern ∈ {solid, dashed, dotted} + strokeWidth; the (pattern, weight) pair is pairwise distinct across families — hue is never the only differentiator (A11Y-02)
  </behavior>
  <action>
Write geometry.test.ts + collision.test.ts FIRST against numeric expectations derived from the two fixtures (RED), then implement. glyphs.ts: SIGN_GLYPHS keyed by envelope `sign` strings verbatim (♈–♓), PLANET_GLYPHS including Sun..Pluto, True Node/North Node/South Node/Chiron/Lilith keys, per-body fallback abbreviations (the A1 tofu path), ASPECT_STYLES keyed by lowercase envelope aspect names. geometry.ts: WheelGeometry builder + anchorLongitude/lonToAngle/polar + FactorRef discriminated union ({kind:'planet',body}|{kind:'sign',sign}|{kind:'house',house}|{kind:'angle',which:'asc'|'mc'|'dsc'|'ic'}|{kind:'aspect',index}) + hit regions (annulus sectors for signs/houses, circles for planets/angles, thick segments for chords with point-to-segment distance) + hitTest + inverseTransform (pure math, unit-testable per Pitfall 5) + MIN_ZOOM=1/MAX_ZOOM=4 constants; provisional_factors bodies flagged on their anchors (D-16 dashed-treatment input). collision.ts: vendor algorithm ported with named parameters (minAngularDistance defaulting to 12°, radiusStep 24·scale, maxLevel 4) + tierForScale. Emit radians everywhere (Pitfall 1). NO React/RN/Skia imports in this module (plain-Node vitest law).
  </action>
  <verify>
    <automated>npx vitest run src/lib/chart-wheel/ && npm test</automated>
  </verify>
  <acceptance_criteria>
  - src/lib/chart-wheel/geometry.ts contains `export function buildWheelGeometry`, `export function lonToAngle`, `export function hitTest`, `export function inverseTransform`
  - src/lib/chart-wheel/collision.ts contains `export function declutter` and `export function tierForScale`
  - src/lib/chart-wheel/glyphs.ts contains `PLANET_GLYPHS` and `ASPECT_STYLES`
  - `npx vitest run src/lib/chart-wheel/` exits 0 with golden-fixture, unknown-time-omission, zoomed-hit-testing, and declutter-invariant cases green
  - No import of `react-native` or `@shopify/react-native-skia` exists under src/lib/chart-wheel/ (module purity — verifiable by grep)
  </acceptance_criteria>
  <done>Deterministic geometry ported from the vendored authority, golden-tested numerically (screenshots never the sole assertion), zoom-safe hit-testing math proven at zoom ≠ 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules → app bundle | New native dependency enters the supply chain (the phase's only install) |
| stored envelope → geometry module | Locally validated data consumed by pure math — no new external input path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering/Elevation | @shopify/react-native-skia install | mitigate | Blocking human legitimacy checkpoint (Task 1) verifying npm registry page + Expo SDK 57 docs pin; install ONLY via `npx expo install` (manifest-selected 2.6.2, tilde-pinned); no install scripts verified in research |
| T-04-01 | Tampering | version drift to npm-latest 2.11.1 | mitigate | Acceptance criterion pins the exact `~2.6.2` string in package.json; npm-latest breaks Expo Go native binaries (Pitfall 3) |
| T-04-02 | Information Disclosure | test graph loading CanvasKit | mitigate | Facade alias proves no vitest graph resolves the real package; full-suite run in Task 2 is the evidence |
</threat_model>

<verification>
- npx vitest run src/lib/chart-wheel/ (geometry + collision golden suites)
- npm test && npx tsc --noEmit (full regression + typecheck after install/layout change)
</verification>

<success_criteria>
- WHEEL-01's geometry half and WHEEL-03's math half proven by numeric fixtures from frozen envelopes (Timed + Unknown)
- The ONE new dependency is installed at the Expo pin with a recorded human approval
- Every later Phase-4 plan consumes this module — no second geometry implementation anywhere
</success_criteria>

## Artifacts this phase produces

*Plan 04-01 symbols later plans consume (exclude from drift verification):*
- `src/lib/chart-wheel/geometry.ts`: buildWheelGeometry, anchorLongitude, lonToAngle, polar, hitTest, inverseTransform, FactorRef, WheelGeometry, MIN_ZOOM, MAX_ZOOM
- `src/lib/chart-wheel/collision.ts`: declutter, tierForScale (+ named parameters MIN_ANGULAR_DISTANCE/RADIUS_STEP/MAX_LEVEL constants)
- `src/lib/chart-wheel/glyphs.ts`: SIGN_GLYPHS, PLANET_GLYPHS, SIGN_FALLBACKS, PLANET_FALLBACKS, ASPECT_STYLES
- `scripts/vitest/skia-facade/index.ts`: no-op Canvas/Group/Circle/Line/Path/Text/matchFont/Skia/DashPathEffect surface
- `src/test/fixtures/unknown-time-envelope.json`: frozen Unknown-confidence envelope fixture
- `vitest.config.ts`: @shopify/react-native-skia alias entry
- `src/app/_layout.tsx`: GestureHandlerRootView wrapper

<output>
Create `.planning/phases/04-semantic-chart-exploration/04-01-SUMMARY.md` when done
</output>
