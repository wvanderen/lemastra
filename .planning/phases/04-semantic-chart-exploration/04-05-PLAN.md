---
phase: 04-semantic-chart-exploration
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - src/components/chart/explore/wheel-canvas.tsx
  - src/components/chart/explore/copy.ts
  - src/lib/chart-wheel/collision.ts
  - src/__tests__/wheel-zoom.test.tsx
  - src/lib/chart-wheel/collision.test.ts
autonomous: true
requirements: [WHEEL-03]
must_haves:
  truths:
    - "Pinch-zoom and pan the wheel canvas via RNGH Gesture.Simultaneous(Pan, Pinch) driving Reanimated shared values into the Skia Group transform — gesture frames never re-render React (D-11 law)"
    - "Tap hit-testing stays correct at any zoom/pan: pointer coordinates inverse-transform through the live {translate, scale, origin} view before polar hit-testing (Pitfall 5, WHEEL-03)"
    - "Zoom is clamped (1–4×) and pan is clamped so the wheel cannot be lost off-canvas; the Group transform carries origin = wheel center (Skia origin defaults top-left — Pitfall 1)"
    - "Label declutter responds to zoom: base tier shows glyphs only, higher tiers add degree labels then finer detail, reusing the vendor collision algorithm parameterized by scale (discretion tiers, A4)"
    - "Wheel pan does not fight the page ScrollView — RNGH activation tuning or external-gesture coordination keeps page scrolling working outside the canvas (Pitfall 6)"
  artifacts:
    - path: "src/components/chart/explore/wheel-canvas.tsx"
      provides: "Zoom/pan shell: shared values, saved values, clamped transforms, tap-through-inverse-transform, declutter tier wiring"
      contains: "useSharedValue"
      min_lines: 40
    - path: "src/lib/chart-wheel/collision.ts"
      provides: "tierForScale consumed live; any tier-threshold refinement stays parameterized (named constants)"
      contains: "tierForScale"
  key_links:
    - from: "src/components/chart/explore/wheel-canvas.tsx"
      to: "src/lib/chart-wheel/geometry.ts"
      via: "tap coordinates → inverseTransform(current view) → hitTest — the pure math proven in 04-01, now fed by live shared values"
      pattern: "inverseTransform"
    - from: "src/components/chart/explore/wheel-canvas.tsx"
      to: "src/lib/chart-wheel/collision.ts"
      via: "declutter tier re-selection when scale crosses tier thresholds (labels re-render on tier change only, not per frame)"
      pattern: "tierForScale"
---

<objective>
Complete WHEEL-03: pinch-zoom + pan inspection of the wheel with zoom-true hit-testing and tiered label declutter.

Purpose: Dense regions (clustered planets, overlapping chords) must be inspectable without losing access to overlapping factors — D-11's locked interaction. All zoom math was proven pure in 04-01; this plan wires it to live gestures on the UI thread.
Output: Zoom/pan gesture shell in WheelCanvas, tiered declutter driven by live scale, zoom hint copy, green zoom tests.
</objective>

## Phase Goal (user story — slice 5 of 7)

**As a** LemAstra user, **I want to** pinch-zoom and pan the wheel when planets crowd together, **so that** I can tap exactly the factor I mean and read finer labels at higher zoom.

*(Derived from ROADMAP Phase 4 Goal + Success Criteria 2.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-semantic-chart-exploration/04-PATTERNS.md
@.planning/phases/04-semantic-chart-exploration/04-03-SUMMARY.md

- 04-RESEARCH.md §Pattern 4 (the verified gesture/zoom shell: savedScale/savedOffset, clamp, Group transform with origin, tap composition) + §Pitfalls 1/5/6 + Assumption A4 (clamp/tier starting values are named constants)
- src/components/chart/explore/wheel-canvas.tsx (the component being extended — Tap-only today), src/components/animated-icon.tsx (Reanimated-4/worklet import posture)
- src/lib/chart-wheel/{geometry.ts, collision.ts} + their tests (the consumed pure math), src/components/chart/explore/copy.ts (deck to extend)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pinch/pan gesture shell — shared values, clamps, transform origin, zoom-true taps</name>
  <files>src/components/chart/explore/wheel-canvas.tsx</files>
  <read_first>
  - 04-RESEARCH.md §Pattern 4 code excerpt (Gesture.Pan/Pinch onUpdate/onEnd with saved values; Gesture.Simultaneous composition; Group transform translateX/translateY/scale with origin) — doc-verified this research session
  - src/components/chart/explore/wheel-canvas.tsx (current Tap-only implementation and its test seam)
  - src/lib/chart-wheel/geometry.ts (inverseTransform signature + MIN_ZOOM/MAX_ZOOM constants from 04-01)
  - src/components/animated-icon.tsx (Reanimated 4 + worklets import style under Expo 57)
  </read_first>
  <action>
Extend WheelCanvas with the Pattern-4 shell: useSharedValue scale/offset + savedScale/savedOffset; Gesture.Pan().onUpdate/onEnd and Gesture.Pinch().onUpdate/onEnd exactly per the excerpt, scale clamped to [MIN_ZOOM, MAX_ZOOM] (1–4), pan offset clamped so the wheel center stays within the canvas bounds; compose Gesture.Simultaneous(pan, pinch) with the existing Tap. Render primitives inside a Skia Group with transform [{translateX},{translateY},{scale}] and origin={{ x: cx, y: cy }} (wheel center — Pitfall 1; radians only). Tap handler: read the live view (shared .value is JS-readable in the tap callback) → inverseTransform(pointer, view) → hitTest → onSelect — no re-render per gesture frame (D-11). Tune RNGH activation so the parent ScrollView keeps working outside the canvas (small activeOffsetX/-Y, or simultaneousWithExternalGesture with the page scroll ref — discretion; on-device checkpoint in 04-07 verifies the feel). Worklet callbacks carry the 'worklet' directive per the animated-icon posture.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/wheel-selection.test.tsx && npm test</automated>
  </verify>
  <acceptance_criteria>
  - wheel-canvas.tsx contains `useSharedValue`, `Gesture.Simultaneous`, `Gesture.Pinch`, `Gesture.Pan`, and `origin` on the transform Group
  - The tap path routes through `inverseTransform` with the live scale/offset values before hitTest (source assertion)
  - Existing wheel-selection tests stay green (tap behavior unchanged at identity view); npm test green
  </acceptance_criteria>
  <done>Pinch/pan drive UI-thread transforms with clamps; selection provably works zoomed (math) and unchanged at rest (component).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tiered label declutter at zoom + zoom hint copy</name>
  <files>src/lib/chart-wheel/collision.ts, src/components/chart/explore/wheel-canvas.tsx, src/components/chart/explore/copy.ts, src/__tests__/wheel-zoom.test.tsx, src/lib/chart-wheel/collision.test.ts</files>
  <read_first>
  - src/lib/chart-wheel/collision.ts + collision.test.ts (tierForScale + parameterized declutter from 04-01 — extend thresholds only as named constants)
  - 04-RESEARCH.md §"Open Questions" 2 (tiers are named constants tuned at on-device UAT — tests assert behavior, not constants) + Assumption A4
  - src/components/chart/explore/copy.ts (deck law — the zoom hint string lands here)
  </read_first>
  <behavior>
  - collision.test.ts additions: tierForScale is monotonic non-decreasing across [1,4]; declutter with a shrunk min-angular-distance (as tier rises) positions a previously-bumped glyph at a lower radius level (denser packing at zoom)
  - component test (facade): label sets differ across tiers — base tier renders glyph labels only; mid tier adds degree labels; high tier adds the finest label set; tier changes only when scale crosses a threshold (not per frame — labels recompute on tier transitions)
  - the zoom hint string renders once near the canvas from the deck (exact-string pin)
  </behavior>
  <action>
Extend collision.test.ts first (RED), then adjust collision.ts tier constants/parameterization if needed (behavior-parameterized, not hardcoded per call site). Wire WheelCanvas to recompute labels via tierForScale on tier transitions (derive current tier from the scale shared value in the tap/change path — React state holds the TIER, never the per-frame scale). Add the zoom-hint deck string + render. wheel-zoom.test.tsx covers tier-set differences and the hint pin.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/wheel-zoom.test.tsx src/lib/chart-wheel/collision.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
  - collision.test.ts exits 0 with monotonicity + denser-packing-at-zoom cases
  - wheel-zoom.test.tsx exits 0 with distinct label sets per tier and the exact zoom-hint string
  - wheel-canvas.tsx holds tier (not scale) in React state — verifiable: no setState inside Pan/Pinch onUpdate callbacks (source assertion)
  </acceptance_criteria>
  <done>Dense regions become inspectable: zoom reveals more labels, declutter packs tighter, and taps stay exact (WHEEL-03 complete).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesture stream → shared values → canvas transform | Local UI only; no data crosses a trust boundary (envelope is render input, never mutated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | DoS | gesture-driven React re-render storms degrading the surface | mitigate | Shared values on the UI thread; React state holds only selection/mode/tier — asserted by source review of callback bodies |
| T-04-11 | Tampering | zoom state desync breaking selection | mitigate | Single inverseTransform source of truth (pure, unit-tested at zoom ≠ 1 since 04-01); tap path reads the same shared values the transform uses |
</threat_model>

<verification>
- npx vitest run src/__tests__/wheel-zoom.test.tsx src/lib/chart-wheel/collision.test.ts src/__tests__/wheel-selection.test.tsx
- npm test (full regression)
</verification>

<success_criteria>
- WHEEL-03 complete: zoom/pan with working hit-testing at any zoom and tiered declutter
- D-11 honored: no React re-render per gesture frame; no bespoke focus mode
- On-device gesture feel deferred to the 04-07 checkpoint (simulator cannot validate touch feel)
</success_criteria>

## Artifacts this phase produces

*Plan 04-05 symbols later plans consume (exclude from drift verification):*
- `src/components/chart/explore/wheel-canvas.tsx`: zoom/pan shell (shared values + clamps + tier state) — 04-06 reads the tier/mode seam for label depth
- `src/lib/chart-wheel/collision.ts`: tier threshold constants (named, parameterized)
- `src/components/chart/explore/copy.ts`: zoom hint string

<output>
Create `.planning/phases/04-semantic-chart-exploration/04-05-SUMMARY.md` when done
</output>
