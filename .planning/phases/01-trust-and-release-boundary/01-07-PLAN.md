---
phase: 01-trust-and-release-boundary
plan: 07
type: execute
wave: 4
depends_on: ["01-02", "01-05", "01-06"]
files_modified:
  - .github/workflows/ci.yml
  - docs/governance/swiss-ephemeris-posture.md
  - docs/governance/data-inventory.md
  - docs/governance/retention-deletion-policy.md
  - docs/governance/secret-isolation-policy.md
  - .planning/STATE.md
autonomous: false
requirements: [GATE-01, GATE-05, GATE-06]
must_haves:
  truths:
    - "CI runs the full test suite, a full-history secret scan, and an exported-bundle secret scan — all exit-nonzero-on-findings"
    - "Every governance document carries an approval record naming the approver and date"
    - "The posture's approval record shows qualified review scheduled before public/commercial beta"
    - "STATE.md Phase 1 blockers reflect the recorded approvals and remaining scheduled items"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI gates: vitest + tsc, gitleaks history scan, bundle scan"
      contains: "bundle-scan"
    - path: "docs/governance/swiss-ephemeris-posture.md"
      provides: "Posture with countersigned approval record"
      contains: "Approved"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".gitleaks.toml"
      via: "CI gitleaks invocations use the repo config (custom expo-public-secret-name rule applies in CI)"
      pattern: "gitleaks"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm ci + vitest + tsc + expo export use the installed toolchain"
      pattern: "vitest run"
---

## Phase Goal (user story)

**As a** prospective LemAstra user, **I want to** understand the supported product, its providers, and its privacy posture, **so that** I know what leaves my device before any remote feature is enabled.

Trust gates must outlive the phase that built them — this plan automates enforcement in CI and records human approval (GATE-01/05/06 closure).

<objective>
Wire the CI pipeline (tests + full-history secret scan + exported-bundle scan), run the phase approval checkpoint over the complete governance set, and record approvals in every governance document.

Purpose: GATE-06 requires automated enforcement at commit/build time, not review time; GATE-01/05 require recorded approval with qualified review explicitly scheduled before the beta trigger point.
Output: .github/workflows/ci.yml, countersigned governance docs, updated STATE.md.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trust-and-release-boundary/01-RESEARCH.md
@.planning/phases/01-trust-and-release-boundary/01-PATTERNS.md
@.planning/phases/01-trust-and-release-boundary/01-05-SUMMARY.md
@.planning/phases/01-trust-and-release-boundary/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the CI workflow with all three gates</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-trust-and-release-boundary/01-RESEARCH.md (### gitleaks CI gate with bundle scan)
    - .planning/phases/01-trust-and-release-boundary/01-PATTERNS.md (### CI + .gitleaks.toml)
    - .gitleaks.toml
    - package.json
  </read_first>
  <action>
Create .github/workflows/ci.yml (GitHub Actions) with three jobs:

1. job id `test`: actions/checkout@v4; actions/setup-node@v4 with node-version 22 and npm cache; `npm ci`; `npx tsc --noEmit`; `npx vitest run`.
2. job id `gitleaks`: actions/checkout@v4 with fetch-depth: 0; gitleaks/gitleaks-action@v2 with GITHUB_TOKEN: secrets.GITHUB_TOKEN. Add a comment documenting the fallback: if the action reports a license requirement for this repository, replace it with a step that downloads the gitleaks release binary and runs `gitleaks git . --redact` directly (exit 1 on findings either way).
3. job id `bundle-scan`: actions/checkout@v4; setup-node 22 + npm cache; `npm ci`; `npx expo export --platform web`; then run gitleaks via the release-binary download (CI runners have no brew): `gitleaks dir dist/ --report-path gitleaks-bundle.json --report-format json --redact` — non-zero exit fails the job.

Trigger on push and pull_request. All three jobs fail on findings/errors — no continue-on-error anywhere.

Known coverage note (accepted for greenfield Phase 1): the bundle-scan job scans only the web export, so "either mobile client" (success criterion 4) is approximated via shared `.env` inlining plus the repo/history scans. When platform-conditional code first appears, extend this job with `expo export --platform ios` and `--platform android` outputs.
   </action>
  <verify>
    <automated>node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/ci.yml','utf8');for(const k of ['test:','gitleaks:','bundle-scan:','fetch-depth: 0','vitest run','expo export --platform web','gitleaks dir dist/']){if(!y.includes(k))process.exit(1)}console.log('ci-ok')"</automated>
  </verify>
  <acceptance_criteria>
- ci.yml defines jobs test, gitleaks, bundle-scan
- History scan uses fetch-depth: 0; bundle scan runs expo export then gitleaks dir dist/ with JSON report + redact
- No continue-on-error on any gate step
- License-requirement fallback documented as a comment
- Workflow YAML parses (node structural check passes)
  </acceptance_criteria>
  <done>GATE-06 enforcement is automated: every push runs tests, full-history secret scan, and shipped-bundle scan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase governance approval</name>
  <read_first>
    - docs/governance/swiss-ephemeris-posture.md
    - docs/governance/data-inventory.md
    - docs/governance/retention-deletion-policy.md
    - docs/governance/secret-isolation-policy.md
    - docs/governance/disclosures/apple-labels.md
    - docs/governance/disclosures/play-data-safety.csv
  </read_first>
  <what-built>
The complete Phase 1 governance set: Swiss Ephemeris licensing posture (decision recorded in plan 01-03), data + provider inventory, retention/deletion policy, secret-isolation policy, Apple label worksheet, Play Data-safety CSV, and the gitleaks/CI enforcement. Structural and consistency tests are green.
  </what-built>
  <how-to-verify>
1. Read docs/governance/swiss-ephemeris-posture.md — confirm the recorded license path is the one you intend, and that qualified legal review is acceptably scheduled before any public/commercial beta.
2. Read docs/governance/data-inventory.md and docs/governance/retention-deletion-policy.md — approve the retention decisions (ephemeral compute-and-discard default, log window + redaction, telemetry exclusion).
3. Read docs/governance/disclosures/apple-labels.md and play-data-safety.csv — confirm they match the inventory (the consistency tests already enforce id parity; review the substance of the answers).
4. Read docs/governance/secret-isolation-policy.md — confirm the three-class classification matches your intent.
5. Informational (non-blocking): note whether Apple Developer Program and Google Play Console accounts exist yet (needed only for Phase 10 publication).
  </how-to-verify>
  <resume-signal>Type "approved" to record approvals, or list corrections to apply first</resume-signal>
  <files>.planning/phases/01-trust-and-release-boundary/01-07-SUMMARY.md</files>
  <action>Present the governance set review to the user and pause plan execution. Surface the five review steps and the file paths. After the resume signal, record the outcome (approval or correction list) verbatim in the plan SUMMARY, then proceed to Task 3 which writes the approval records. If corrections are listed, apply them to the affected governance documents first and re-present.</action>
  <verify>
    <automated>grep -q -i 'approved' .planning/phases/01-trust-and-release-boundary/01-07-SUMMARY.md || grep -q -i 'correction' .planning/phases/01-trust-and-release-boundary/01-07-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
- Explicit approval (or correction list) recorded in the plan SUMMARY before Task 3 runs
  </acceptance_criteria>
  <done>Governance set reviewed and approved by the product owner (or corrections applied and re-approved); approval ready to be recorded in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Record approvals and close phase state</name>
  <files>docs/governance/swiss-ephemeris-posture.md, docs/governance/data-inventory.md, docs/governance/retention-deletion-policy.md, docs/governance/secret-isolation-policy.md, .planning/STATE.md</files>
  <read_first>
    - docs/governance/swiss-ephemeris-posture.md (## 5. Approval Record)
    - .planning/STATE.md
  </read_first>
  <action>
Record the approvals from Task 2:

1. docs/governance/swiss-ephemeris-posture.md — in section "## 5. Approval Record": set product approval to Approved with approver name, approval date (today), scope (chosen path + distribution model), and keep qualified legal review as scheduled-before-public/commercial-beta.
2. docs/governance/data-inventory.md, retention-deletion-policy.md, secret-isolation-policy.md — replace each Approval placeholder line with: Approved by {approver} on {date} (product approver-of-record); qualified review scheduled before public/commercial beta where applicable.
3. .planning/STATE.md — update Blockers/Concerns: mark the licensing-posture blocker resolved-to-scheduled (posture approved; qualified review remains scheduled before beta); update the provider-inventory blocker to reflect approvals recorded and store publication deferred to Phase 10.
4. Re-run the governance structural tests to confirm the edited docs still pass (section headings unchanged).
  </action>
  <verify>
    <automated>grep -q 'Approved' docs/governance/swiss-ephemeris-posture.md && grep -q 'Approved' docs/governance/data-inventory.md && grep -q 'Approved' docs/governance/retention-deletion-policy.md && grep -q 'Approved' docs/governance/secret-isolation-policy.md && npx vitest run src/__tests__/governance-docs.test.ts src/__tests__/disclosures-consistency.test.ts</automated>
  </verify>
  <acceptance_criteria>
- All four governance docs contain an Approved line with approver and date
- Posture keeps qualified-review-scheduled-before-beta status (not falsely marked legally reviewed)
- Governance/consistency tests still pass after edits
- STATE.md blockers updated to reflect approvals and scheduled items
  </acceptance_criteria>
  <done>Phase 1's gates are closed: CI enforcement live, approvals recorded honestly (product approval now, qualified legal review scheduled before the beta trigger).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI runner | Workflow files and action pins execute with repo secrets (GITHUB_TOKEN) |
| approval record → legal posture | Approval records must not overstate (qualified review ≠ done) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Tampering | CI gate bypass (continue-on-error, skipped jobs) | mitigate | Plan forbids continue-on-error; acceptance check verifies all three job ids and gate commands present; structural test would catch workflow deletion in later phases if suite extended |
| T-01-16 | Tampering | gitleaks-action supply chain / license friction | mitigate | Official gitleaks-action@v2 used; documented fallback to release-binary invocation keeps the gate functional either way |
| T-01-17 | Repudiation (legal) | Overstated approval (marking legal review done) | mitigate | Task 3 explicitly preserves scheduled-before-beta status; acceptance criteria forbid false completion |
</threat_model>

<verification>
- ci.yml structural check passes (three jobs, gate commands, fetch-depth 0)
- Full local suite green: `npx vitest run && npx tsc --noEmit`
- Approvals recorded in all four governance docs; tests still pass
- STATE.md reflects the closed phase boundary
</verification>

<success_criteria>
- GATE-06 enforced in CI on every push (tests + history scan + bundle scan)
- GATE-01/05 approval records complete and honest (product approval recorded; qualified review scheduled)
- Phase 1 trust boundary established for all later phases to build on
</success_criteria>

<artifacts_produced>
- .github/workflows/ci.yml — CI pipeline with jobs test / gitleaks / bundle-scan (consumed by every later phase)
- Countersigned approval records in the four governance documents
- Updated STATE.md phase-blocker entries
</artifacts_produced>

<output>
Create `.planning/phases/01-trust-and-release-boundary/01-07-SUMMARY.md` when done
</output>
