---
phase: 03-private-local-workspace
plan: 10
type: execute
wave: 8
depends_on: ["03-09"]
files_modified:
  - src/lib/workspace/errors.ts
  - src/lib/workspace/repository.ts
  - src/lib/workspace/db.ts
  - src/lib/redact.ts
  - src/__tests__/redact.test.ts
  - src/__tests__/workspace-db.test.ts
  - src/__tests__/workspace-sync-path.test.ts
autonomous: true
gap_closure: true
requirements: [WORK-02, PRIV-03, PRIV-04]
must_haves:
  truths:
    - "A save (or delete) transaction failure on any device appears in the Metro console through the sanctioned logger with its error_code and the underlying storage-engine message — one save attempt after this plan names the exact exception the UAT device is hitting (UAT Test 1 unblocked diagnostically)"
    - "A device-resident lemastra.db from earlier Phase-03 dev builds self-heals on a dev build: the gate detects migrate failure OR post-migrate schema-shape mismatch, drops the workspace tables + drizzle journal, re-migrates, and saveChart then succeeds — the ranked root cause (1) is closed without manual app deletion"
    - "A gate failure on a production build (no dev flag) throws a typed WorkspaceError OPEN_FAILED — catchable, code-bearing, logged — never a raw untyped error escaping getWorkspaceDb"
    - "The telemetry law stays build-enforced: no new console call sites exist (telemetry-guard.test.ts green), and error_message log metadata can only ever carry storage-engine text because zod/parse failures keep their fixed copy and never route through toWorkspaceError"
    - "A randomUUID failure inside the save transaction surfaces as a logged, typed SAVE_FAILED (never swallowed), and ids produced through the real save path match the UUIDv4 shape contract"
  artifacts:
    - path: "src/lib/workspace/errors.ts"
      provides: "WorkspaceErrorCode + WorkspaceError extracted to a db.ts-importable module (no repository→db import cycle); repository.ts re-exports both so every existing import keeps working"
      contains: "WorkspaceError"
    - path: "src/lib/workspace/repository.ts"
      provides: "toWorkspaceError logs every newly-wrapped engine failure through the sanctioned logger before re-throwing; already-typed WorkspaceErrors pass through unlogged (NOT_FOUND dedupe-adjacent paths stay quiet)"
      contains: "toWorkspaceError"
    - path: "src/lib/workspace/db.ts"
      provides: "Hardened gate: typed OPEN_FAILED wrap + logging, post-migrate PRAGMA table_info shape verification against the drizzle schema, and a dev-build-only drop-and-re-migrate self-heal bounded to one attempt"
      contains: "OPEN_FAILED"
    - path: "src/lib/redact.ts"
      provides: "Allowlist extended with error_message (a deliberate, review-visible act documented in-module: storage-engine-originated failure text only — never user/chart content)"
      contains: "error_message"
    - path: "src/__tests__/workspace-sync-path.test.ts"
      provides: "Sync-path coverage the 415-test suite never had: randomUUID throw → typed + logged SAVE_FAILED; UUIDv4 shape contract through the real ids module"
      contains: "SAVE_FAILED"
  key_links:
    - from: "src/lib/workspace/repository.ts"
      to: "src/lib/redact.ts"
      via: "toWorkspaceError calls the sanctioned logger error method with metadata { error_code, error_message } that redact() filters — the only log seam in src/ (D-16 law)"
      pattern: "logger\\.error"
    - from: "src/lib/workspace/db.ts"
      to: "src/lib/workspace/errors.ts"
      via: "the gate throws typed WorkspaceError OPEN_FAILED on open/migrate/shape failure instead of letting a raw error escape getWorkspaceDb"
      pattern: "WorkspaceError"
    - from: "src/lib/workspace/db.ts"
      to: "src/lib/workspace/schema.ts"
      via: "post-migrate shape verification compares PRAGMA table_info column names against drizzle getTableColumns(schema.charts / schema.chartRevisions) — the check stays correct automatically as the schema evolves"
      pattern: "table_info"
    - from: "src/__tests__/workspace-db.test.ts"
      to: "src/lib/workspace/db.ts"
      via: "seeded stale-device-DB fixtures (old column set + journal row at the current `when`; partial tables without a journal row) prove the dev self-heal and the production typed failure"
      pattern: "resetWorkspaceDbForTests"
---

<objective>
Close the CONFIRMED observability layer of the UAT Test 1 save blocker and harden the DB gate against the ranked remaining cause (device-resident lemastra.db from earlier Phase-03 dev builds), so the next save attempt on the user's dev client either succeeds outright (self-healed DB) or names its exact exception in the Metro console.

Purpose: UAT Test 1 (real restart survival) is blocked — "chart unable to save" with zero console output. The diagnosis (.planning/debug/chart-save-fails.md) proved every JS layer healthy and pinned the silence on toWorkspaceError never logging + the gate's untyped raw throws. This plan makes storage failures observable through the sanctioned telemetry seam and self-heals stale dev databases; without it, UAT tests 1–5 cannot re-run meaningfully on the user's device.

Output: typed+logged storage error boundary (errors.ts + repository.ts), hardened self-healing DB gate (db.ts), one allowlisted log key (redact.ts), and the sync-path test coverage the suite never had (workspace-sync-path.test.ts + workspace-db.test.ts gate scenarios).
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-private-local-workspace/03-UAT.md
@.planning/debug/chart-save-fails.md

# The two laws this plan must not break:
@src/lib/redact.ts
@src/lib/workspace/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed-error extraction + sanctioned logging at the repository error boundary</name>
  <files>src/lib/workspace/errors.ts, src/lib/workspace/repository.ts, src/lib/redact.ts, src/__tests__/redact.test.ts</files>
  <action>
    Gap reason (UAT Test 1, severity blocker): toWorkspaceError (repository.ts:253) wraps but never logs; useSaveChart captures the rejection into isError silently — the exact underlying exception is unobservable in the current build. Missing item 1.

    1. Create src/lib/workspace/errors.ts: move WorkspaceErrorCode and the WorkspaceError class there verbatim (constructor body, name, readonly code). The module imports nothing (fixes the repository→db cycle that currently blocks db.ts from throwing the typed error: repository imports db, so db must not import repository). In repository.ts, replace the inline definitions with a re-export of both from ./errors — every existing import path (tests, screens, hooks import WorkspaceError from @/lib/workspace/repository) keeps working unchanged.

    2. Extend the frozen log allowlist in src/lib/redact.ts: add "error_message" to ALLOWED_KEY_LIST, and extend the block comment above it with the review-visible rationale this act requires per the module's own law: error_message may carry ONLY storage-engine failure text (SQLite/driver/DB-gate messages routed through toWorkspaceError); it must never receive user input, chart payloads, or zod diagnostics — which is structurally guaranteed because parseOrThrow and parseRevisionAtRead catch zod failures and emit fixed developer copy, so zod's value-echoing messages can never reach the wrap boundary. Keep the comment free of any console-method literal (the telemetry guard scans tokens, comments included).

    3. In repository.ts toWorkspaceError: import the sanctioned logger from @/lib/redact and, ONLY on the wrap branch (when error is NOT already a WorkspaceError — NOT_FOUND dedupe-adjacent rethrows stay quiet), call the logger's error method with a developer-authored compile-time message (e.g. "workspace operation failed — underlying storage error") and metadata { error_code: code, error_message: the same underlying message string being preserved on the wrapped error }. Never interpolate the message into the log message argument itself — the engine text rides only in the redact()-filtered metadata.

    4. Extend src/__tests__/redact.test.ts: error_message passes through as a primitive exactly like error_code; error_message with a non-primitive value (object/array) is dropped by the existing default-deny rules; the never-emits-outside-allowlist assertion now includes the new key.

    Constraint (D-16 / 03-02 telemetry law): no new console call sites anywhere — repository.ts logs ONLY through the imported logger; telemetry-guard.test.ts must stay green unmodified (it has no allowlist file by design — do not touch it).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/redact.test.ts src/__tests__/telemetry-guard.test.ts src/__tests__/workspace-repository.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>WorkspaceError lives in errors.ts and is re-exported from repository.ts (all 415 existing tests import-path-compatible); toWorkspaceError logs every newly-wrapped engine failure through the sanctioned logger with error_code + error_message metadata; already-typed errors pass through unlogged; redact allowlist carries error_message with its documented safety invariant; telemetry guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the DB gate — typed OPEN_FAILED, shape verification, dev-build self-heal</name>
  <files>src/lib/workspace/db.ts, src/lib/workspace/errors.ts</files>
  <action>
    Gap reason: ranked root cause (1) — device-resident lemastra.db from earlier dev builds conflicts with drizzle/0000_nebulous_meggan.sql (CREATE TABLE/CREATE UNIQUE INDEX lack IF NOT EXISTS; a journal row at the current `when` makes drizzle skip re-migration over a stale table shape), and gate failures currently propagate as raw untyped errors (saveChart's await getWorkspaceDb sits outside its try/catch). Missing item 2. Do NOT edit the migration SQL or drizzle/meta — robustness lives in the gate, so no migration regeneration is needed.

    Rework getWorkspaceDb's init in src/lib/workspace/db.ts:

    1. TYPED GATE FAILURES: import WorkspaceError from ./errors (Task 1). Any failure of the gate (open, migrate, shape verification) is logged via the sanctioned logger (error_code OPEN_FAILED + error_message with the underlying text, same metadata contract as Task 1) and rethrown as WorkspaceError { code: "OPEN_FAILED", message: fixed developer copy naming the stage that failed } — callers (repository functions, hooks, error cards) receive a typed, code-bearing error instead of a raw throw. Keep the existing memo-clearing on failure (dbPromise = null → next call retries the gate) and close the client handle on the hard-failure path (guarded so a close error cannot mask the original failure).

    2. SHAPE VERIFICATION after migrate(): compare PRAGMA table_info column NAME SETS (order-independent) for the two personal tables against drizzle's getTableColumns(schema.charts) / getTableColumns(schema.chartRevisions) (import getTableColumns and getTableName from drizzle-orm; schema is already imported). Read PRAGMA through the raw client's prepareSync(...).executeSync().getAllSync() rows' name field — this works identically on expo-sqlite and the node:sqlite vitest facade. A missing OR unexpected column is a mismatch. Deriving expected names from schema.ts means future migrations keep the check correct with zero maintenance.

    3. DEV-BUILD SELF-HEAL: on ANY gate failure or shape mismatch, when the dev flag is active (guard with a runtime-safe check — typeof __DEV__ is not undefined AND truthy, because __DEV__ is a RN global that does not exist under plain-Node vitest) AND a client handle was obtained: drop, in foreign-key-safe order, chart_revisions, then charts, then the drizzle journal table __drizzle_migrations (DROP TABLE IF EXISTS via the client's execSync, one statement per call — deliberately NOT file deletion: the vitest facade has no deleteDatabaseSync, and execSync drops work identically on device). Then re-run migrate() on the SAME drizzle instance and re-verify the shape. Bound the heal to ONE attempt: a second failure logs and throws typed OPEN_FAILED as in (1). Log the heal itself at the logger's warn level with a compile-time message and a count metadata field so healed devices are visible in diagnostics. Production builds (dev flag false): NO wipe ever — go straight to the typed OPEN_FAILED failure of (1). The wipe spares everything outside SQLite (AsyncStorage disclosure flags untouched — same sparing semantics as deleteAllData); pre-release, no production users exist, and dev-build data loss to a stale schema is the honest recovery.

    4. openDatabaseSync itself failing: no client exists — skip the heal entirely, log + typed OPEN_FAILED directly.

    Keep the module's documented contract intact: memoized promise, migration gate before any query, resetWorkspaceDbForTests unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/workspace-db.test.ts src/__tests__/telemetry-guard.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>getWorkspaceDb never lets a raw error escape: gate failures are logged WorkspaceError OPEN_FAILED; a post-migrate PRAGMA shape check compares actual columns to the drizzle schema; dev builds self-heal stale/partial databases once via FK-ordered drops + re-migrate (observable as a saveChart succeeding against a previously-conflicting DB); production builds fail typed without wiping; the memo-clear retry semantics are preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Gate self-heal + sync-path test coverage (the untested native-path seams)</name>
  <files>src/__tests__/workspace-db.test.ts, src/__tests__/workspace-sync-path.test.ts</files>
  <action>
    Gap reason: ranked remaining causes (1) and (2) — the dev-client sync-path primitives (expo-crypto randomUUID, the transaction insert path) and stale-device-DB states were never exercised by tests; missing item 3. vitest cannot reach the device native path — this task pins everything reachable at the seams we can test and proves the self-heal end to end on the file-backed node:sqlite facade.

    1. Extend src/__tests__/workspace-db.test.ts with three gate scenarios (reuse the file's existing fixture/open patterns: resetWorkspaceDbForTests, SQLite.openDatabaseSync(WORKSPACE_DB_NAME), facade handle closeSync before reopening through the gate; stub the dev flag with vi.stubGlobal and unstub in teardown):
    a. STALE SHAPE, JOURNAL PRESENT (the UAT device scenario): seed the DB with an OLD-shaped chart_revisions (e.g. missing the identity_date / identity_place_label summary columns) + a charts table + a __drizzle_migrations row whose created_at equals the committed journal's `when` value (so drizzle's migrator skips re-applying). With the dev flag on: getWorkspaceDb resolves, the shape check detects the mismatch, the heal drops + re-migrates, and a repository saveChart then succeeds and reads back. Same fixture with the dev flag OFF: getWorkspaceDb rejects with WorkspaceError code OPEN_FAILED and the underlying detail is observable via the sanctioned logger spy.
    b. PARTIAL MIGRATION, JOURNAL ABSENT: seed tables but no journal row (migration died mid-way on device). Dev flag on → heal recovers; saveChart succeeds.
    c. HEAL BOUNDED: a DB that fails even after the drop + re-migrate (e.g. make the second migrate throw by corrupting the journal table shape beyond the drop list if feasible with the facade; if not feasible cleanly, drive the gate's failure path directly by stubbing the migrate module to throw twice) → typed OPEN_FAILED, exactly one heal attempt observed via the logger spy.
    2. Create src/__tests__/workspace-sync-path.test.ts (new file; repository tests mock expo-crypto at the package boundary with node:crypto — follow that convention):
    a. UUIDv4 CONTRACT: run repository saveChart against the real ids module with expo-crypto mocked to node:crypto randomUUID; assert the persisted chart_id and revision_id (read back through getChartDetail) match the canonical v4 shape (8-4-4-4-12 hex, version-nibble 4) — pins the id contract the future sync path relies on, through the real drizzle insert path.
    b. randomUUID THROWS: mock expo-crypto randomUUID to throw a named engine-style error; saveChart rejects with WorkspaceError code SAVE_FAILED AND the sanctioned logger's error method was called with metadata carrying error_code SAVE_FAILED and the underlying message in error_message (spy on the logger object exported from @/lib/redact) — the exact class of device failure cause (b) is now named, not swallowed.
    c. ALREADY-TYPED PASSES QUIET: a NOT_FOUND (append under an unknown chartId) does NOT trigger a logger error call — the dedupe-adjacent vocabulary stays log-noise-free (Task 1's only-on-wrap rule).
    d. Full round-trip on the facade: save → close → reopen → listCharts + getChartDetail return the identical envelope/inputs/identity (restart survival pre-UAT, through the same gate the device runs).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/workspace-db.test.ts src/__tests__/workspace-sync-path.test.ts src/__tests__/workspace-repository.test.ts src/__tests__/telemetry-guard.test.ts</automated>
  </verify>
  <done>Stale-shape-with-journal and partial-migration DB fixtures both self-heal under the dev flag and fail typed OPEN_FAILED without it; the heal is bounded to one attempt; a randomUUID failure surfaces as typed + logged SAVE_FAILED; persisted ids satisfy the v4 contract through the real save path; NOT_FOUND stays unlogged; the whole suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage engine → log output | underlying engine error text crosses from SQLite/driver into device-visible logs (Metro console now; Phase 7+ telemetry later) |
| on-device DB state → app logic | pre-existing device database content (from earlier dev builds) influences gate behavior including a destructive self-heal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Information Disclosure | toWorkspaceError logging / redact allowlist | mitigate | Engine text reaches logs only as redact()-filtered metadata (error_message primitive); zod/parse failures keep fixed copy and structurally never reach the wrap boundary — no birth data, chart payload, or prose can ride the new key; telemetry-guard.test.ts stays the fail-hard scan |
| T-03-10-02 | Tampering | dev-build self-heal (drop + re-migrate) | mitigate | Wipe is dev-flag-only (runtime-safe __DEV__ check), bounded to one attempt, limited to the two personal tables + drizzle journal via FK-ordered DROP IF EXISTS; production builds NEVER wipe — typed OPEN_FAILED instead; heal is logged at warn level for visibility |
| T-03-10-03 | Denial of Service | DB gate on a corrupt production DB | accept | Production fails honestly with a typed, logged OPEN_FAILED surfaced to error cards — pre-release there are no production users; recovery guidance arrives with the error-code surfacing in 03-11 |
| T-03-10-SC | Tampering | package installs | accept | No new packages — this plan adds zero dependencies (all seams already in the lockfile) |
</threat_model>

<verification>
- npx vitest run — full suite green (was 415 tests; now includes the new gate + sync-path coverage; telemetry-guard and redact suites green with the extended allowlist).
- npx tsc --noEmit — clean.
- Device unblock path for the UAT re-run (documented in the SUMMARY): rebuild the dev client onto the user's device — the gate self-heals the stale lemastra.db at first DB access; if any failure remains, one save attempt now names the exact exception (error_code + engine message) in the Metro console, which is the evidence the next UAT report must quote.
</verification>

<success_criteria>
- Missing items 1, 2, 3 from the UAT Test 1 gap are closed: sanctioned-logger observability at the storage error boundary, gate robustness for device-resident DBs from earlier dev builds, and sync-path test coverage for the primitives the 415-test suite never reached.
- The user can re-run UAT Test 1 on their device and either it passes (self-healed DB) or the Metro console carries the named failure class for the next diagnosis pass.
- No regression: all existing tests, the telemetry law, the D-03 seam, and the migration artifacts (untouched .sql + journal) are preserved.
</success_criteria>

<output>
Create `.planning/phases/03-private-local-workspace/03-10-SUMMARY.md` when done
</output>
