---
phase: 03-private-local-workspace
plan: 11
type: execute
wave: 8
depends_on: ["03-09"]
files_modified:
  - src/components/workspace/copy.ts
  - src/app/chart/result.tsx
  - src/app/index.tsx
  - src/__tests__/save-flow.test.tsx
  - src/__tests__/home-workspace.test.tsx
autonomous: true
gap_closure: true
requirements: [WORK-01, WORK-03]
must_haves:
  truths:
    - "A failed save shows the WorkspaceError code alongside the existing save-error card copy, so a UAT report quoting the screen carries the failure class (e.g. SAVE_FAILED vs OPEN_FAILED) without needing console access"
    - "A failed boot-time listCharts renders a distinct error card on home (heading + Try again that refetches) — no longer pixel-identical to the empty workspace, so a DB failure can no longer hide as 'no charts'"
    - "The empty workspace, web degradation, and error states remain visually and semantically distinct from each other on home"
    - "All copy comes from the workspace copy deck (no invented strings) and the exact-copy tests stay green, including save-prompt.test.tsx's copy-shape assertions"
  artifacts:
    - path: "src/components/workspace/copy.ts"
      provides: "saveErrorCodeLine(code) template + HOME_LIST_ERROR_COPY card class added to the deck — the only new copy definition sites"
      contains: "HOME_LIST_ERROR_COPY"
    - path: "src/app/chart/result.tsx"
      provides: "save-error card gains the code caption when the rejection is a WorkspaceError; non-workspace rejections render the unchanged card with no code line"
      contains: "saveErrorCodeLine"
    - path: "src/app/index.tsx"
      provides: "charts.isError branch rendering the home list error card with a Try-again refetch action, testID home-list-error"
      contains: "home-list-error"
    - path: "src/__tests__/home-workspace.test.tsx"
      provides: "failed-listCharts coverage: error card renders, saved-charts heading does not, Try again re-invokes the query"
      contains: "isError"
  key_links:
    - from: "src/app/chart/result.tsx"
      to: "src/components/workspace/copy.ts"
      via: "the isError branch renders saveErrorCodeLine(save.error.code) when save.error is a WorkspaceError instance — code only, never the engine message (messages may carry SQL fragments and stay in logs)"
      pattern: "saveErrorCodeLine"
    - from: "src/app/index.tsx"
      to: "src/hooks/use-workspace.ts"
      via: "charts.isError gates an ErrorCard whose action calls charts.refetch — the query seam the home screen already consumes"
      pattern: "refetch"
    - from: "src/__tests__/save-flow.test.tsx"
      to: "src/lib/workspace/repository.ts"
      via: "the rejection test rejects with a real WorkspaceError instance so the instanceof-based code extraction is exercised, not assumed"
      pattern: "WorkspaceError"
---

<objective>
Surface failure classes in the two screens the UAT tester actually looks at: the save-error card carries the WorkspaceError code, and the home chart list gets a distinct error state instead of rendering a failed query identically to an empty workspace.

Purpose: UAT Test 1 reports quoted only static copy ("Couldn't save the chart…") with no failure class, and the diagnosis's secondary finding showed a failed boot-time listCharts is indistinguishable from an empty workspace — both hide DB failures from the person testing. With 03-10 making failures observable in the console, this plan makes them observable on screen so UAT reports carry the class and home can never silently mask a dead DB.

Output: two new copy-deck entries, code caption on the save-error card, home list error card with retry, and test coverage for both in the existing suites.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-private-local-workspace/03-UAT.md

# Copy-deck law + error-card vocabulary:
@src/components/workspace/copy.ts
@src/components/workspace/error-card.tsx
@src/app/index.tsx
@src/app/chart/result.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save-error card carries the WorkspaceError code</name>
  <files>src/components/workspace/copy.ts, src/app/chart/result.tsx, src/__tests__/save-flow.test.tsx</files>
  <action>
    Gap reason: SAVE_ERROR_COPY is static — no failure class reaches the user or the UAT report (missing item 4).

    1. In src/components/workspace/copy.ts, add to the workspace error-deck section: an exported template function saveErrorCodeLine(code: WorkspaceErrorCode): string returning a fixed structural caption embedding the code token (e.g. the literal prefix "Error code: " joined with the code) — import the WorkspaceErrorCode type from @/lib/workspace/repository (the stable re-export site; 03-10 may have moved the class to errors.ts behind that same export — the import path is unaffected either way). Per the deck's trust-boundary rules this is a template over a machine enum, not interpretation prose: document it in the same style as the neighboring entries. SAVE_ERROR_COPY itself stays byte-identical — the heading/body/action assertions in save-flow.test.tsx and save-prompt.test.tsx must remain valid unmodified.

    2. In src/app/chart/result.tsx, in the existing save.isError block: keep the ErrorCard exactly as-is (same props, same testID) and render beneath it, ONLY when save.error is a WorkspaceError instance, a small textSecondary ThemedText with the saveErrorCodeLine(save.error.code) output and its own testID (result-save-error-code). A non-WorkspaceError rejection (the existing "disk full" plain-Error test case) renders NO code line. Display the code ONLY — never the error message body (engine text may carry SQL fragments; messages stay in logs per 03-10).

    3. Extend src/__tests__/save-flow.test.tsx (reuse renderResult/saveThroughPrompt/waitFor idioms from the existing error-card test): (a) saveChart rejects with a real WorkspaceError { code: SAVE_FAILED } → card copy renders AND the code caption with the exact saveErrorCodeLine output is present; (b) the existing plain-Error rejection case additionally asserts the code caption is ABSENT; (c) a WorkspaceError with a different code (e.g. OPEN_FAILED) renders that code — proving the line reflects the actual class, not a constant.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/save-flow.test.tsx src/__tests__/save-prompt.test.tsx src/__tests__/result-screen.test.tsx</automated>
  </verify>
  <done>WorkspaceError save rejections render the deck-authored code caption under the unchanged save-error card; plain-Error rejections render no caption; all pre-existing exact-copy assertions pass without modification.</done>
</task>

<task type="auto">
  <name>Task 2: Distinct home chart-list error state (no longer identical to empty)</name>
  <files>src/components/workspace/copy.ts, src/app/index.tsx, src/__tests__/home-workspace.test.tsx</files>
  <action>
    Gap reason: home renders a failed boot-time listCharts identically to an empty workspace (charts.data ?? [] with no error state) — hides DB failures (missing item 5, the diagnosis's secondary finding).

    1. In src/components/workspace/copy.ts, add a workspace error-deck entry HOME_LIST_ERROR_COPY: WorkspaceErrorCopy with heading, body, and an action label for retry — wording in the deck's established voice (couldn't-load framing, charts-unaffected reassurance, Try-again action), placed with the other error classes. This is a new deck-approved class defined once here; screens never paraphrase it.

    2. In src/app/index.tsx: add a charts.isError branch (native path only — it cannot co-occur with the web card since the query never mounts on web). When the list query has failed, render an ErrorCard with HOME_LIST_ERROR_COPY heading/body/actionLabel, onAction calling charts.refetch, and testID home-list-error — positioned where the list section renders (beneath the CTA, above the footer). The hero and the calculate CTA remain (calculation needs no DB); the saved-charts heading and rows do NOT render while errored (items stay empty — the error card is what distinguishes the state, never a half-rendered list). isError takes precedence over the hasCharts branch; empty-success and web branches are byte-untouched.

    3. Extend src/__tests__/home-workspace.test.tsx following its repository-seam fake conventions: (a) listCharts rejects → the error card renders with the exact deck copy, NO saved-charts heading, and the hero still renders; (b) pressing the card's action re-triggers the query (repository listCharts called again — e.g. reject-once-then-resolve proves the card disappears and the rows render after recovery); (c) the existing zero-charts and web cases keep passing unmodified (empty workspace renders NO error card — the two states stay distinguishable).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home-workspace.test.tsx src/__tests__/chart-list.test.tsx</automated>
  </verify>
  <done>A failed listCharts renders the home-list-error card with a working Try-again refetch; empty-workspace, web-degradation, and error states are mutually distinct; zero existing home-workspace assertions change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage layer → user-visible UI | WorkspaceError code enum values cross from the repository/hook layer onto the rendered screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11-01 | Information Disclosure | save-error code caption / home error card | mitigate | Only the closed WorkspaceErrorCode enum token is rendered (deck template over a machine enum); underlying engine messages NEVER reach the screen — they stay in redact()-filtered logs (03-10); no birth data, labels, or payload fragments can appear in either card |
| T-03-11-02 | Denial of Service | home Try-again action | accept | refetch is user-initiated and TanStack retry posture is unchanged (local reads deterministic); a persistently failing DB re-renders the card — recovery is 03-10's typed gate, not retry spam |
| T-03-11-SC | Tampering | package installs | accept | No new packages — UI-only change over existing components |
</threat_model>

<verification>
- npx vitest run — full suite green (both new states covered; every pre-existing exact-copy and screen assertion passes unmodified).
- npx tsc --noEmit — clean.
- UAT re-run readiness: a failed save on device now shows the failure class on screen (e.g. "Error code: SAVE_FAILED"), and a failed home list shows the couldn't-load card — quotes from the next UAT pass identify the class without console access.
</verification>

<success_criteria>
- Missing items 4 and 5 from the UAT Test 1 gap are closed: the save-error card surfaces the WorkspaceError code, and the home chart list renders a distinct, retryable error state.
- Copy-deck law holds: both new strings are deck-defined and test-asserted; no screen invents copy.
- Combined with 03-10, the UAT tester can re-run Test 1 and either pass or report a named, class-carrying failure.
</success_criteria>

<output>
Create `.planning/phases/03-private-local-workspace/03-11-SUMMARY.md` when done
</output>
