---
phase: 03-private-local-workspace
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - src/components/workspace/chart-list.tsx
  - src/components/workspace/web-unsupported.tsx
  - src/components/workspace/copy.ts
  - src/hooks/use-workspace.ts
  - src/app/index.tsx
  - src/app/chart/saved.tsx
  - src/app/_layout.tsx
  - src/__tests__/chart-list.test.tsx
  - src/__tests__/home-workspace.test.tsx
  - src/__tests__/saved-chart-detail.test.tsx
autonomous: true
requirements: [WORK-01, WORK-03]
must_haves:
  truths:
    - "Home lists saved charts — label, '{date} · {place}' identity line, confidence marker only when not Timed, '{n} revisions' badge only when n > 1 — ordered most-recently-updated first, beneath the Calculate CTA (D-09, D-11)"
    - "Tapping a row opens /chart/saved?id= which reads the chart from the repository by id, re-parses the stored envelope, and renders the Phase-2 composition with ZERO network calls (D-02 reopen law, WORK-03)"
    - "An empty workspace renders exactly the existing hero; once charts exist the CTA label becomes 'Calculate a chart' and a 'Saved charts' heading appears (A-3-UI-5)"
    - "No sign-in/account surface exists anywhere in the workspace (WORK-01)"
    - "Web renders 'Saved charts are available in the app' in place of the list — no storage code path mounts (D-03)"
    - "A stored envelope that fails parse renders the typed 'Couldn't open this saved chart.' state — never a partial render, never a redirect to /birth"
  artifacts:
    - path: "src/app/index.tsx"
      provides: "Workspace home: hero + state-dependent CTA + Saved charts list + web degradation (D-09)"
      contains: "Saved charts"
    - path: "src/components/workspace/chart-list.tsx"
      provides: "D-11 list rows with present-only chips and a11y row labels"
      contains: "listitem"
    - path: "src/app/chart/saved.tsx"
      provides: "Saved-chart detail by id param: repository read → parse-then-trust → Phase-2 composition + loading/open-failed states"
      contains: "getChartDetail"
    - path: "src/components/workspace/web-unsupported.tsx"
      provides: "D-03 web capability card"
      contains: "available in the app"
  key_links:
    - from: "src/app/index.tsx"
      to: "src/hooks/use-workspace.ts"
      via: "useWorkspaceCharts query over repository.listCharts, ordered by the repository (component adds no sorting)"
      pattern: "useWorkspaceCharts"
    - from: "src/app/chart/saved.tsx"
      to: "src/lib/workspace/repository.ts"
      via: "getChartDetail(chartId) → calculateResponseSchema re-parse → PlacementList/AssumptionsLine/ProvenanceDetails/UnavailableFactors render stored evidence only"
      pattern: "getChartDetail"
---

<objective>
Deliver the browse-and-reopen vertical slice (D-09, D-11, WORK-01/03): home becomes the workspace (hero stays on top, saved charts listed beneath, web degradation card), rows navigate to the new /chart/saved route which reads by id from the repository — never a router-param envelope, never a network call — and renders the Phase-2 result composition from stored evidence.

Purpose: after this plan a user can save a chart, kill the app, relaunch, and reopen their chart — the core private-workspace loop (ROADMAP Success Criterion 1).
Output: chart-list + web-unsupported components, workspace home, saved detail route, list/detail hooks, green home/detail tests.
</objective>

## Phase Goal (user story)

**As a** LemAstra user, **I want to** see my saved charts on the home screen and reopen any of them after restarting the app, **so that** my work is there when I come back — without an account and without my device re-sending anything.

*(Derived from ROADMAP Phase 3 Goal + Success Criterion 1.)*

## Context Budget Note

This plan touches 10 files — above the 5–8 planning target, but the excess is three test files, copy-deck strings, and additive edits to existing screens rather than net-new subsystem reads. Keep context in budget: execute tasks strictly in order, commit after each task, and read only what `read_first` names. If context consumption passes ~60% before the final task completes, commit what is green, write the SUMMARY with exact hand-off state, and let a fresh executor finish the remaining task(s).

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-private-local-workspace/03-UI-SPEC.md
@.planning/phases/03-private-local-workspace/03-PATTERNS.md
@.planning/phases/03-private-local-workspace/03-04-SUMMARY.md

- 03-UI-SPEC.md §Home workspace block (row/chip/CTA/empty/web copy, loading rule: render nothing extra), §"/chart/saved" screen contract (layout order, Loading chart… state, open-failed banner), §"Screen & Flow Contracts" id-param law
- 03-RESEARCH.md §"Common Pitfalls" 3 (loading state until first query), 5 (typed-routes regen after adding routes BEFORE tsc), §"Architecture Patterns" anti-patterns (never route envelopes)
- 03-PATTERNS.md §chart-list/revision-history analog (placement-list.tsx list skeleton + present-only slot rule), §saved.tsx/revision.tsx analog (result.tsx parse→redirect guard adapted to id+repository), §index.tsx modification (privacy.tsx list semantics), §test archetype 1 (RNTL /pure + repository-mock seam)
- src/app/index.tsx, src/app/chart/result.tsx, src/app/_layout.tsx (files being extended), src/lib/workspace/repository.ts (listCharts/getChartDetail), src/hooks/use-workspace.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chart-list rows + web-unsupported card + list hook</name>
  <files>src/components/workspace/chart-list.tsx, src/components/workspace/web-unsupported.tsx, src/components/workspace/copy.ts, src/hooks/use-workspace.ts, src/__tests__/chart-list.test.tsx</files>
  <read_first>
  - 03-UI-SPEC.md §Home workspace rows + accessible-label copy, §"Component Inventory" chart-list/web-unsupported rows, §"Accessibility Contract" (single-tap rows, chips decorative inside the row label)
  - 03-PATTERNS.md §chart-list analog (placement-list.tsx: typed readonly props, role=list/listitem, per-row a11y label, present-only slots), Shared Patterns (themed card + hitSlop + CTA conventions)
  - src/components/chart/placement-list.tsx (the list skeleton), src/app/index.tsx (hitSlop link idiom), src/components/workspace/copy.ts (deck being extended), src/hooks/use-workspace.ts (hook file being extended)
  </read_first>
  <behavior>
  - Each row renders label (Body/600), identity line "{date} · {place}" (Label, textSecondary), confidence chip only when confidence ≠ "Timed", "{n} revisions" chip only when n > 1 — chips never render empty placeholders
  - Row accessible label: "{label}, {date}, {place}{, confidence}{, n revisions}. Opens the chart."
  - Rows are Pressables (≥48dp) emitting the chartId via onOpen; component renders rows in the exact order received (ordering is the repository's job)
  - Web card renders heading "Saved charts are available in the app" + body "Charts are stored only on your device. Saving, reopening, and exporting work in the LemAstra app on iOS or Android."
  </behavior>
  <action>
  Extend the workspace copy deck with the home/list strings (SAVED_CHARTS_HEADING, HOME_CTA_WITH_CHARTS "Calculate a chart", row/chip/a11y templates, WEB_UNSUPPORTED heading/body). Build chart-list.tsx on the placement-list skeleton: typed readonly ChartListItem[] prop, role=list/listitem rows with the themed card treatment, present-only chips per D-11, single onOpen prop. Build web-unsupported.tsx as the D-03 capability card (secondary fill, heading + body, no actions). Extend use-workspace.ts with useWorkspaceCharts (query key ['charts'], repository seam). Tests: two-row render with/without chips, exact a11y labels, order passthrough, web card strings.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/chart-list.test.tsx</automated>
  </verify>
  <done>D-11 row contract exact; present-only slots; ordering untouched by the component; web card copy exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: workspace home (D-09) — list beneath CTA, state-dependent label</name>
  <files>src/app/index.tsx, src/hooks/use-workspace.ts, src/__tests__/home-workspace.test.tsx</files>
  <read_first>
  - 03-UI-SPEC.md §Home workspace block (layout order hero → CTA → list → footer; empty = hero alone; loading rule: nothing extra), §"Interaction Contract" List & reopen paragraph
  - 03-PATTERNS.md §index.tsx analog (keep hero/CTA, privacy.tsx list semantics)
  - src/app/index.tsx (current hero/CTA/footer being extended), src/lib/workspace/repository.ts (isWorkspaceStorageAvailable + listCharts), src/__tests__/privacy-screen.test.tsx (list-assertion style)
  </read_first>
  <behavior>
  - Zero charts: exactly the current hero renders (heading, sub-line, CTA "Calculate your first chart", privacy link) — no "Saved charts" heading
  - ≥1 chart: CTA reads "Calculate a chart"; "Saved charts" heading (24/600) + ChartList render beneath, above the footer
  - Web: the web-unsupported card replaces the list; CTA remains
  - No sign-in/account/register surface exists anywhere on home (WORK-01) — asserted by exact-render assertions over the tree
  </behavior>
  <action>
  Restructure home per D-09: hero and CTA stay on top; below the CTA mount useWorkspaceCharts and render ChartList only when data exists (isWorkspaceStorageAvailable() false → WebUnsupported instead); CTA label switches on whether the list is non-empty; loading renders nothing extra (local query resolves fast — no skeleton per UI-SPEC). Extend use-workspace if the query needs a platform-availability wrapper (return empty + unavailable flag on web). home-workspace tests: fake the repository seam with empty/non-empty corpora, assert the three states + no-account assertion; web branch covered by rendering WebUnsupported (already unit-tested) — the Platform gate is a thin conditional.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/home-workspace.test.tsx</automated>
  </verify>
  <done>Home is the workspace: empty-hero truth preserved, list appears with charts, web degrades, WORK-01 asserted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /chart/saved detail route — reopen by id, parse-then-trust</name>
  <files>src/app/chart/saved.tsx, src/app/_layout.tsx, src/hooks/use-workspace.ts, src/__tests__/saved-chart-detail.test.tsx</files>
  <read_first>
  - 03-UI-SPEC.md §"/chart/saved" contract (title = chart label, identity line, body composition order, Loading chart…, open-failed banner copy, back behavior), §"Trust-Boundary Display Rules" (stored re-parsed evidence only)
  - 03-PATTERNS.md §saved.tsx analog (result.tsx param→parse→redirect guard adapted: id param + repository read; screen layout constants block), §Router discipline (id params only, _layout registration, typed-routes landmine)
  - src/app/chart/result.tsx (the composition being reused), src/lib/workspace/repository.ts (getChartDetail + WorkspaceError), src/components/workspace/error-card.tsx + copy.ts (open-failed strings)
  </read_first>
  <behavior>
  - Route takes chartId only; repository getChartDetail drives a query; while pending renders centered "Loading chart…" and nothing else
  - Success: title (Display) = chart label, identity line from stored identity + confidence, then PlacementList → AssumptionsLine (existing adjust action unchanged this plan) → ProvenanceDetails → validation status → UnavailableFactors — all rendering the latest revision's stored envelope
  - Reopen makes ZERO network calls — test mocks global networking and asserts no call count (D-02)
  - Repository null (missing chart) → back to home; WorkspaceError OPEN_FAILED → the error card "Couldn't open this saved chart." with its UI-SPEC body — never a partial render, never a redirect to /birth
  - Back from detail returns home with the list intact (query cache)
  </behavior>
  <action>
  Create saved.tsx following the result.tsx screen skeleton with the id-read substitution per PATTERNS: useLocalSearchParams chartId → useWorkspaceChart(chartId) query (add to use-workspace) over repository.getChartDetail; render the loading state, the open-failed error card, or the Phase-2 composition exactly as the result screen composes it (same components, same order, stored identity + zone_source for the place-resolution row). Register chart/saved in _layout. After the route exists, regenerate typed routes (boot the dev server once) BEFORE npx tsc --noEmit — the Phase-1 CI ordering note. Tests per behavior rows with the repository faked and networking mocked to zero.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/saved-chart-detail.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>WORK-03 UI complete: id-based reopen, zero-network law test-enforced, typed open-failure surface, typed routes regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored rows → rendered evidence | Reopen re-parses the frozen envelope; failures are typed, never partial |
| route params → repository query | chartId is the only input; an unknown id is data, not code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Information Disclosure | reopen re-calling the API (envelope egress) | mitigate | repository is the only data source for /chart/saved; zero-network test asserts no fetch during reopen (D-02/PRIV-01) |
| T-03-16 | Tampering | envelope-in-router-param anti-pattern | mitigate | id-param-only routes; the anti-pattern is named in PATTERNS and this plan's guard test renders from the repository seam |
| T-03-17 | DoS | first-render race before migrations (Pitfall 3) | mitigate | query renders Loading chart… until getChartDetail resolves; repository awaits the migration gate |
</threat_model>

<verification>
- npx vitest run src/__tests__/chart-list.test.tsx src/__tests__/home-workspace.test.tsx src/__tests__/saved-chart-detail.test.tsx
- npx vitest run (full suite) && npx tsc --noEmit (after typed-routes regen)
</verification>

<success_criteria>
- ROADMAP Success Criterion 1 loop closes in tests: save (03-04) → home list → reopen by id with zero network
- WORK-01 asserted (no account surface); D-03 web degradation honest (no storage path)
</success_criteria>

## Artifacts this phase produces

*Plan 03-05 symbols later plans consume (exclude from drift verification):*
- `src/app/chart/saved.tsx`: SavedChartScreen (chartId param contract)
- `src/components/workspace/chart-list.tsx`: ChartList ({items, onOpen})
- `src/components/workspace/web-unsupported.tsx`: WebUnsupported
- `src/hooks/use-workspace.ts`: useWorkspaceCharts, useWorkspaceChart additions
- `src/app/_layout.tsx`: chart/saved registration
- copy.ts additions: home/list/web strings

<output>
Create `.planning/phases/03-private-local-workspace/03-05-SUMMARY.md` when done
</output>
