---
phase: 03-private-local-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - drizzle.config.ts
  - drizzle/migrations
  - scripts/vitest/expo-sqlite-facade/index.ts
  - scripts/vitest/expo-sqlite-facade/README.md
  - vitest.config.ts
  - src/lib/workspace/schema.ts
  - src/lib/workspace/db.ts
  - src/__tests__/expo-sqlite-facade.test.ts
  - src/__tests__/workspace-db.test.ts
autonomous: true
requirements: [WORK-02]
must_haves:
  truths:
    - "A real SQLite database opens, migrates, accepts a typed insert, and re-reads it after close and reopen through the same seam the repository will use — proven by automated tests against a file-backed temp database (WORK-02/03 storage foundation)"
    - "Dependency pins match Expo SDK 57 bundledNativeModules tilde ranges (expo-sqlite ~57.0.1, expo-file-system ~57.0.5, expo-sharing ~57.0.14, expo-crypto ~57.0.1) with no postinstall scripts"
    - "Generated migrations are committed artifacts and the imperative migration gate runs before any query is served (no querying an unmigrated database)"
  artifacts:
    - path: "src/lib/workspace/schema.ts"
      provides: "Drizzle charts + chart_revisions tables and the stored-inputs/stored-identity zod contracts (D-01/D-02)"
      contains: "chart_revisions"
    - path: "src/lib/workspace/db.ts"
      provides: "Lazy singleton openDatabaseSync + drizzle() + migrate() gate (Pitfall 3)"
      contains: "getWorkspaceDb"
    - path: "scripts/vitest/expo-sqlite-facade/index.ts"
      provides: "node:sqlite-backed expo-sqlite test alias implementing exactly the drizzle sync call surface"
      contains: "openDatabaseSync"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit codegen config (dialect sqlite, driver expo)"
      contains: "sqlite"
  key_links:
    - from: "src/lib/workspace/db.ts"
      to: "drizzle/migrations"
      via: "imperative migrate(db, migrations) inside the lazy singleton — verifies A5 (generated module shape imports in-app)"
      pattern: "migrate"
    - from: "vitest.config.ts"
      to: "scripts/vitest/expo-sqlite-facade/index.ts"
      via: "resolve.alias entry routing the expo-sqlite specifier to the facade (same slot as the react-native facade)"
      pattern: "expo-sqlite"
---

<objective>
Stand up the repo's first SQLite data layer (D-01, D-04): install expo-sqlite/drizzle/expo-file-system/expo-sharing/expo-crypto per the legitimacy audit, build the node:sqlite test facade that makes the real Drizzle expo-sqlite driver runnable under vitest, define the charts + chart_revisions schema with generated migrations, and prove the open→migrate→write→read→close→reopen pipeline end to end.

Purpose: every later Phase-3 plan (repository, save flow, workspace UI) builds on this engine; proving it against a real SQL engine now removes the phase's biggest unknowns (A5 migration module shape, facade surface fidelity).
Output: installed+locked deps, committed drizzle/ migrations, workspace schema + db gate, green facade-contract and migration-gate tests.
</objective>

## Phase Goal (user story)

**As a** LemAstra user, **I want to** save my calculated charts with names and reopen, revise, rename, export, and delete them on my device, **so that** my astrology work persists privately without an account.

*(Derived from ROADMAP Phase 3 Goal + Success Criteria — the ROADMAP goal line is outcome-shaped, not story-shaped; slots derived faithfully.)*

## Context Budget Note

This plan touches 11 files — above the 5–8 planning target, but the excess is dependency manifests, generated migration output, a README, and two test files rather than net-new subsystem reads. Keep context in budget: execute tasks strictly in order, commit after each task, and read only what `read_first` names. Monitor point: Task 3 (schema + db gate + first `drizzle-kit generate` + migration-gate test) is the heaviest step — if the A5 module-shape verification forces exploration beyond the documented fallback, or context consumption passes ~60% before Task 3 completes, commit what is green, write the SUMMARY with exact hand-off state, and let a fresh executor finish.

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-private-local-workspace/03-RESEARCH.md
@.planning/phases/03-private-local-workspace/03-PATTERNS.md

- 03-RESEARCH.md §"Standard Stack" (versions + install commands), §"Package Legitimacy Audit" (all six packages Approved; expo-* SUS "too-new" disposition = existing npx-expo-install convention), §"Architecture Patterns" Pattern 1/2 (db gate + table shape), §"Code Examples" (facade core), §"Common Pitfalls" 2/3/8, §"Assumptions Log" A4/A5
- 03-PATTERNS.md §`src/lib/workspace/schema.ts`, §`src/lib/workspace/db.ts`, §`scripts/vitest/expo-sqlite-facade/` (governs the no-analog files)
- src/lib/api-schemas.ts (CalculateResponse + closed enums the stored contracts import), src/lib/query-client.tsx (lazy-singleton precedent), vitest.config.ts + scripts/vitest/react-native-shim.ts (facade-alias machinery)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install storage/export dependencies with legitimacy rationale</name>
  <files>package.json, package-lock.json</files>
  <read_first>
  - 03-RESEARCH.md §"Standard Stack" (exact install commands) and §"Package Legitimacy Audit" (per-package verdicts + the expo-* "too-new" note)
  - package.json (current deps/devDeps), node_modules/expo/bundledNativeModules.json (SDK-57 tilde pins)
  </read_first>
  <acceptance_criteria>
  - package.json dependencies contain expo-sqlite, expo-file-system, expo-sharing, expo-crypto with tilde pins resolvable to ~57.0.1 / ~57.0.5 / ~57.0.14 / ~57.0.1 and drizzle-orm on the ^0.45 line; devDependencies contain drizzle-kit
  - `npm ls expo-sqlite drizzle-orm drizzle-kit expo-file-system expo-sharing expo-crypto` exits 0 with no missing/invalid
  - `node -e "for (const p of ['expo-sqlite','expo-file-system','expo-sharing','expo-crypto','drizzle-orm','drizzle-kit']) { const s=require(`./node_modules/${p}/package.json`); if (s.scripts && s.scripts.postinstall) process.exit(1) }"` exits 0 (no postinstall on any package)
  - The commit message body (or SUMMARY) records the per-dependency legitimacy rationale: drizzle-orm/drizzle-kit OK (20M/16.8M weekly, drizzle-team org); the four expo-* first-party packages follow the Phase-1 npx-expo-install convention (SUS "too-new" = publish recency only)
  </acceptance_criteria>
  <action>
  Install per D-01 and the research install sketch: run npx expo install expo-sqlite expo-file-system expo-sharing expo-crypto (pins from bundledNativeModules, not the moving latest tag), then npm install drizzle-orm, then npm install -D drizzle-kit. Do NOT add any analytics/crash package (D-16). Verify the pinned ranges in package.json against bundledNativeModules.json and that none of the six packages carries a postinstall script. Record the legitimacy rationale from the audit table in the task SUMMARY.
  </action>
  <verify>
    <automated>npm ls expo-sqlite expo-file-system expo-sharing expo-crypto drizzle-orm drizzle-kit && node -e "for (const p of ['expo-sqlite','expo-file-system','expo-sharing','expo-crypto','drizzle-orm','drizzle-kit']) { const s=require('./node_modules/'+p+'/package.json'); if (s.scripts && s.scripts.postinstall) process.exit(1) }"</automated>
  </verify>
  <done>Deps installed and locked; tilde/caret pins verified; legitimacy rationale recorded; no postinstall scripts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: node:sqlite test facade + vitest alias + facade contract test</name>
  <files>scripts/vitest/expo-sqlite-facade/index.ts, scripts/vitest/expo-sqlite-facade/README.md, vitest.config.ts, src/__tests__/expo-sqlite-facade.test.ts</files>
  <read_first>
  - 03-RESEARCH.md §"Code Examples" last block (facade core over node:sqlite DatabaseSync — source-verified against drizzle-orm@0.45.2/expo-sqlite/session.js) and §"Common Pitfalls" 8 (implement exactly the drizzle call surface, nothing more)
  - scripts/vitest/react-native-shim.ts (problem/solution/identity rationale — the machinery this mirrors), vitest.config.ts (config-time alias slot the new entry joins)
  - node_modules/drizzle-orm/expo-sqlite/session.js (the authoritative call surface — read to confirm, do not guess)
  </read_first>
  <behavior>
  - openDatabaseSync(name) returns an object exposing prepareSync, execSync, closeSync (and nothing else beyond what drizzle-orm/expo-sqlite imports at module load)
  - prepareSync(sql).executeSync(params) returns an object with numeric changes, lastInsertRowId, plus getAllSync/getFirstSync functions; executeForRawResultSync(params) returns an object with getAllSync
  - Database name maps to a file under a per-run temp directory so close → reopen by the same name sees the same data; an exported test-only reset helper clears the temp dir and open-handle map
  - drizzle-orm/expo-sqlite's drizzle() runs a real insert/select/transaction over the facade (contract test proves driver compatibility, not just shape parity)
  </behavior>
  <action>
  Create the facade as a committed TypeScript module (not a config-time write — unlike react-native it needs no prebundling). Implement exactly the source-verified drizzle sync surface over node:sqlite DatabaseSync per the research code example: prepareSync returning executeSync/executeForRawResultSync bound to stmt run/all/get, execSync, closeSync, and an openDatabaseSync(name) that resolves names to files under a mkdtemp-created temp dir (never :memory: — WORK-03 restart tests need file persistence) with an exported reset helper for test isolation. Add a README naming Pitfall 8 (surface discipline). Extend vitest.config.ts resolve.alias with the expo-sqlite → facade entry in the same slot style as the react-native facade; leave every existing alias untouched. Contract test: pin every return shape from the behavior rows AND run a tiny drizzle insert/select/delete-with-where roundtrip plus a transaction block through drizzle(facade.openDatabaseSync(...)) — this is the proof the whole phase's test strategy rests on.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/expo-sqlite-facade.test.ts</automated>
  </verify>
  <done>Facade contract test green; drizzle driver proven over node:sqlite; alias resolves; existing suites untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drizzle schema + generated migrations + lazy db gate</name>
  <files>drizzle.config.ts, src/lib/workspace/schema.ts, src/lib/workspace/db.ts, src/__tests__/workspace-db.test.ts</files>
  <read_first>
  - 03-RESEARCH.md §"Architecture Patterns" Pattern 1 (db gate) and Pattern 2 (verified table shape — the starting schema), §"Common Pitfalls" 3 (query-before-migration), §"Assumptions Log" A4/A5
  - 03-PATTERNS.md §`src/lib/workspace/schema.ts` (api-schemas .describe() discipline), §`src/lib/workspace/db.ts` (query-client lazy-singleton precedent + module-header style)
  - src/lib/api-schemas.ts (CalculateResponse, confidenceSchema, houseSystemSchema, timeResolutionModeSchema, placeCandidateSchema — the types the stored contracts reuse), src/app/birth.tsx (google/manual place-union branches to mirror in stored inputs)
  </read_first>
  <behavior>
  - charts table: id (text pk), label (text notNull), created_at/updated_at (integer timestamp_ms notNull)
  - chart_revisions table: id (text pk), chart_id (text notNull references charts.id), input_revision (text notNull), confidence / identity_date / identity_place_label summary text columns, envelope + inputs + identity as text json-mode typed columns, created_at (timestamp_ms); uniqueIndex on (chart_id, input_revision) and index on (chart_id, created_at)
  - storedCalculationInputsSchema (zod): date, time (empty string allowed for Unknown), optional time_resolution object, confidence, house_system, place object with label/lat/lon, place_form discriminated union mirroring birth.tsx google/manual branches, iana_zone, zone_source — every field .describe()-ed
  - storedIdentitySchema (zod): date, time, label, zone_source (the result-screen identity shape D-02 stores)
  - getWorkspaceDb() memoizes one promise: openDatabaseSync → drizzle() → await migrate() → db; repeated calls return the same instance; an exported test-only reset clears the memoized promise
  - Migration-gate test through the facade: get db → insert a chart + revision via drizzle → read back → reset singleton → get db again (same file) → the row is still there and migrations did not re-apply destructively
  </behavior>
  <action>
  Create drizzle.config.ts (dialect sqlite, driver expo, schema path src/lib/workspace/schema.ts, out drizzle/). Add package.json script db:generate running drizzle-kit generate. Write schema.ts per Pattern 2 with the summary columns above (denormalized confidence/date/place-label so listCharts never parses envelope JSON), typed json columns importing CalculateResponse from @/lib/api-schemas and the two stored zod contracts for inputs/identity — follow the .describe() contract-docs discipline from api-schemas.ts. Run npm run db:generate and commit the emitted drizzle/ journal + migration modules (A5 verification: the generated migrations index must be importable by the migrator — if the module shape differs from the documented default export, fall back to wiring useMigrations per the research alternative and record the deviation in the SUMMARY). Write db.ts as the lazy memoized-promise singleton per Pattern 1, with a module header naming D-03 (adapter seam) and the Pitfall 3 gate, plus the test-only singleton reset. Migration-gate test per the behavior rows using the facade temp-dir file.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/workspace-db.test.ts src/__tests__/expo-sqlite-facade.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Migrations committed; open→migrate→insert→read→reopen proven on a file-backed DB; tsc green; schema doubles as contract reference via .describe().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored JSON ↔ typed columns | Envelope/inputs/identity cross a serialization boundary at every row |
| dependency install → app bundle | New native-adjacent packages enter the client (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (expo-sqlite, drizzle-orm, drizzle-kit, expo-file-system, expo-sharing, expo-crypto) | mitigate | Package Legitimacy Audit in 03-RESEARCH.md: all Approved (expo-* SUS "too-new" = publish recency on first-party expo/expo monorepo packages, 1M–9M weekly); installs via npx expo install tilde pins; postinstall-script check in Task 1 verify |
| T-03-01 | Tampering | schema/migration drift between generated SQL and schema.ts | mitigate | drizzle-kit journal is the single generator; migration-gate test fails if migrate() cannot reproduce the schema on a fresh file |
| T-03-02 | Tampering | facade drift from expo-sqlite device semantics (Pitfall 8) | mitigate | facade implements exactly the source-read drizzle session surface; contract test pins return shapes and proves driver compatibility; repository code stays on the drizzle API so the facade surface stays minimal |
| T-03-03 | DoS | querying before migrations complete (Pitfall 3) | mitigate | every repository call awaits the same memoized getWorkspaceDb() promise; migration gate runs inside it |
</threat_model>

<verification>
- npx vitest run src/__tests__/expo-sqlite-facade.test.ts src/__tests__/workspace-db.test.ts
- npx vitest run (full suite — no regression) && npx tsc --noEmit
- ls drizzle/ shows committed journal + migration modules; package.json db:generate script exists
</verification>

<success_criteria>
- The repo's first SQLite layer exists, is migration-gated, and is provably testable against a real SQL engine with zero new dev dependencies
- A5 resolved (migrations import in-app) or the documented useMigrations fallback recorded
- No UI yet — this plan is the engine room; the first user-visible slice lands in 03-04/03-05
</success_criteria>

## Artifacts this phase produces

*Plan 03-01 symbols later plans consume (exclude from drift verification):*
- `src/lib/workspace/schema.ts`: charts + chartRevisions tables, storedCalculationInputsSchema, storedIdentitySchema, StoredCalculationInputs/StoredIdentity types
- `src/lib/workspace/db.ts`: getWorkspaceDb() lazy singleton + test-only reset
- `scripts/vitest/expo-sqlite-facade/index.ts`: openDatabaseSync + reset helper (the expo-sqlite test alias)
- `drizzle/` committed migrations; `drizzle.config.ts`; package.json `db:generate` script

<output>
Create `.planning/phases/03-private-local-workspace/03-01-SUMMARY.md` when done
</output>
