---
phase: 03-private-local-workspace
plan: 08
type: execute
wave: 6
depends_on: ["03-06"]
files_modified:
  - src/components/privacy/copy.ts
  - src/components/privacy/data-controls.tsx
  - src/lib/workspace/export.ts
  - src/app/privacy.tsx
  - src/__tests__/data-controls.test.tsx
autonomous: true
requirements: [PRIV-05, PRIV-06]
must_haves:
  truths:
    - "The /privacy screen gains a 'Your data' section: intro, 'Export all data' card, and a destructive 'Delete all data' card below the registry-driven provider list (D-15)"
    - "Export all data writes one pretty-printed JSON file containing every chart and every revision stored on the device (deep-equal to the repository corpus) and offers it via the share sheet (PRIV-05)"
    - "Delete all data requires the confirmation modal (scope + permanence + the surviving acknowledgement preference); confirm wipes every chart/revision row in one transaction and renders 'No personal data is stored on this device.' (PRIV-06, D-15)"
    - "The disclosure acknowledgement (AsyncStorage) survives delete-all — asserted by test (D-15)"
    - "The provider disclosure content remains 100% registry-driven; the existing Phase-1 governance consistency tests pass unchanged"
    - "Web renders the export/delete cards disabled with the 'Available in the LemAstra app on iOS or Android.' helper"
  artifacts:
    - path: "src/components/privacy/data-controls.tsx"
      provides: "'Your data' cards: export-all (file+share), delete-all (confirm-gated), completion state, web-disabled variants"
      contains: "Delete all data"
    - path: "src/components/privacy/copy.ts"
      provides: "Privacy-surface copy module for the data-controls section (keeps workspace copy deck ownership clean per UI-SPEC)"
      contains: "Your data"
    - path: "src/lib/workspace/export.ts"
      provides: "exportAllDataFile — the all-data file write beside the single-chart export"
      contains: "lemastra-all-data"
  key_links:
    - from: "src/app/privacy.tsx"
      to: "src/components/privacy/data-controls.tsx"
      via: "one section rendered below the provider list — provider content untouched (registry invariant)"
      pattern: "DataControls"
    - from: "src/components/privacy/data-controls.tsx"
      to: "src/lib/workspace/repository.ts"
      via: "repository.exportAllData() → file write → share; repository.deleteAllData() behind DeleteConfirm variant=all"
      pattern: "deleteAllData"
---

<objective>
Deliver the PRIV-05/PRIV-06 vertical slice (D-15): the "Your data" section on the registry-driven /privacy screen — Export all data (one JSON file with every chart + revision + metadata via the established file+share mechanics) and Delete all data (confirm-gated transactional wipe sparing the non-personal disclosure flag) — with the web-disabled capability variants.

Purpose: the retention-deletion-policy §5 principle ("deletable and exportable without an account") becomes a user control on the exact screen where privacy promises are made, without adding any new provider claim (governance tests stay green unchanged).
Output: privacy copy module + data-controls component, export-all file helper, extended /privacy, green tests incl. disclosure-flag survival.
</objective>

## Phase Goal (user story)

**As a** LemAstra user, **I want to** export everything LemAstra stores about me or delete all of it in one confirmed action, **so that** my data leaves or dies on my terms, without an account standing in the way.

*(Derived from ROADMAP Phase 3 Goal + Success Criteria 4/5; retention-deletion-policy §5/§6.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-private-local-workspace/03-UI-SPEC.md
@.planning/phases/03-private-local-workspace/03-PATTERNS.md
@.planning/phases/03-private-local-workspace/03-06-SUMMARY.md

- 03-UI-SPEC.md §"'Your data' section (/privacy additions)" (heading, intro, card copy, pending "Creating file…", completion state, web-disabled helper), §Deletion — all data block (exact modal copy incl. the surviving-acknowledgement sentence), §"Trust-Boundary Display Rules" (private-by-default visible: "no account and no server copy")
- 03-RESEARCH.md §"Common Pitfalls" 9 (delete-all spares migration journal + AsyncStorage flags; never delete the .db file), §"Architecture Patterns" D-15 row
- 03-PATTERNS.md §privacy.tsx modification (registry-driven invariant + the section/copy structure), §data-controls analog
- src/app/privacy.tsx (screen being extended), src/lib/workspace/repository.ts (exportAllData/deleteAllData), src/lib/workspace/export.ts (file+share mechanics), src/components/workspace/delete-confirm.tsx (variant=all), src/hooks/use-disclosure.ts (the flag that must survive), src/__tests__/disclosures-consistency.test.ts (must stay green)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: privacy copy module + data-controls component + export-all helper</name>
  <files>src/components/privacy/copy.ts, src/components/privacy/data-controls.tsx, src/lib/workspace/export.ts, src/__tests__/data-controls.test.tsx</files>
  <read_first>
  - 03-UI-SPEC.md "Your data" + Deletion-all blocks (every literal string) and §Color rules (delete-all is the destructive trigger; cancel default)
  - src/components/workspace/copy.ts + delete-confirm.tsx (the variant=all strings live in the workspace deck — reuse, do not fork), src/lib/workspace/export.ts (the write/share pattern to extend), src/app/birth/confirm.tsx (local useMutation precedent — this component keeps its mutations local; use-workspace.ts is owned by 03-07 this wave)
  - src/hooks/use-disclosure.ts (the AsyncStorage flag delete-all must spare), src/lib/workspace/repository.ts (exportAllData/deleteAllData shapes)
  </read_first>
  <behavior>
  - DataControls renders the section heading "Your data" (24/600), intro "Everything you save stays on this device — there's no account and no server copy. Export it as a file, or delete it.", the export card ("Export all data" + helper) and the destructive delete card ("Delete all data", error-colored + helper)
  - Export-all: repository.exportAllData() → pretty JSON file named lemastra-all-data.json under Paths.cache → capability-gated share; pending shows "Creating file…"; failure renders "Couldn't delete your data."-deck sibling: "Couldn't create the export file." + Try again via the workspace ErrorCard
  - Delete-all: DeleteConfirm variant "all" → repository.deleteAllData() → ['charts'] invalidation → section swaps to "No personal data is stored on this device."; failure renders "Couldn't delete your data." + Try again; cancel removes nothing
  - After a successful delete-all, the AsyncStorage disclosure key still reads "true" (mocked store, asserted)
  - Web (capability unavailable): both cards render disabled with helper "Available in the LemAstra app on iOS or Android."
  </behavior>
  <action>
  Create the privacy copy module with the exact section strings (export deck constants + delete-all completion/web helpers; destructive modal strings come from the workspace deck's all-variant). Extend export.ts with exportAllDataFile(payload) mirroring exportChartRevision (pretty JSON, Paths.cache, capability gate). Build data-controls.tsx with LOCAL useMutation hooks (confirm.tsx precedent — do NOT modify use-workspace.ts, which 03-07 owns this wave), composing the workspace DeleteConfirm variant "all" and ErrorCard, invalidating the ['charts'] key on success, and rendering the completion/web states per the behavior rows. Tests: seeded multi-chart corpus (fake repository) → export file content deep-equals the corpus; delete-all wipes to the completion state with the flag surviving; cancel no-op; web-disabled rendering.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/data-controls.test.tsx</automated>
  </verify>
  <done>PRIV-05/06 controls behave exactly per deck; the surviving-flag and corpus-equality laws are test-enforced.</done>
</task>

<task type="auto">
  <name>Task 2: extend /privacy — one section, zero provider drift</name>
  <files>src/app/privacy.tsx, src/__tests__/data-controls.test.tsx</files>
  <read_first>
  - 03-UI-SPEC.md §"/privacy — 'Your data'" screen contract (section below the provider list) and the governance invariant note
  - src/app/privacy.tsx (the registry-driven screen being extended — its no-provider-content-of-its-own law), src/__tests__/privacy-screen.test.tsx + src/__tests__/disclosures-consistency.test.ts (must stay green unchanged)
  </read_first>
  <acceptance_criteria>
  - privacy.tsx renders the DataControls section below the provider list; the provider list rendering code is byte-unchanged (git diff shows only additive section mounting + import)
  - The existing privacy-screen tests and disclosures-consistency tests pass without modification
  - A new test asserts the section renders after the provider list in document order and that the empty-storage completion variant renders inside the section
  - Full suite green: npx vitest run && npx tsc --noEmit
  </acceptance_criteria>
  <action>
  Additively mount DataControls below the provider list in privacy.tsx (import + one section element — nothing else changes; the screen's provider content stays 100% registry-driven per its module law). Extend data-controls tests with the document-order assertion. Run the full suite and confirm the Phase-1 governance gates are untouched.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/data-controls.test.tsx src/__tests__/privacy-screen.test.tsx src/__tests__/disclosures-consistency.test.ts && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>/privacy owns user controls only; provider disclosure parity provably intact; the full Phase-3 suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local database → exported file | The complete personal corpus crosses into a shareable artifact — user-initiated only |
| destructive wipe → durable state | delete-all must remove every personal row while sparing non-personal flags and engine bookkeeping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-25 | Information Disclosure | incomplete export (user believes they have everything) | mitigate | export-all deep-equals the repository corpus in tests; provenance fields included (retention §5) |
| T-03-26 | Information Disclosure | incomplete deletion / orphaned rows | mitigate | transactional wipe of personal tables (Pitfall 9: never delete the .db file; migration journal spared); counts-to-zero proven in 03-03 and surfaced here via the completion state |
| T-03-27 | Tampering | governance drift (new UI inventing provider/retention claims) | mitigate | section adds user controls only; provider rendering byte-unchanged; Phase-1 consistency tests gate it |
| T-03-28 | DoS | accidental mass deletion | mitigate | confirm modal naming scope + permanence + what survives; cancel default; no shortcut triggers |
</threat_model>

<verification>
- npx vitest run src/__tests__/data-controls.test.tsx
- npx vitest run (entire phase suite — all 8 plans' tests) && npx tsc --noEmit
- git diff on privacy.tsx shows additive-only provider-list changes
</verification>

<success_criteria>
- ROADMAP Success Criteria 4 + 5 complete: single-chart export (03-06) + export-all/delete-all with telemetry exclusion (03-02) close every PRIV requirement in Phase 3
- The privacy screen's governance invariant survives its first extension
</success_criteria>

## Artifacts this phase produces

*Plan 03-08 symbols later plans consume (exclude from drift verification):*
- `src/components/privacy/data-controls.tsx`: DataControls (export-all/delete-all/completion/web states)
- `src/components/privacy/copy.ts`: your-data copy constants
- `src/lib/workspace/export.ts`: exportAllDataFile addition

<output>
Create `.planning/phases/03-private-local-workspace/03-08-SUMMARY.md` when done
</output>
