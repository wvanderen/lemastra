---
phase: 03-private-local-workspace
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - src/components/workspace/copy.ts
  - src/components/workspace/save-prompt.tsx
  - src/components/workspace/error-card.tsx
  - src/hooks/use-workspace.ts
  - src/app/birth/confirm.tsx
  - src/app/chart/result.tsx
  - src/__tests__/save-prompt.test.tsx
  - src/__tests__/save-flow.test.tsx
autonomous: true
requirements: [WORK-02, PRIV-01]
must_haves:
  truths:
    - "Tapping Save chart on the result screen opens a label-prompt modal prefilled with the smart default; confirming a valid name persists chart + revision through the repository (D-10, WORK-02)"
    - "Nothing is persisted before the Save tap — a calculated-but-unsaved chart writes zero rows (explicit-save-only, PRIV-01)"
    - "After save the CTA becomes the neutral Saved ✓ chip; saving identical inputs to the same chart again shows Saved ✓ with 'Already saved with these exact details.' and creates nothing (D-06 dedupe surface)"
    - "Save failure renders 'Couldn't save the chart.' with a Try again action and loses nothing on screen"
    - "The confirm screen threads the CalculateRequest inputs to the result screen so the saved revision can later prefill the revise flow (D-08 groundwork)"
  artifacts:
    - path: "src/components/workspace/save-prompt.tsx"
      provides: "RN Modal label prompt: validated TextInput, smart-default prefill, live-region field error, disabled-while-invalid-or-pending confirm"
      contains: "Modal"
    - path: "src/hooks/use-workspace.ts"
      provides: "useSaveChart mutation (retry: false, invalidates ['charts'])"
      contains: "useSaveChart"
    - path: "src/app/chart/result.tsx"
      provides: "Save CTA below the identity line + Saved ✓/dedupe states + request-param parsing"
      contains: "Save chart"
    - path: "src/components/workspace/copy.ts"
      provides: "Workspace copy deck (save-flow strings + workspace error classes)"
      contains: "Saved"
  key_links:
    - from: "src/app/chart/result.tsx"
      to: "src/lib/workspace/repository.ts"
      via: "useSaveChart mutation → repository.saveChart with parsed envelope + inputs + identity (parse-then-trust at save, D-02)"
      pattern: "saveChart"
    - from: "src/app/birth/confirm.tsx"
      to: "src/app/chart/result.tsx"
      via: "router.push gains a third param request = JSON of the built CalculateRequest plus the form's place-union branch (Pattern 5 / A6)"
      pattern: "request"
---

<objective>
Deliver the save vertical slice (D-10, WORK-02): the result screen gains the Save chart CTA directly below the identity line, the label-prompt modal with smart default and 1–60 validation, the post-save Saved ✓ state and the dedupe already-saved state, storage-failure recovery copy, and the request-param threading from the confirm screen that stores the inputs revise will need (D-08 groundwork).

Purpose: first user-visible Phase-3 capability — a calculated chart becomes a saved, labeled, private local artifact, with explicit-save-only enforced (PRIV-01).
Output: workspace copy deck, save-prompt + error-card components, use-workspace hook, extended result/confirm screens, green save-flow tests.
</objective>

## Phase Goal (user story)

**As a** LemAstra user, **I want to** name and save the chart I just calculated, **so that** it survives this screen and stays on my device — and nothing is stored until I explicitly choose to save it.

*(Derived from ROADMAP Phase 3 Goal + Success Criteria 1/5.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-private-local-workspace/03-UI-SPEC.md
@.planning/phases/03-private-local-workspace/03-PATTERNS.md
@.planning/phases/03-private-local-workspace/03-03-SUMMARY.md

- 03-UI-SPEC.md §"Save flow (/chart/result additions)" (exact copy: prompt strings, field error, helper, Saved ✓ treatment, dedupe helper, label bounds), §"Error states" workspace rows, §"Interaction Contract" Save paragraph (double-tap protection), §"Accessibility Contract" dialog rules
- 03-PATTERNS.md §save-prompt/delete-confirm analog (calculation-disclosure.tsx controlled-card pattern — but note the UI-SPEC overrides: dialogs are RN Modal), §use-workspace (confirm.tsx useMutation posture: retry false), §result.tsx modification (CTA idiom + local zod schema precedent)
- src/app/chart/result.tsx + src/app/birth/confirm.tsx (files being extended), src/lib/workspace/repository.ts + label.ts (saveChart contract, smartDefaultLabel)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workspace copy deck + save-prompt modal + error card</name>
  <files>src/components/workspace/copy.ts, src/components/workspace/save-prompt.tsx, src/components/workspace/error-card.tsx, src/__tests__/save-prompt.test.tsx</files>
  <read_first>
  - 03-UI-SPEC.md §"Copy Deck" Save flow block (every literal string) and §"Design System" Dialog primitive row (RN Modal, NOT Alert — label input must work cross-platform), §"Error states" table
  - 03-PATTERNS.md §copy.ts (chart/copy.ts structural conventions: header law, SCREAMING_SNAKE constants, templated pure functions), §calculation-disclosure.tsx analog (controlled props + a11y live region)
  - src/components/chart/copy.ts (copy-deck law to replicate), src/components/ui/error-banner.tsx (visual structure to mirror: 1px error border, heading, body, action), src/components/themed-text.tsx + src/constants/theme.ts (primitives/tokens)
  </read_first>
  <behavior>
  - Save prompt renders heading "Save this chart", field label "Chart name", TextInput prefilled with the provided smart default, helper "Saved charts stay on this device. Nothing is sent anywhere."
  - Empty/whitespace or >60-character input disables the confirm button and shows "Enter a name (up to 60 characters)." in a polite live region
  - Confirm emits the trimmed label via onSave; Cancel emits onCancel; both buttons disable while pending
  - Error card renders an error-bordered card with heading, body, and optional action label from the workspace copy deck (structure mirroring error-banner.tsx, fed by props)
  </behavior>
  <action>
  Create the workspace copy deck with the exact UI-SPEC save-flow strings (SAVE_CTA, SAVE_PROMPT_HEADING, CHART_NAME_LABEL, SAVE_PROMPT_HELPER, LABEL_FIELD_ERROR, SAVE_PROMPT_CONFIRM, SAVE_PROMPT_CANCEL, SAVED_STATE with the checkmark-word rule, DEDUPE_HELPER) plus the workspace error-deck constants for later plans (save/export/delete/delete-all/open-failed headings+bodies+actions — one place, consumed by error-card). Build save-prompt.tsx as a controlled RN Modal (centered card, accessibilityViewIsModal, autofocus TextInput, labelSchema.safeParse gating the confirm per keystroke, pending props) — controlled onConfirm/onCancel/pending props, no internal persistence. Build error-card.tsx per the error-banner visual structure taking heading/body/action-label/onAction props. Tests per behavior rows (RNTL /pure pattern).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/save-prompt.test.tsx</automated>
  </verify>
  <done>Copy deck exact-asserted; modal validates and gates per D-10/A-3-UI-4; error card mirrors the Phase-2 banner structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: request param threading + Save CTA on the result screen</name>
  <files>src/hooks/use-workspace.ts, src/app/birth/confirm.tsx, src/app/chart/result.tsx, src/__tests__/save-flow.test.tsx</files>
  <read_first>
  - 03-RESEARCH.md §"Architecture Patterns" Pattern 5 (stored CalculateRequest rationale — the result screen lacks lat/lon/zone today) + §"Open Questions (RESOLVED)" 3 (adopted threading decision) + Assumption A6 (param size ~300B)
  - 03-UI-SPEC.md §"/chart/result — Save entry point" (insertion point below identity line, before Placements) and §"Interaction Contract" Save paragraph
  - src/app/birth/confirm.tsx (buildRequest + router.push params — the file being extended), src/app/chart/result.tsx (param-parsing + redirect guard being extended), src/lib/workspace/repository.ts + label.ts + schema.ts (saveChart, smartDefaultLabel, storedCalculationInputsSchema)
  - src/app/birth/confirm.tsx useMutation block (retry-false posture to copy), src/__tests__/result-screen.test.tsx (fixture builders + router-mock scaffold)
  </read_first>
  <behavior>
  - Confirm screen pushes a third param request = JSON.stringify of the built CalculateRequest plus the draft's place-union branch (flat: date, time, time_resolution?, confidence, house_system, place, place_form, iana_zone, zone_source)
  - Result screen parses the request param through storedCalculationInputsSchema beside the existing envelope/identity parses; a present-but-malformed request param does not redirect (save is disabled instead)
  - Save chart CTA renders below the identity line, above Placements; tapping opens the prompt prefilled with smartDefaultLabel(identity.date, identity.label)
  - Confirming calls repository.saveChart({label, envelope, inputs, identity}) — test asserts the persisted envelope equals the parsed screen envelope and that NO save occurs until the Save CTA is tapped (PRIV-01 explicit-save-only)
  - Success → prompt closes, CTA becomes the Saved ✓ chip (backgroundSelected fill); repository returning appended: false renders Saved ✓ plus DEDUPE_HELPER; failure renders the error card ("Couldn't save the chart." + Try again re-opens nothing lost); double-tap protection: CTA disabled while pending
  </behavior>
  <action>
  Create src/hooks/use-workspace.ts with useSaveChart (useMutation, retry false per T-02-32 posture, invalidates the ['charts'] list key on success — Pitfall 10 map starts here; repository injected through the module seam so tests can fake it per the D-03 seam). Extend confirm.tsx: inside the calculate onSuccess, add the request param per the behavior row (build it from buildRequest() output plus draft.place — one flat object). Extend result.tsx: parse the request param with storedCalculationInputsSchema imported from the workspace schema (single source — do not duplicate the contract), mount the Save CTA + SavePrompt + error card per the UI-SPEC insertion point, wire Saved ✓/dedupe/pending states, and keep the existing redirect guard untouched for envelope/identity. save-flow tests: reuse the result-screen fixture builders; fake the repository module; assert every behavior row including the zero-persisted-before-save case and appended-false dedupe copy.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/save-flow.test.tsx src/__tests__/result-screen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Save slice walkable in tests: calculate → Save chart → label → persisted via repository; dedupe + failure + double-tap states exact; existing result-screen tests stay green (new CTA additive).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router params → screen state | request param crosses an untrusted serialization boundary (parsed with zod before use) |
| user label → repository → filesystem later | Label validated 1–60 trimmed before persist (slug sanitization happens at export in 03-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | request param malformed/stale | mitigate | storedCalculationInputsSchema parse beside existing guards; malformed → save disabled, never a crash |
| T-03-13 | Information Disclosure | implicit persistence (auto-save) | mitigate | explicit-save-only: the only write trigger is the Save confirm handler; save-flow test asserts zero repository writes before the tap (PRIV-01) |
| T-03-14 | DoS | duplicate saves / double-tap (Pitfall 4) | mitigate | CTA disabled while pending + repository (chart, input_revision) dedupe backstop; appended:false surfaces the already-saved state |
</threat_model>

<verification>
- npx vitest run src/__tests__/save-prompt.test.tsx src/__tests__/save-flow.test.tsx
- npx vitest run (full suite) && npx tsc --noEmit
</verification>

<success_criteria>
- WORK-02 complete at the UI layer; PRIV-01's explicit-save law is test-enforced end to end
- D-08 groundwork in place: every saved revision carries the inputs revise needs
</success_criteria>

## Artifacts this phase produces

*Plan 03-04 symbols later plans consume (exclude from drift verification):*
- `src/components/workspace/copy.ts`: save-flow + error-deck copy constants
- `src/components/workspace/save-prompt.tsx`: SavePrompt ({visible/defaultLabel/pending/onSave/onCancel})
- `src/components/workspace/error-card.tsx`: ErrorCard ({heading, body, actionLabel?, onAction?})
- `src/hooks/use-workspace.ts`: useSaveChart + ['charts'] query-key convention
- `src/app/chart/result.tsx`: request-param parsing + Save CTA/Saved ✓ contract
- `src/app/birth/confirm.tsx`: request param in the /chart/result push

<output>
Create `.planning/phases/03-private-local-workspace/03-04-SUMMARY.md` when done
</output>
