---
phase: 03-private-local-workspace
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/redact.ts
  - src/__tests__/redact.test.ts
  - src/__tests__/telemetry-guard.test.ts
autonomous: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "redact() strips envelope-shaped and birth-data-shaped values from any metadata object before it can reach a log call — enforced by unit tests (PRIV-03/04)"
    - "No analytics or crash SDK exists in the dependency graph, no telemetry import exists anywhere in src/, and no direct logging call site exists outside the sanctioned logger module and test files — enforced by automated source-scan tests (D-16)"
  artifacts:
    - path: "src/lib/redact.ts"
      provides: "Allowlist redact() utility + the only sanctioned logger entry point (D-16 guardrail for PRIV-03/PRIV-04)"
      contains: "redact"
    - path: "src/__tests__/telemetry-guard.test.ts"
      provides: "Fail-hard governance scan: no telemetry dependency, no telemetry import, no stray logging call sites"
      contains: "package.json"
  key_links:
    - from: "src/lib/redact.ts"
      to: "later Sentry integration (Phase 7+)"
      via: "logger wraps every future telemetry hook so a beforeSend equivalent inherits the allowlist guardrail"
      pattern: "logger"
---

<objective>
Make the no-telemetry posture enforced in code rather than promised (D-16, PRIV-03/PRIV-04): land the allowlist redact() utility and the sanctioned logger module, plus the guard tests that fail the build if any analytics/crash SDK, telemetry import, or stray logging call site appears.

Purpose: the retention-deletion-policy §4 posture (birth data, chart payloads, questions, prose never leave the device via logs/telemetry) becomes test-enforced before any feature that could leak it exists; the Phase-7+ Sentry integration will inherit this guardrail instead of retrofitting one.
Output: src/lib/redact.ts + redact/telemetry-guard tests wired into the existing vitest CI job.
</objective>

## Phase Goal (user story)

**As a** LemAstra user, **I want to** my birth data and chart contents never to leak through logs or telemetry, **so that** private-by-default is a property of the code, not a promise.

*(Derived from ROADMAP Phase 3 Goal + Success Criterion 5; PRIV-03/PRIV-04.)*

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-private-local-workspace/03-RESEARCH.md
@.planning/phases/03-private-local-workspace/03-PATTERNS.md

- 03-RESEARCH.md §"Architecture Patterns" Pattern 7 (telemetry guardrail as test-enforced convention), §"Security Domain" (PRIV-03/04 rows, information-disclosure threats)
- 03-PATTERNS.md §test archetype 2 (source-scan / governance-docs.test.ts fail-hard pattern) and §No Analog Found row for redact.ts
- docs/governance/retention-deletion-policy.md (§4 log/telemetry redaction posture this encodes), docs/governance/secret-isolation-policy.md (logging conventions)
- src/__tests__/governance-docs.test.ts (the fail-hard source-scan archetype to copy)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: redact() allowlist utility + sanctioned logger</name>
  <files>src/lib/redact.ts, src/__tests__/redact.test.ts</files>
  <read_first>
  - 03-RESEARCH.md §"Architecture Patterns" Pattern 7 (allowlist, coarse fields only — ids without birth data, error codes, durations)
  - 03-PATTERNS.md §`src/lib/redact.ts` (no analog; baseline is zero direct logging call sites in src/)
  - src/lib/api.ts + src/lib/api-schemas.ts (the envelope/birth-data shapes the tests must prove stripped)
  </read_first>
  <behavior>
  - redact({error_code: "CALC_TIMEOUT", duration_ms: 42}) keeps both coarse fields verbatim
  - redact() removes envelope-shaped values: an input containing chart_data / provenance / placements keys has them dropped from the output
  - redact() removes birth-data-shaped values: date / time / place / label / iana_zone / envelope / identity / inputs keys are dropped even when nested one level deep inside an allowlisted container value that is itself an object
  - unknown keys are dropped by default (allowlist, not blocklist); the output never contains a value the caller did not explicitly sanction
  - logger exposes info/warn/error that route through redact() before any output call — the only module in src/ permitted to touch the console global
  </behavior>
  <action>
  Build redact.ts per D-16 and Pattern 7: an exported ALLOWED_LOG_KEYS frozen set (coarse keys only — error codes, durations, counts, chart/revision ids without any birth data), redact(metadata: Record<string, unknown>) returning a new object containing only allowlisted keys whose values are primitives (objects/arrays are dropped wholesale — no deep-merging untrusted structure), and a logger module-level export wrapping the console global through redact() with a module header stating the PRIV-03/04 law and that the Phase-7+ telemetry integration must route through it. Unit tests per the behavior rows, following the pure-unit archetype of api-schemas.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/redact.test.ts</automated>
  </verify>
  <done>Allowlist proven closed: coarse fields survive, every envelope/birth-data shape is stripped, nested objects dropped; logger is the single sanctioned output path.</done>
</task>

<task type="auto">
  <name>Task 2: telemetry guard source-scan tests</name>
  <files>src/__tests__/telemetry-guard.test.ts</files>
  <read_first>
  - 03-RESEARCH.md §"Validation Architecture" PRIV-03/PRIV-04 rows (exact assertions required), §"Security Domain" phase-specific task list item 1
  - src/__tests__/governance-docs.test.ts (fail-hard node:fs scan archetype with threat-naming header comment), package.json (dependency lists to scan)
  </read_first>
  <acceptance_criteria>
  - Test 1 fails if package.json dependencies or devDependencies contain any of: sentry, posthog, amplitude, segment, firebase, datadog, bugsnag, analytics (substring match on names) — passes today
  - Test 2 fails if any src/**/*.{ts,tsx} file (excluding src/__tests__/** and src/test/**) contains a telemetry-module import (the same name list matched against import/require specifiers)
  - Test 3 fails if any src/**/*.{ts,tsx} file outside src/lib/redact.ts and the test/setup directories contains a direct logging call token (console followed by a dot and an output-method name) — baseline today is clean, so the suite is green
  - A positive-control is committed as a skipped/conditional self-check or the test documents the mutation-verified trip (temporarily adding a stray logging call makes the suite exit non-zero — verified once during execution and recorded in the SUMMARY)
  </acceptance_criteria>
  <action>
  Create telemetry-guard.test.ts following the governance-docs.test.ts fail-hard archetype: readFileSync over package.json and a recursive src/ walk (excluding tests/setup), asserting the three conditions above with the threat named in the header comment (PRIV-03: no analytics surface exists; PRIV-04: no stray logging call sites). Prove the gate fires once by temporarily adding a stray logging call, observing the non-zero exit, then reverting — record the positive control in the SUMMARY (Phase-1 scanner-positive-control pattern). Do not create an allowlist file; if a future legitimate call site appears it must extend the sanctioned logger instead.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/telemetry-guard.test.ts src/__tests__/redact.test.ts && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Full suite green with the guards active; positive control recorded; the D-16 posture is now build-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app internals → logs/telemetry | The only boundary PRIV-03/04 govern: anything crossing it must pass redact() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | log call sites leaking birth data / chart payloads | mitigate | redact() allowlist + logger as sole sanctioned output path + telemetry-guard call-site scan (this plan) |
| T-03-05 | Information Disclosure | future analytics/crash SDK auto-collecting sensitive payloads | mitigate | dependency-graph + import scan fails the build if any telemetry package appears; Phase-7+ integration must route through the logger allowlist |
| T-03-06 | Tampering | guard-test erosion (weakening the scan instead of the code) | mitigate | positive-control discipline: the trip must be demonstrated; no allowlist file exists to quietly append to |
</threat_model>

<verification>
- npx vitest run src/__tests__/telemetry-guard.test.ts src/__tests__/redact.test.ts
- npx vitest run (full suite) && npx tsc --noEmit
- SUMMARY records the positive control (guard fires on an injected stray logging call)
</verification>

<success_criteria>
- PRIV-03 and PRIV-04 are enforced by tests that run in the mandatory CI vitest job — adding any telemetry SDK or stray logging call site now breaks the build
- redact() is the seam the Phase-7+ Sentry beforeSend inherits
</success_criteria>

## Artifacts this phase produces

*Plan 03-02 symbols later plans consume (exclude from drift verification):*
- `src/lib/redact.ts`: redact(metadata), ALLOWED_LOG_KEYS, logger (info/warn/error)
- `src/__tests__/telemetry-guard.test.ts`, `src/__tests__/redact.test.ts`: governance gates

<output>
Create `.planning/phases/03-private-local-workspace/03-02-SUMMARY.md` when done
</output>
