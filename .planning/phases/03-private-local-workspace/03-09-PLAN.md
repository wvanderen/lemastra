---
phase: 03-private-local-workspace
plan: 09
type: execute
wave: 7
depends_on: ["03-01"]
files_modified:
  - metro.config.js
  - babel.config.js
  - package.json
  - package-lock.json
  - src/__tests__/bundler-config-guard.test.ts
  - vitest.config.ts
autonomous: false
gap_closure: true
requirements: [WORK-01, WORK-02]
must_haves:
  truths:
    - "npx expo export --platform web exits 0 — the web bundle builds with drizzle/migrations.js's .sql imports resolved (exact reproduction command from the debug session now passes)"
    - "npx expo export --platform ios exits 0 — the same Metro graph native emulators bundle at boot resolves (proxy for the reported emulator boot failure)"
    - "The full vitest suite and tsc --noEmit stay green after the bundler wiring (no test-pipeline regression)"
    - "A guard test fails the CI vitest job if metro.config.js, babel.config.js, or the babel-plugin-inline-import devDependency is ever removed — 03-01's skip can never silently recur"
  artifacts:
    - path: "metro.config.js"
      provides: "Expo default Metro config with the sql resolver extension (drizzle Expo guide Step 6)"
      contains: "sourceExts"
    - path: "babel.config.js"
      provides: "babel-preset-expo + inline-import plugin for .sql imports (drizzle Expo guide Step 7)"
      contains: "inline-import"
    - path: "src/__tests__/bundler-config-guard.test.ts"
      provides: "fail-hard config-presence guard in the telemetry-guard.test.ts archetype"
      contains: "bundler"
  key_links:
    - from: "metro.config.js"
      to: "drizzle/migrations.js"
      via: "resolver sourceExts gains sql so Metro can resolve the ./0000_nebulous_meggan.sql import on line 4"
      pattern: "sourceExts"
    - from: "babel.config.js"
      to: "drizzle/migrations.js"
      via: "inline-import transforms the .sql module into a raw string export the drizzle-orm expo-sqlite migrator consumes (migrations.m0000)"
      pattern: "inline-import"
    - from: "src/__tests__/bundler-config-guard.test.ts"
      to: "metro.config.js"
      via: "reads the config source and fails when the sql resolver wiring is absent"
      pattern: "readFileSync"
---

<objective>
Close the UAT Test 1 blocker (03-UAT.md, severity: blocker, 4 tests blocked): the app cannot boot on web or native because Metro cannot resolve the `.sql` import in drizzle-kit's generated `drizzle/migrations.js`. Root cause fully diagnosed in `.planning/debug/app-boot-crash-drizzle-migration.md` — plan 03-01 wired drizzle but skipped Steps 6–7 of drizzle's official Expo setup guide (metro + babel bundler wiring), and every pre-UAT gate (vitest virtual modules, tsc companion `.d.ts`, unexecuted CI bundle-scan) masked the gap.

Purpose: Restore the observable truth "App boots on web and the saved chart reopens from the home list after force-quit/relaunch" (WORK-01) by making the storage engine's migration artifacts (WORK-02) bundleable on every platform, and install a guard so the omission can never recur silently.

Output: `metro.config.js`, `babel.config.js`, `babel-plugin-inline-import` devDependency, `src/__tests__/bundler-config-guard.test.ts`, corrected vitest.config.ts comment — proven by `npx expo export --platform web` and `--platform ios` both exiting 0.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-private-local-workspace/03-UAT.md
@.planning/debug/app-boot-crash-drizzle-migration.md
@.planning/phases/03-private-local-workspace/03-01-SUMMARY.md

# Read-first sources (importer is CORRECT — do not modify; config-comment fix target; guard-test archetype)
@src/lib/workspace/db.ts
@vitest.config.ts
@drizzle/migrations.js
@src/__tests__/telemetry-guard.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve babel-plugin-inline-import install (package legitimacy gate)</name>
  <what-built>
Nothing yet — this is the pre-install legitimacy gate for the ONE new devDependency this fix requires: `babel-plugin-inline-import`. It is absent from 03-RESEARCH.md's Package Legitimacy Audit (the table predates this gap), so per the fallback policy it is treated as [ASSUMED] and requires human verification before install. This checkpoint is never auto-approvable.
  </what-built>
  <action>
Blocking pre-install legitimacy checkpoint (no automated work — the gate itself is the action). `babel-plugin-inline-import` is [ASSUMED] (absent from 03-RESEARCH.md's Package Legitimacy Audit); human must verify it on npmjs.com before Task 2 may run `npm i -D`. Details in how-to-verify below.
  </action>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/babel-plugin-inline-import
    2. Confirm the package matches the registry metadata the planner already pulled (registry.npmjs.org/babel-plugin-inline-import/latest, fetched 2026-08-29): latest 3.0.0 (published 2018 — stable and unchanged since), license MIT, author Quadric ApS (Victor Duarte), source repo github.com/Quadric/babel-plugin-inline-import, 6 files / ~12 KB unpacked, single runtime dependency `require-resolve@0.0.2`, scripts contain only a `prepare` build step (no postinstall/preinstall), not deprecated, dist integrity sha512-thnykl4F…
    3. Cross-check legitimacy: this exact plugin (with the .sql extension option) is mandated by drizzle's official Expo setup guide Steps 6–7 (https://orm.drizzle.team/docs/get-started/expo-new) — the canonical fix for this diagnosed bug.
    4. Confirm you approve installing it as a devDependency at the root package.json.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe concerns about the package</resume-signal>
  <verify>Human confirmation via resume-signal: "approved" (or concerns addressed before Task 2 runs)</verify>
  <done>Package legitimacy human-verified and install approved; Task 2 unblocked</done>
</task>

<task type="auto">
  <name>Task 2: Install plugin + create metro/babel bundler wiring (drizzle Expo guide Steps 6–7)</name>
  <files>metro.config.js, babel.config.js, package.json, package-lock.json</files>
  <action>
Install and create the three missing items from the gap's `missing` list (exact fix_direction from the debug session):

1. Install the devDependency approved in Task 1: `npm i -D babel-plugin-inline-import` (resolves to ^3.0.0). Verify it lands in devDependencies, not dependencies — it is build-time only and must never ship in the app dependency graph.

2. Create `metro.config.js` (repo root, CommonJS): require `getDefaultConfig` from `expo/metro-config`, call it with `__dirname` to get Expo SDK 57's default config, then append the string `sql` to `config.resolver.sourceExts` behind an includes-guard so re-requiring the config never duplicates the entry (idempotent). Export the config via module.exports. This is drizzle Expo guide Step 6 — it is what lets Metro resolve the `./0000_nebulous_meggan.sql` import on line 4 of `drizzle/migrations.js` (the debug session's resolver error showed the tried-extension list has no sql entry).

3. Create `babel.config.js` (repo root, CommonJS): a `presets` array containing `babel-preset-expo` (a transitive dependency of `expo` — resolvable from the repo root; using it explicitly matches Expo's own template and keeps the worklets/reanimated plugin auto-inclusion identical to the previous implicit default — do NOT add any reanimated/worklets plugin manually, babel-preset-expo handles it), and a `plugins` array containing the `inline-import` plugin configured with a `extensions` option of `[".sql"]` — drizzle Expo guide Step 7. This transforms the .sql import into a raw string module, which is exactly the shape `drizzle-orm/expo-sqlite/migrator`'s `readMigrationFiles` expects (`migrations.m0000` as a raw SQL string it splits on the statement-breakpoint marker).

DO NOT modify `drizzle/migrations.js` or `src/lib/workspace/db.ts` — the debug session eliminated both as causes (generated file matches the migrator contract; the relative import path resolves; the failure is purely resolver-extension config). Both files stay byte-identical.

Caching note: if `expo export` still reports the old resolution error after the configs exist, rerun with `--clear` — Metro caches resolver configuration.

Why: Metro is the bundler for BOTH web and native (dev, export, and CI bundle-scan), so this single wiring unblocks web boot AND native emulator boot (UAT Tests 1–5).
  </action>
  <verify>
    <automated>npm ls babel-plugin-inline-import && node -e "const m=require('./metro.config.js');const b=require('./babel.config.js');if(!m.resolver.sourceExts.includes('sql'))throw new Error('sql missing from sourceExts');if(!JSON.stringify(b).includes('inline-import'))throw new Error('inline-import missing from babel config');console.log('bundler config ok')"</automated>
  </verify>
  <done>babel-plugin-inline-import present in devDependencies; metro.config.js loads and its resolver.sourceExts contains sql; babel.config.js loads and references the inline-import plugin; drizzle/migrations.js and src/lib/workspace/db.ts untouched</done>
</task>

<task type="auto">
  <name>Task 3: Bundler-config guard test + correct the masking vitest comment</name>
  <files>src/__tests__/bundler-config-guard.test.ts, vitest.config.ts</files>
  <action>
1. Create `src/__tests__/bundler-config-guard.test.ts` following the fail-hard source-scan archetype of `src/__tests__/telemetry-guard.test.ts` (readFileSync + expect-throws-inside-vitest → non-zero exit → CI fails). Assert, from the repo root:
   - `metro.config.js` exists and its source text contains BOTH the getDefaultConfig usage (expo/metro-config) AND the sql resolver-extension registration — a stale or deleted metro config must fail the suite.
   - `babel.config.js` exists and its source text contains BOTH the babel-preset-expo preset AND the inline-import plugin configured for the .sql extension.
   - `package.json` devDependencies includes babel-plugin-inline-import (a mistaken move to dependencies or removal must fail).
   The doc-comment must state WHY this guard exists: 03-01 skipped drizzle Expo guide Steps 6–7, every pre-UAT gate masked it (vitest virtual modules in vitest.config.ts, tsc satisfied by the companion drizzle/migrations.d.ts, CI bundle-scan never ran because the phase branches were unpushed), and the gap surfaced only as the UAT Test 1 boot crash on all platforms. Reference `.planning/debug/app-boot-crash-drizzle-migration.md`. No allowlist file — weakening the scan instead of the config is the anti-pattern this guard exists to surface (same guard-erosion stance as telemetry-guard).

2. In `vitest.config.ts`, correct the ONE false sentence in the drizzleMigrationsIndex comment block — the text asserting Metro bundles it fine on device so only the test pipeline needs help. Replace it with the truth: real bundling works because metro.config.js registers sql as a resolver extension and babel.config.js inlines .sql imports as strings (03-09, drizzle Expo guide Steps 6–7), and these vite plugins remain the test-pipeline mirror of that wiring because vite consumes neither metro nor babel config. Change ONLY that comment sentence — the plugin behavior (sqlAsRawString, drizzleMigrationsIndex virtual module) stays exactly as-is; it is still required and still serves the same journal+sql content the real bundler now inlines.

3. Mutation-verify the guard once (positive control, telemetry-guard precedent): temporarily rename metro.config.js (e.g. to metro.config.js.bak), run the new suite, confirm it exits non-zero, restore the file, re-run to green. Record the verified trip in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/bundler-config-guard.test.ts</automated>
  </verify>
  <done>Guard suite green asserting all three artifacts; mutation control confirmed the suite fails when metro.config.js is absent (then restored); vitest.config.ts false assumption replaced with the truthful bundler-wiring statement; plugin code paths unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | New build-time package `babel-plugin-inline-import` enters the toolchain (no runtime trust boundary — it never ships in the app graph; no user input crosses any new boundary in this fix) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of babel-plugin-inline-import | mitigate | [ASSUMED] package (absent from 03-RESEARCH.md audit table) → blocking-human legitimacy checkpoint BEFORE install (Task 1, never auto-approvable); registry metadata pre-verified by planner (MIT, Quadric, 6 files, no install scripts, integrity sha512-thnykl4F…); post-install `npm ls` in Task 2 verify |
| T-03-GC-01 | Tampering | metro.config.js / babel.config.js (build config integrity — a silent removal reintroduces the all-platform boot crash) | mitigate | bundler-config-guard.test.ts in the mandatory CI vitest job fails on deletion/staleness of either config or the devDependency; no allowlist file to erode |
| T-03-GC-02 | Information Disclosure | drizzle/*.sql migration artifacts bundled into the web/native output | accept | migration SQL is schema DDL (table/column names), already committed to the repo — no personal or secret data inlines; identical exposure to every Expo app shipping schema |
</threat_model>

<verification>
1. `EXPO_NO_TELEMETRY=1 npx expo export --platform web` → exit 0 (the exact reproduction command from the debug session, previously exit 1 with "Unable to resolve module ./0000_nebulous_meggan.sql"; also the exact command the CI bundle-scan job runs, so the latent CI failure clears before the branch is ever pushed).
2. `npx expo export --platform ios` → exit 0 (exercises the same Metro graph native emulators bundle at boot — automated proxy for the reported emulator failure, no emulator required).
3. `npm test` → full suite green (vitest is unaffected by babel/metro config — vite consumes neither; proves the vitest.config.ts comment fix changed no behavior).
4. `npx tsc --noEmit` → green (metro.config.js/babel.config.js are root .js files outside tsconfig's **/*.ts(x) include).
5. UAT re-run happens after execution via /gsd-verify-work — Tests 1–5 unblocked by this plan.
</verification>

<success_criteria>
- The diagnosed blocker is closed at the bundler level: web export and ios export both exit 0 with the drizzle migration .sql imports resolved inline.
- All 5 blocked UAT tests (restart survival, share-sheet exports, revise round-trip, MVP walkthrough, modal quality) become re-runnable — the boot crash affected every platform and every test.
- The regression is structurally prevented: deleting/staling any of the three wiring artifacts fails the mandatory CI vitest job.
- No collateral damage: full vitest suite and tsc stay green; drizzle/migrations.js, src/lib/workspace/db.ts, and all app source untouched.
- SUMMARY records the guard's mutation-verified positive control.
</success_criteria>

<output>
Create `.planning/phases/03-private-local-workspace/03-09-SUMMARY.md` when done
</output>
