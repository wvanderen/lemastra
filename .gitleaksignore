# LemAstra gitleaks fingerprint allowlist (GATE-06).
#
# CONTRACT — binding for every entry added to this file
# (authority: docs/governance/secret-isolation-policy.md §3):
#
#   1. Every fingerprint line MUST be preceded by two comment lines:
#        # class: publishable-identifier   <- the only class that may be allowlisted
#        # rationale: <what the identifier is and why it is safe to ship>
#      (e.g. Supabase publishable key with RLS-enforced authorization, Sentry DSN,
#      API endpoint URL — see the policy's classification table).
#   2. Entries claiming any other class (secret, user secret) are INVALID and
#      must be rejected in review — that is allowlist abuse (threat T-01-11).
#   3. An unannotated fingerprint is a gate violation, not a style issue:
#      silent ignores destroy the scanner's signal.
#
# The first entry below is a scanner false positive on a non-secret upstream
# fixture (introduced by the 02-01 vendor snapshot), not a client identifier.
# Future Supabase/Sentry publishable identifiers append below the same
# class + rationale comment pair.

# class: publishable-identifier
# rationale: "America/Kentucky/Lexington" — IANA-zone-shaped test constant in the
#   pinned public vendored skill (vendor/astrology-skill/tools/tz_smoke_test.py L142;
#   upstream fixture exercising invalid-zone error copy). Public by construction in
#   the upstream repo, carries no authorization, safe to ship; tripped the
#   generic-api-key entropy heuristic. A vendor promotion that moves this line
#   re-flags it — re-verify the class before re-allowlisting.
1d9772af027f3e49cb09b71f4b46e452b789f484:vendor/astrology-skill/tools/tz_smoke_test.py:generic-api-key:142
